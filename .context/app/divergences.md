---
name: divergences
description: Every Lelañea edit to a file Daybreak or Sunrise owns, why it was forced, and what would let us delete it.
---

# Divergence ledger

Lelañea owns the `leaf-*` seams, `.context/app/`, `components/app/`,
`prisma/schema/app.prisma`, `app/brand-theme.css`, and any new file it creates.
Everything else arrives from Daybreak (which carries Sunrise through) and merges
on every sync.

**A row here is the price of editing one of those files.** Without it, the next
sync meets the change as a conflict with no explanation, and the tempting
resolution — take upstream — silently reverts a decision somebody made on
purpose. So: every edit to a file we do not own gets a row, in the same PR as
the edit (`sunrise.divergences`).

**Filling a seam is not a divergence.** `lib/app/leaf-*.ts`,
`lib/app/reserved-tiers.ts`, `app/brand-theme.css` and the rest of the empty
`lib/app/*` scaffolds exist precisely to be filled, and they merge cleanly.
Only files an upstream tier owns _and keeps evolving_ belong here.

**Every row carries a deletion trigger** — the specific thing that would make it
removable. A ledger without them only grows.

## How to use this on a sync

Read the rows before you merge, not after. For each conflict in a file named
here, the "on conflict" column says what to do; the default is to re-apply our
change on top of upstream's, never to take one side wholesale.

---

## Row 1 · `app/layout.tsx` — brand fonts on `<html>`

|                      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| -------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **File**             | `app/layout.tsx` (Sunrise)                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| **Change**           | Imports `brandFontVariables` from `app/fonts.ts` and adds it as `className` on `<html>`.                                                                                                                                                                                                                                                                                                                                                                                                       |
| **Forced by**        | `next/font` returns a class that must be applied to a real element; there is no seam that applies one. `lib/app/` is out — the platform's own ESLint boundary bans runtime `next/*` imports there and names `app/` as the remedy. A route-group layout would miss body-portaled overlays (`.context/ui/surface-theming.md` constraint 1), leaving every dialog and toast in the browser's default face.                                                                                        |
| **On conflict**      | Keep both. Take upstream's `<html>` attributes and re-add `className={brandFontVariables}`. If upstream has introduced its own `className` there, compose rather than replace.                                                                                                                                                                                                                                                                                                                 |
| **Upstream status**  | Not filed. This is ordinary fork work that a seam would tidy, not a defect. Known cost: `next/font` defaults to `preload: true`, so all seven woff2 files are preloaded (Instrument Serif normal + italic, Hanken Grotesk 400/500/600, Cormorant Garamond italic 400 + 500) on `/admin` too, where `brand-theme.css` never maps them onto a font token. Accepted — `preload: false` would delay the faces on the surfaces that DO use them, to save bandwidth on an internal, low-traffic one. |
| **Deletion trigger** | Daybreak or Sunrise ships a font seam — any scaffold whose value the root layout applies to `<html>`. Move the three loaders into it and delete this row.                                                                                                                                                                                                                                                                                                                                      |

## Row 2 · `app/layout.tsx` + `hooks/use-theme.tsx` — the system preference stays a default

|                      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| -------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **Files**            | `app/layout.tsx` (the no-flash script) and `hooks/use-theme.tsx` — both Sunrise                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| **Change**           | Both stop writing the resolved system preference to `localStorage`; the hook follows `prefers-color-scheme` for as long as nothing is stored. **The hook's public shape now DIVERGES too** (t-23): `choice: Theme \| null` reports the stored choice, and `clearTheme()` forgets it and resolves back to the device. `setTheme`'s signature and the storage key and `<html>` class are untouched — both additions are new names, so an upstream change to `setTheme` still merges cleanly beside them.                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| **Forced by**        | Upstream persisted the _default_ as though it were a choice, so from the second visit onward "hasn't chosen" and "chose light" were the same stored state, and a later OS switch was never followed. Decision D4 (owner, 8 September 2026) is that the system preference is the default and only the toggle persists. It was first written up here as invisible in vanilla Sunrise ("both themes near-greyscale"); that was wrong — Sunrise's white and near-black are as different as ours, and the bug shows on any deployment to a user whose OS preference changes after their first visit. Corrected on sunrise#756 (16 September 2026). **The shape change is forced by the same decision**: D4 gives the app three states and upstream publishes two, so `/app/settings` could neither report "following your device" nor return to it. t-11 worked around the first half by reading `localStorage` in the view, duplicating the hook's private key; t-23 removed that. |
| **On conflict**      | Keep ours. The two files must agree: the script's resolution and the hook's initial state are the same expression, and if they diverge the first paint disagrees with the first render. `sessionIntent` is a THREE-state ref (`'chose'` / `'cleared'` / `null`), widened from the boolean THIS ROW's first half introduced — upstream has no ref, no `matchMedia` listener and no wrapping `setTheme` at all, so there is no upstream artefact to reconcile it against — both writers swallow a storage failure on purpose, so storage is not a reliable record of what the reader just did and the OS listener needs to know which way to fail. If upstream has rewritten theme handling entirely, check whether it now distinguishes "no choice" from an explicit one — if it does, take upstream and delete this row.                                                                                                                                                       |
| **Upstream status**  | **[sunrise#756](https://github.com/human-centric-engineering/sunrise/issues/756), open.** Originally [daybreak#236](https://github.com/human-centric-engineering/daybreak/issues/236), closed `NOT_PLANNED` on 10 September 2026 as filed at the wrong tier — `hooks/use-theme.tsx` and `app/layout.tsx` are byte-identical in Sunrise and Daybreak, so Daybreak could not have fixed either without editing a file it does not own. This row pointed at the closed issue until t-23. Reported as a defect with this repo as the reference implementation; we did not volunteer the platform's fix (`B7`).                                                                                                                                                                                                                                                                                                                                                                     |
| **Deletion trigger** | sunrise#756 lands: theme resolution that treats an absent `localStorage` value as "follow the system" rather than writing to it, whether as a fix or behind a seam. Check on the sync that carries it, not before — a partial fix moving only the hook still needs our layout-script change. **Note the row has two halves now**: a fix to the persistence defect does not necessarily bring `choice` and `clearTheme` with it, and `/app/settings` needs both. Drop only the half upstream actually replaces.                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |

## Row 3 · `components/surface-sync.tsx` — the surface must swap before paint

|                      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| -------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **File**             | `components/surface-sync.tsx` (Sunrise)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| **Change**           | `useEffect` → a guarded `useIsomorphicLayoutEffect`, so `data-surface` is rewritten before the browser paints. One added import, one changed hook; the effect body is untouched.                                                                                                                                                                                                                                                                                                                                                                                 |
| **Forced by**        | `useEffect` runs after paint, so a client-side navigation between two differently-themed surfaces shows one frame of the old theme. Upstream is right that this is harmless in vanilla Sunrise — an empty `brand-theme.css` means no theme delta — but we are the fork that filled it, and `components/auth/user-button.tsx` puts a `<Link href="/admin">` in the site header (via `components/layouts/header-actions.tsx`). So an admin moving between the app and `/admin` sees a full frame of oyster white over the admin's white, and charcoal coming back. |
| **On conflict**      | Keep ours, unless upstream has itself moved to a layout effect — in which case take upstream. This is upstream's own documented remedy, not an invention, so a conflict most likely means the file was restructured rather than that the advice changed.                                                                                                                                                                                                                                                                                                         |
| **Upstream status**  | Not filed as a defect: upstream's comment already describes this exact case and names this exact fix, so there is nothing to report. It is a fork switch that Sunrise anticipated.                                                                                                                                                                                                                                                                                                                                                                               |
| **Deletion trigger** | Sunrise makes the layout effect the default, or ships the timing as a prop/seam. Also removable if `classifySurface` ever collapses to a single surface here, since the delta is what makes the frame visible.                                                                                                                                                                                                                                                                                                                                                   |

## Row 4 · ~~`scripts/ci/scoped-tests.ts` — a leaf entry in `ALWAYS_RUN_TESTS`~~ — RETIRED

**Closed by Daybreak 0.3.0.** Our four always-run entries now live in
`lib/app/leaf-ci.ts` (`leafAlwaysRunTests`), and `scripts/ci/scoped-tests.ts` is
byte-identical to upstream again.

The row's own deletion trigger was written per entry — "this test is deleted, or
the thing that makes its input a file goes away" — and none of those happened.
It closed the other way: **a seam arrived**. Sunrise #759/#762 added
`lib/app/ci.ts`, Daybreak 0.3.0 made that one of its bridges and reserved
`lib/app/leaf-ci.ts` for us, and the entries moved across unchanged, reasons and
all. Daybreak deleted its own keep-mine edits to the same file in the same
release.

**The lesson worth keeping is `HB5`**: a divergence forced at plan time is not
forced forever, and the seam catalog is the cheap thing to re-read on a sync.
This row was carried for three tasks and closed with no change to the tests it
protected.

Kept as a stub rather than deleted outright because rows 9, 10 and 11 refer to
each other by number; renumbering to close a gap would break those references
for no gain. Numbering stays stable, and the next sync can see what happened.

## Row 5 · `tests/unit/components/layouts/protected-nav.test.tsx` — pinned to our nav

|                      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| -------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **File**             | `tests/unit/components/layouts/protected-nav.test.tsx` (Sunrise — blob identical on `sunrise/main` and `daybreak/main` at `4ed01375` before this change)                                                                                                                                                                                                                                                                                                                                       |
| **Change**           | One case. `renders the platform default links when no override is set` → `renders this fork's nav from the filled seam`: asserts `/app` "Your journey" in place of `/dashboard` "Dashboard", keeps the `/profile` and `/settings` assertions untouched, and adds one asserting no `/dashboard` link survives. The other six cases are unchanged.                                                                                                                                               |
| **Forced by**        | `HB2`, one tier down. The case reads `lib/app/protected-nav.ts` unmocked and asserts the platform default; §04 t-9 fills that seam, so it necessarily fails. Pinned rather than deleted because what it really protects is the component's end of the seam — that `ProtectedNav` reads the list, renders every entry and gets the hrefs right — and that is worth keeping whatever the values are. The generic replacement mechanism stays covered by the case below it, which mocks the seam. |
| **On conflict**      | Keep ours, and re-read it. A conflict most likely means upstream changed `DEFAULT_PROTECTED_NAV` — which does not affect us, since we replace it wholesale — or restructured the file. If upstream has since given the case a way to run against a filled seam (a fixture, or mocking the seam as the third case already does), take upstream and delete this row: that is strictly better than pinning.                                                                                       |
| **Upstream status**  | Not filed, and nothing to file. This is the documented consequence of filling a seam, and `defaults.test.ts` says so in its own header. Worth noting for the tier above, though: `defaults.test.ts` is _designed_ to be pinned by a fork and says so, while this file asserts the same default with no such note — so a fork meets it as a surprise. If it recurs on another seam, that is worth raising as a docs gap rather than a defect.                                                   |
| **Deletion trigger** | We stop replacing the platform nav — either `lib/app/protected-nav.ts` returns to `null`, or upstream drops `/dashboard` from `DEFAULT_PROTECTED_NAV` so the default and our value agree. Also removable if upstream reworks the case to mock the seam.                                                                                                                                                                                                                                        |

## Row 6 · `app/(public)/layout.tsx` — the public frame is the leaf's

|                      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| -------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **File**             | `app/(public)/layout.tsx` (Sunrise)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| **Change**           | Three imports and two elements: `AppHeader` + `PublicNav` → `SiteHeader`, `PublicFooter` → `SiteFooter`, both from `components/app/site/`. `MaintenanceWrapper`, the metadata block and the `<main>` are untouched.                                                                                                                                                                                                                                                                                                                                                                                                                 |
| **Forced by**        | The design's bar ENDS with a "Join the waitlist" action, and `AppHeader` closes with `HeaderActions` — `ThemeToggle` plus `UserButton`, no props — so no seam reaches that position; `publicNavItems` governs a list of links. The footer carries the "not a medical application… not a crisis service" disclaimer between the links and the legal line, and the footer seams are two link lists plus `footerCopyright`, a single attribution string with no paragraph slot. Filling the seams would have produced a header missing the one control the site exists to offer and a footer missing the paragraph it exists to carry. |
| **On conflict**      | Keep ours for the two elements; take upstream for everything else in the file. If upstream restructures the group (a new wrapper, a changed `<main>`), re-apply the two swaps inside its new shape rather than reverting the file. Do NOT resolve by taking upstream wholesale: that silently restores the platform frame and drops both the waitlist CTA and the disclaimer, and every test still passes.                                                                                                                                                                                                                          |
| **Upstream status**  | Not filed as a defect — a fork replacing its own marketing frame is ordinary. Worth raising as a gap only if a second Daybreak leaf hits it: the shape that would fix it is an actions slot on `AppHeader` and a block slot on `PublicFooter`.                                                                                                                                                                                                                                                                                                                                                                                      |
| **Deletion trigger** | `AppHeader` gains a trailing-actions slot AND `PublicFooter` gains a block/notice slot, at which point the design fits the seams and both leaf components collapse into `lib/app/public-nav.ts` + the new slots. A partial fix (either one alone) does not close this row.                                                                                                                                                                                                                                                                                                                                                          |

## Row 7 · The contact route is gone, and three platform surfaces linked to it

|                                   |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| --------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Files**                         | Deleted: `app/(public)/about/page.tsx`, `app/(public)/contact/page.tsx`. Edited: `app/error.tsx`, `app/global-error.tsx`, `components/layouts/protected-footer.tsx`, `tests/unit/app/global-error.test.tsx`, `tests/unit/components/layouts/protected-footer.test.tsx` — all Sunrise. **Deliberately NOT touched: `app/api/v1/contact/route.ts` and `components/forms/contact-form.tsx`.**                                                                                                                                                                                                                |
| **Change**                        | Both placeholder marketing pages deleted. The three "contact support" / "Help & Support" affordances that pointed at `/contact` are removed rather than repointed. The two tests asserting those links are INVERTED — they now assert no link to a route that does not exist — not deleted.                                                                                                                                                                                                                                                                                                               |
| **Forced by**                     | The contact page hardcoded "Have a question about Sunrise?" and would have shipped that sentence on lelanea.com; the design has no contact or about route. Deleting it left three platform surfaces linking to a 404, two of them error boundaries — the one place a user arrives when everything else has already failed. D3 (owner, f-public) rules nothing rather than a dead link, and an internal 404 is a dead link. Owner confirmed removal over repointing to `/`, which would have sent "contact support" to a marketing page.                                                                   |
| **The backend stays, on purpose** | Deleting the page left `POST /api/v1/contact` (unauthenticated, writes a row, emails an admin, 5/hr/IP) and `components/forms/contact-form.tsx` with no consumer, which reads as orphaned attack surface. They stay anyway: **f-public t-7 names that route as the working template for the waitlist** — per-flow limiter, honeypot in `lib/validations/contact.ts`, Zod, persist, notify — so removing it would delete the reference implementation the next task points at, and t-7 would rebuild it from memory. Revisit at f-public close-out: if t-7 ended up not modelling on it, delete both then. |
| **On conflict**                   | Keep ours. If upstream has moved the support link somewhere new, remove it there too and extend this row rather than accepting it. Taking upstream restores a link to a page this repo does not have, and nothing fails — the tests would be upstream's again as well.                                                                                                                                                                                                                                                                                                                                    |
| **Upstream status**               | Not filed. Sunrise is right to ship a support route; a fork deleting it is the fork's business. Nothing upstream is defective.                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| **Deletion trigger**              | Lelañea gains a real support destination — a contact page of its own, a help centre, or a support address. Then repoint all three affordances at it, restore the upstream form of both tests, and delete this row. This is the row most likely to be closed by product rather than by a sync.                                                                                                                                                                                                                                                                                                             |

## Row 8 · `app/layout.tsx` — `metadataBase`, so a shared link unfurls

|                      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| -------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **File**             | `app/layout.tsx` (Sunrise) — one field added to the `metadata` export.                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| **Change**           | One field on the `metadata` export — `metadataBase: resolveMetadataBase(process.env.NEXT_PUBLIC_APP_URL)` — plus its import. The resolution is OURS, in `lib/site/metadata-base.ts`, so this file carries a call rather than an expression. **Do not re-inline it as `new URL(process.env.NEXT_PUBLIC_APP_URL ?? '…')`**: that was the first form and it is wrong, because Docker's `ENV X=$ARG` yields the empty string rather than `undefined`, `??` does not catch it, and `new URL('')` throws at root-layout module scope. |
| **Forced by**        | Next resolves a relative `og:image` against `metadataBase`, and with none set falls back `VERCEL_URL` → `VERCEL_PROJECT_PRODUCTION_URL` → `http://localhost:3000` (`next/dist/lib/metadata/resolvers/resolve-url.js`). This app deploys via Docker, so none of the Vercel variables exist and every link shared to Slack, X or LinkedIn would have unfurled against localhost. Vanilla Sunrise ships no OG image, so the gap only became load-bearing when t-5 added `app/opengraph-image.tsx`.                                 |
| **On conflict**      | Keep ours, and compose. If upstream has introduced its own `metadataBase`, take upstream — but check it derives from a configured URL rather than a Vercel variable before deleting this row.                                                                                                                                                                                                                                                                                                                                   |
| **Upstream status**  | Not filed yet. Arguably a genuine upstream gap rather than fork work: any Docker-deployed fork that adds a social card meets the same silent localhost fallback, and `NEXT_PUBLIC_APP_URL` already exists as the obvious source. Worth filing against Sunrise if a second fork hits it — the blob-comparison check in `CLAUDE.md` should decide the tier before anything is opened.                                                                                                                                             |
| **Deletion trigger** | Sunrise sets `metadataBase` in the root layout from a configured app URL. Then take upstream's and delete this row — but confirm it is not keyed on `VERCEL_URL`, which is absent in this deployment and would reinstate the same defect while looking fixed.                                                                                                                                                                                                                                                                   |

## Row 9 · `app/(public)/terms/page.tsx` — her Terms of Use, not the starter's

|                      |                                                                                                                                                                                                                                                                                                                                                                                           |
| -------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **File**             | `app/(public)/terms/page.tsx` (Sunrise) — replaced whole.                                                                                                                                                                                                                                                                                                                                 |
| **Change**           | The starter's eight placeholder sections are gone. The page now renders the `terms_of_use` foundational document through `AuthoredDocument`, and its metadata is ours (title, description, canonical).                                                                                                                                                                                    |
| **Forced by**        | The upstream body says "This is a placeholder terms of service. Replace this content with your actual terms" and links `legal@example.com`. There is no seam: a route is a page file, and a page is replaced rather than configured. Lelañea's terms are authored content, so the replacement is a render of the document rather than prose written here — see `.context/app/content.md`. |
| **On conflict**      | Keep ours, wholesale. There is nothing of upstream's to compose with — a conflict here means Sunrise edited its own placeholder, which is of no interest to a fork that has real terms. The only thing worth reading in upstream's version is a change to the surrounding layout or metadata conventions; take that part.                                                                 |
| **Upstream status**  | Not filed, and nothing to file. Sunrise is right to ship a placeholder legal page, and every fork is expected to replace it. This row exists for the merge mechanics.                                                                                                                                                                                                                     |
| **Deletion trigger** | None that a sync can produce — this row closes only if Lelañea stops having its own terms, which is not a thing that happens. It is the one permanent row here, and it is cheap: the conflict resolution is always "keep ours".                                                                                                                                                           |

## Row 10 · `app/(public)/privacy/page.tsx` — the template, kept and labelled

|                      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| -------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **File**             | `app/(public)/privacy/page.tsx` (Sunrise) — two additions, body otherwise untouched.                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| **Change**           | `robots: { index: false }` on the `metadata` export, and one notice element after the `h1` saying the policy is interim and pointing at `/data` and `/disclaimer`. Plus the `next/link` import both need. The starter's eight sections are left exactly as they are.                                                                                                                                                                                                                                                                                 |
| **Forced by**        | Decision D8 (owner, f-public): Sunrise's generic privacy page STAYS and is linked as interim until Lelañea's own policy exists. Deleting it would break the footer link and leave the site with no privacy policy at all; writing one here would put a legal document on lelanea.com that no lawyer has seen. The `noindex` stops "This is a placeholder privacy policy" being what a search for lelanea.com's privacy policy returns. The Terms of Use cross-reference this page (clause 12) and the authored file flags it as an open review note. |
| **On conflict**      | Keep both. Take upstream's body and re-apply the two additions — they are deliberately separable for exactly this. If upstream has restructured the page, the notice goes directly under whatever heading opens it.                                                                                                                                                                                                                                                                                                                                  |
| **Upstream status**  | Not filed. A fork labelling a template it has not replaced yet is fork work, not an upstream defect.                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| **Deletion trigger** | Lelañea's own privacy policy is written. Then this page is replaced the way `/terms` was (row 9), both additions go with the body, and this row is deleted — leaving row 9's shape behind it. Closed by product, not by a sync.                                                                                                                                                                                                                                                                                                                      |

## Row 11 · `app/sitemap.ts` — the routes this site actually has

|                      |                                                                                                                                                                                                                                                                                                                                                                        |
| -------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **File**             | `app/sitemap.ts` (Sunrise) — the `publicPages` array only.                                                                                                                                                                                                                                                                                                             |
| **Change**           | The starter's `/about` and `/contact` entries are gone with the pages themselves (row 7), replaced by `/lelanea`, `/mission`, `/data` and — from t-6 — `/disclaimer`. The surrounding function, base URL resolution and return shape are untouched.                                                                                                                    |
| **Forced by**        | A sitemap listing two routes that 404 and omitting four that exist is worse than none. There is no seam: the route list is a literal inside the file. **This row is late** — the edit landed with t-5 and went unledgered, which is precisely the failure the ledger exists to stop, and it was found by the ownership check at t-6 rather than by anything automatic. |
| **On conflict**      | Keep ours for the entries, take upstream for everything else. If Sunrise adds a route we also have, add it; if it adds one we deleted, do not. The array is the only part in dispute.                                                                                                                                                                                  |
| **Upstream status**  | Not filed. A fork's sitemap listing the fork's routes is the file working as intended.                                                                                                                                                                                                                                                                                 |
| **Deletion trigger** | Sunrise derives the sitemap from the route tree rather than from a hand-kept array — at which point the entries maintain themselves and this row is dead weight. Until then it is permanent, like row 9.                                                                                                                                                               |

## Row 12 · Four platform tests that assert the leaf seams are empty

|                      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| -------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Files**            | `tests/unit/lib/app/defaults.test.ts` (Sunrise), `tests/unit/reserved-fork-tiers.test.ts` (Sunrise — blob identical on `sunrise/main` and `daybreak/main`), `tests/unit/lib/app/db-drift.test.ts` (Daybreak) and `tests/unit/lib/framework/privacy/export.test.ts` (Daybreak — absent from Sunrise).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| **Change**           | Pinned rows and cases for what §03 t-7 put in `leaf-bootstrap.ts`, `leaf-data-export.ts`, `leaf-db-drift.ts` and `app.prisma`. `defaults.test.ts` and `db-drift.test.ts` each gain a `vi.mock('@/lib/db/client', …)`. `defaults.test.ts`'s `lib/app/data-export.ts` bridge row now compares the registry against `framework-*.prisma` **and** `app.prisma`. `export.test.ts` gains an `appWaitlistEntry` delegate on its Prisma stub and a `LEAF_SECTIONS`/`LEAF_MODELS` pair its three bridge cases add to the framework's. `reserved-fork-tiers.test.ts`'s `app.prisma declares no models` row asserts our two declarations instead of `[]`. **Daybreak 0.3.0 added two more pins to `defaults.test.ts`**, both from moving our always-run entries into `lib/app/leaf-ci.ts` (retired row 4): the `lib/app/leaf-ci.ts` row asserts our four paths instead of `[]`, and the `lib/app/ci.ts` **bridge** row asserts Daybreak's two entries followed by our four. The bridge row is pinned as well as the seam row on purpose — it is the only one that proves the bridge still SPREADS the leaf list. Drop the spread and the seam row alone would still pass while every scoped run silently stopped loading those four tests. This is the bridge-above-the-leaf-seam case daybreak#234 describes, and the first time we have met it. **§05 t-12 re-pins two rows**: `leaf-bootstrap.ts` now asserts the seventeen journey-module slugs alongside the erasure hook, and `lib/app/eslint.config.mjs` asserts two blocks (the content boundary plus `leaf-tests-may-import-framework`, with its `files` glob pinned because widening it is the whole risk). **§05 t-28 re-pins three**: `leaf-admin-nav.ts` asserts a THIRD item (`/admin/app/voice`), `leaf-data-export.ts` asserts the two `AppVoiceComparison*` tables as exclusions beside `AppKnowledgeDesignation` — with the reason text pinned, not just the model names, because a placeholder reason passes the coverage guard and fails the data subject reading it — and `reserved-fork-tiers.test.ts` gains the same two models. `export.test.ts`'s `LEAF_EXCLUDED_MODELS` grows by the same two. **§08 t-53 re-pins four**: `leaf-admin-nav.ts` asserts a FOURTH item (`/admin/app/agent`); `leaf-data-export.ts` asserts `AppUserBudget` as the `budget` source and `AppAgentSettings` as an exclusion, with its reason pinned; `reserved-fork-tiers.test.ts` gains both models; `export.test.ts` gains the `appUserBudget` delegate, `budget` in `LEAF_SECTIONS`, `AppUserBudget` in `LEAF_MODELS` and `AppAgentSettings` in `LEAF_EXCLUDED_MODELS`. **f-safety t-63 re-pins four**: `leaf-admin-nav.ts` asserts a FIFTH item (`/admin/app/safety`); `leaf-data-export.ts` asserts `AppCrisisCopy` and `AppCrisisRegion` as exclusions; `reserved-fork-tiers.test.ts` gains both models and the `AppCrisisContentStatus` enum; `export.test.ts`'s `LEAF_EXCLUDED_MODELS` gains both. |
| **Forced by**        | **Three of the four pins are not really a divergence** — `defaults.test.ts`, `db-drift.test.ts` and `export.test.ts` each carry a FORK NOTE telling a fork to pin rather than delete, and `export.test.ts` names these exact two cases. What is genuinely forced: (a) the Prisma **stub**, because t-7 is the first task whose seam registrations reach Prisma (`leaf-bootstrap.ts` registers the waitlist erasure hook; `leaf-data-export.ts` collects waitlist rows), so importing the real seam builds a `pg.Pool` from a `DATABASE_URL` this harness does not have; and (b) `reserved-fork-tiers.test.ts`, which has **no seam to answer through**. `lib/app/reserved-tiers.ts` exists for exactly this and subtracts declared tiers — but its accepted values are the five reserved _directories_, and that row reads a _file_. Every leaf with a model of its own meets it and can only edit the platform test.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| **On conflict**      | Keep ours, and re-apply on top of upstream. Take the upstream structure and re-add the stub and the pinned assertions — never resolve by dropping a row. Two parts most likely to look like noise on a merge: the `app.prisma` half of `defaults.test.ts`'s bridge row (dropping it does not fail anything locally, it just stops the row noticing an unaccounted **framework** table), and `LEAF_SECTIONS` in `export.test.ts` (dropping it re-asserts emptiness on our behalf, which is the thing its own FORK NOTE warns against).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| **Upstream status**  | Ownership confirmed with the three-way blob check. **`reserved-fork-tiers.test.ts` is Sunrise's** (identical blob on `sunrise/main` and `daybreak/main`, so Daybreak could not fix it) and is filed as [`sunrise#764`](https://github.com/human-centric-engineering/sunrise/issues/764) (`upstream-gap`, `seam`): the file ships a seam for declaring an occupied tier, and that seam cannot express the one reserved _file_ it also checks. Cross-referenced from `sunrise#636`, the adjacent "seam-filled suite run" proposal, as a concrete instance of that class. The Prisma-stub half is a second, weaker candidate, not filed — `tests/setup.ts` already stubs `@/lib/app/brand` globally for this class of reason, so it is worth raising only if it recurs. `db-drift.test.ts` and `export.test.ts` are Daybreak's and would take a different ticket; neither is defective, both did what their FORK NOTE said.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| **Deletion trigger** | Two halves, and they close separately. The **stub** half closes when `tests/setup.ts` gains a default `@/lib/db/client` stub upstream — **re-checked on the 0.4.0 sync (t-49): what 0.4.0 added to `tests/setup.ts` is a `@/lib/app/llm-providers` pin, not a Prisma stub, so this half stays open.** The **`reserved-fork-tiers`** half closes when `sunrise#764` lands. Note our pin there may deliberately survive it: pinning keeps the row failing on a model landing in `app.prisma` that nobody decided about, which a plain subtraction would not, and #764 says so. The pinned rows stay either way — they are the documented consequence of filling a seam, not a divergence. That now includes the two `leaf-ci.ts` pins, which have no trigger of their own: they last as long as we declare an always-run test, and updating them is the cost of adding or removing one.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |

## Row 13 · `public/favicon.ico` + `public/favicon.svg` — the lotus, not the sun

|                      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| -------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Files**            | `public/favicon.ico` and `public/favicon.svg` (Sunrise — both blobs identical on `sunrise/main` and `daybreak/main`, `09c70038` and `eae4ee1b`).                                                                                                                                                                                                                                                                                                                                                         |
| **Change**           | Both replaced with the lotus bloom — `public/lotus-mark.svg` cropped to the petals and core on a square 120-unit frame, ripples dropped per §6.6, edge weight 1.9 from `LOTUS_FRAMES.tight`. The `.ico` carries 16/32/48 PNG entries. The same glyph is also added as `app/icon.svg` and, on an opaque oyster ground, `app/apple-icon.png` — those two are new files and ours; Next's file convention emits the `<link rel="icon">` / `<link rel="apple-touch-icon">` for them.                          |
| **Forced by**        | `CUSTOMIZATION.md` §"Logo & favicon" names `public/favicon.ico` as the file a fork replaces, so this is the documented path rather than an invention — but it is still a Sunrise-owned blob, and a binary conflicts on every sync in which upstream touches it. `favicon.svg` is replaced rather than deleted because nothing references it and a modify/delete conflict is noisier than a "keep mine". Without this row, the obvious resolution — take upstream — ships Sunrise's sun in the tab again. |
| **On conflict**      | Keep ours, for both files. There is nothing of upstream's to merge in: the file is the brand, and the brand is not Sunrise's.                                                                                                                                                                                                                                                                                                                                                                            |
| **Upstream status**  | Not filed. A brand asset with no seam is the expected shape of a starter template; `app/icon.svg` is the seam Next itself provides, and that half needs no upstream change.                                                                                                                                                                                                                                                                                                                              |
| **Deletion trigger** | Sunrise removes `public/favicon.*` in favour of the `app/icon.*` convention (or a `lib/app/*` icon seam). Then delete our copies in `public/` and this row; `app/icon.svg` and `app/apple-icon.png` stay.                                                                                                                                                                                                                                                                                                |

## Row 14 · `tests/setup.ts` — the email-override seam is pinned empty for the core suite

|                      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| -------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **File**             | `tests/setup.ts` (Sunrise; Daybreak already diverges it — it drops the provider-eligibility pin — so the blob differs from `sunrise/main`, but the section we add sits beside Sunrise's own brand pin)                                                                                                                                                                                                                                                                                                                                                                                              |
| **Change**           | One `vi.mock('@/lib/app/emails', () => ({ emailOverrides: {} }))` above the brand pin, so every core test file sees the seam Sunrise ships rather than ours. `defaults.test.ts`'s `lib/app/emails.ts` row reads the real seam with `vi.importActual`, exactly as its brand row does.                                                                                                                                                                                                                                                                                                                |
| **Forced by**        | §06 t-41 fills `lib/app/emails.ts` with our `welcome` and `invitation`. Two Sunrise tests then fail without having done anything wrong: `tests/unit/lib/email/registry.test.tsx` ("falls back to the platform default when no override is registered" — it does not `doMock` the seam, so it reads ours) and `tests/unit/lib/auth/config-database-hook.test.ts` (the welcome-email cases mock `@/emails/welcome` and assert it was called; ours is called instead). The "core test a fork cannot satisfy" class (`sunrise#660`); Sunrise pins the brand seam for it (#661) and the emails seam not. |
| **On conflict**      | Keep ours. Take upstream's file and re-add the block; if upstream has added its own pin for `@/lib/app/emails`, take theirs and delete this row.                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| **Upstream status**  | Ownership by the three-way check: `registry.test.tsx` and `config-database-hook.test.ts` are byte-identical on `sunrise/main` and `daybreak/main`, so Daybreak could not have fixed either. Reported as evidence on [`sunrise#636`](https://github.com/human-centric-engineering/sunrise/issues/636) (the seam-filled-suite proposal) rather than as a new issue: it is that exact class, found the way #636 says it can only be found. Not filed separately.                                                                                                                                       |
| **Deletion trigger** | Sunrise's `tests/setup.ts` pins `@/lib/app/emails` itself, or #636's sweep lands and the two tests `doMock` the seam. Check on the sync that carries it; the `importActual` in `defaults.test.ts` stays either way. **Re-checked on the 0.4.0 sync (t-49): 0.4.0 adds an `@/lib/app/llm-providers` pin beside ours and no emails pin, so the row stays.**                                                                                                                                                                                                                                           |

## Row 15 · `app/(auth)/login/page.tsx` — what the closed door says

|                      |                                                                                                                                                                                                                                                                                                                                                                                                                 |
| -------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **File**             | `app/(auth)/login/page.tsx` (Sunrise; blob identical on `sunrise/main` and `daybreak/main`)                                                                                                                                                                                                                                                                                                                     |
| **Change**           | One import and one line: `{isInviteOnly() && <InviteOnlyNotice />}` after the platform's own `{!isInviteOnly() && (…sign-up link…)}` block. The copy and the destination live in ours, `components/app/site/invite-only-notice.tsx`, so the line stays a line.                                                                                                                                                  |
| **Forced by**        | §03 t-38 sets `SIGNUP_MODE=invite_only`. The platform closes every door and redirects `/signup` here — then says nothing. A stranger who typed `/signup` landed on "Welcome back" with no form and no reason, which reads as a broken site rather than a gated one. The platform's closed state is "hide the link"; it has no seam for what to say instead, and the thing to say — join the waitlist — is ours. |
| **On conflict**      | Keep ours: take upstream's file and re-add the import and the line beneath the sign-up block. If upstream's block has moved or been rewritten, the notice goes wherever the sign-up link's absence is; it must not render when `isInviteOnly()` is false (the leaf test pins that).                                                                                                                             |
| **Upstream status**  | [`sunrise#796`](https://github.com/human-centric-engineering/sunrise/issues/796) (`upstream-gap`): a `lib/app/*` scaffold — or a component slot — the login page reads under `isInviteOnly()`. Ownership by the three-way blob check; Daybreak carries the file unchanged.                                                                                                                                      |
| **Deletion trigger** | #796 lands. Move the copy into whatever seam arrives, delete the line and this row. The leaf component may survive as the seam's value.                                                                                                                                                                                                                                                                         |

## Row 16 · `.github/workflows/ci.yml` — the `docker` job has an off-switch

|                      |                                                                                                                                                                                                                                                                                                                                                                                                 |
| -------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **File**             | `.github/workflows/ci.yml` (Sunrise; blob identical on `sunrise/main` and `daybreak/main`)                                                                                                                                                                                                                                                                                                      |
| **Change**           | One clause and one comment: `vars.CI_DOCKER != 'off' &&` as the first line of the `docker` job's `if:`, and an "Economics knob 3" paragraph beside the two existing knobs. The switch itself is the repo variable `CI_DOCKER=off`, set in GitHub, not in the tree.                                                                                                                              |
| **Forced by**        | `Docker Build + Stack Smoke` runs on every code push to `main` — a multi-stage image build, a prod-compose stack, a health wait and a smoke, up to 30 minutes and ~1.4–1.9GB of cache per PR — for an image Lelañea never builds or runs: we deploy through Vercel's Git integration. The other economics are repo variables; this job had no knob, and the `if:` is the only place to add one. |
| **On conflict**      | Keep ours: take upstream's job and re-add the clause as the first line of its `if:` (it must AND with whatever gating upstream has, never replace it) and the knob paragraph. If upstream has added a `CI_DOCKER` — or any variable that skips the job — use theirs and delete this row.                                                                                                        |
| **Upstream status**  | [`sunrise#802`](https://github.com/human-centric-engineering/sunrise/issues/802) (`upstream-gap`), which proposes exactly this clause and paragraph; filed from HCE Hub, which carries the same edit. Ownership by the three-way blob check; Daybreak carries the file unchanged.                                                                                                               |
| **Deletion trigger** | #802 merges through Daybreak. Delete the clause and the paragraph, keep the repo variable (it becomes upstream's knob), delete this row.                                                                                                                                                                                                                                                        |

## Row 17 · `VERSIONING.md` — twelve Lelañea modules named in Sunrise's Covered list

|                      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| -------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **File**             | `VERSIONING.md` (Sunrise; Daybreak already diverges it — the "reserved leaf seams" block naming its six `leaf-*.ts` files — so the blob differs from `sunrise/main`, and our block sits directly under Daybreak's)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| **Change**           | One block, "Lelañea's own scaffolds", with twelve rows: `lib/app/agent/` (added by §08 t-52), `content/`, `conversation/` (added by §10 t-64), `gateway/`, `journey/`, `modules/`, `resources/` (added by f-resources t-77), `safety/` (added by f-safety t-58), `slots/` (added by f-slots t-70), `usage/` (added by f-budget t-94), `voice/`, `waitlist/`. Each row is a path and a one-line description; none carries a `→ export`, because none is a seam anybody fills.                                                                                                                                                                                                                                 |
| **Forced by**        | Sunrise 0.12.0's always-run `tests/unit/versioning-seam-coverage.test.ts` classifies **every** entry in `lib/app/` that is not a dotfile, `.d.ts` or `.md` as a scaffold — a subdirectory as one scaffold with a trailing slash — and fails until each is named in the Covered list. Our directories are leaf-owned modules that live under `lib/app/` because that is where the ESLint app-boundary rule and the seams are scoped; they are absent from both upstream tiers and are nobody's public surface. The guard has no fork seam and no exclusion for "a fork's own module", so the only exit is a row in a Sunrise-owned document. Arrived with the 0.4.0 sync (t-49), which did not anticipate it. |
| **On conflict**      | Keep ours: take upstream's file and re-add the block under Daybreak's "reserved leaf seams" block. Add a row here whenever a new directory lands under `lib/app/` (the guard tells you, loudly). If upstream has grown a fork-declared list or an exclusion the guard reads, move the rows there and delete this row.                                                                                                                                                                                                                                                                                                                                                                                        |
| **Upstream status**  | Ownership by the three-way check: `sunrise/main` and `daybreak-v0.4.0` differ only by Daybreak's own block, and the guard is byte-identical, so it is Sunrise's. Same class as [`sunrise#799`](https://github.com/human-centric-engineering/sunrise/issues/799) (a literal roster a fork edits on every sync, no seam) — evidence added as [a comment there](https://github.com/human-centric-engineering/sunrise/issues/799#issuecomment-5715898033) rather than a new issue; [`sunrise#734`](https://github.com/human-centric-engineering/sunrise/issues/734) is the adjacent classifier question.                                                                                                         |
| **Deletion trigger** | Sunrise ships a fork seam for the coverage guard — a leaf-declared scaffold list, or an exclusion for fork-owned directories — and it merges through Daybreak. Move the rows to the seam, delete the block, delete this row.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |

## Row 18 · `app/api/v1/framework/facilitation/[role]/chat/stream/route.ts` + `lib/framework/facilitation/agents/turn-hook.ts` — a leaf hook around the facilitation turn

|                      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| -------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **File**             | The route (Daybreak; absent from `sunrise/main`, blob unchanged between `daybreak-v0.4.0` and `daybreak/main`), and a **new** file in Daybreak's tree, `lib/framework/facilitation/agents/turn-hook.ts`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| **Change**           | The route: one import, `turnId: z.string().trim().min(1).max(128).optional()` on the body schema, and its `streamChat({...})` call wrapped as (the turn object carries the request's `signal`, so a hook that claims before streaming can settle an early abort) `await runFacilitationTurn({...}, (extras) => streamChat({ ...sameArgs, ...extras }))`. The new file: a `globalThis`-backed hook registry — `registerFacilitationTurnHook`, `runFacilitationTurn` (which turns a hook's returned refusal into a 409 — returned, not thrown, because a hook registered at boot builds its error classes in another module graph), and `passThroughFacilitationTurn`, which is what runs when nothing is registered. No Lelañea vocabulary in either. Lelañea registers `runRecordedTurn` from `lib/app/leaf-bootstrap.ts`. **§08 t-55 widened the contract, two lines:** `FacilitationTurnExtras` gains `signal` — a hook's own signal **replaces** the request's on the `streamChat` call (the route already spreads `...extras` last, so this is a type change only) — and the turn object gains `keepAlive`, which the route fills with `(work) => after(work)` from `next/server` (one import, one property). Lelañea passes its own signal, fired only by its whole-turn deadline, so a client going away no longer aborts the model call; and hands `after()` the turn's run so a serverless function is not frozen with the turn half-done. At rest neither is used: no signal in the extras means the request's, and nothing calls `keepAlive`. **f-safety t-58 widened it by one more line:** the turn object gains `headers` — the request's `Headers`, which the route fills with `request.headers` — so a hook can read the request (Lelañea reads `Accept-Language` to choose the crisis resource's region) without reaching for `next/headers` from a boot-graph module that may not share the route's request scope. At rest nothing reads it. |
| **Forced by**        | §08 t-54 (owner ruling: edit Daybreak's route and ledger it, rather than a parallel leaf route or waiting for a release). A turn sent twice was two turns and two bills, a cost row knew the user but not the seat, and nothing recorded which prompt produced a turn. The route builds the `streamChat` call itself, accepts only `{ message }`, and a framework route may not import the leaf (ESLint tier rule) — so there was nowhere to put any of it. The registry lives in the framework tier for that reason, and on `globalThis` because the route and the boot seam can land in different module graphs.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| **On conflict**      | Keep ours: take upstream's route and re-apply the edits (the two imports — the hook and `after` — `turnId`, `keepAlive: (work) => after(work)` and `headers: request.headers` on the turn object, and the wrapped call — every argument upstream passes `streamChat` goes inside the closure, then `...extras` last, which is what lets a hook's `signal` win). A new Daybreak file at the same path means upstream shipped the seam: use theirs, re-point `leaf-bootstrap.ts` at it, delete this row. `tests/unit/app/api/v1/framework/facilitation/turn-seam.test.ts` must still pass — its "at rest" half is the neutrality proof.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| **Upstream status**  | [`daybreak#265`](https://github.com/human-centric-engineering/daybreak/issues/265), filed with this implementation as the reference; it also names the module surface twin (`app/api/v1/framework/modules/[slug]/chat/stream/route.ts`), which has the same gap and is not edited here. The §08 t-55 widening (`signal` in the extras, `keepAlive`) is commented on #265.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| **Deletion trigger** | #265 ships in a Daybreak release we sync. Re-point the registration in `lib/app/leaf-bootstrap.ts` at upstream's seam, delete our copy of the registry file and the route edit, delete this row.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |

## Row 19 · `tests/unit/db-raw-sql-allowlist.test.ts` — the meter's three raw-SQL reads

|                      |                                                                                                                                                                                                                                                                                                                        |
| -------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **File**             | `tests/unit/db-raw-sql-allowlist.test.ts` (Sunrise's test; Daybreak already diverges it with its own `DAYBREAK` block of three `framework_*` rows, so the blob differs from `sunrise/main` and ours sits in a `LELAÑEA` block beside it).                                                                              |
| **Change**           | One allowlist entry: `lib/app/agent/metering.ts`, 3 calls, with its `why` (§08 t-56).                                                                                                                                                                                                                                  |
| **Forced by**        | The meter's breakdowns group `ai_cost_log` by a seat read from JSON metadata with a fallback to the conversation's context, and by UTC day. Prisma `groupBy` can express neither, and summing in TypeScript would page every cost row of a month through the process. The guard is a literal roster with no fork seam. |
| **On conflict**      | Keep ours: take upstream's file and re-add the `LELAÑEA` block, sorted by path. Bump its `calls` if the module's raw-SQL count changes (the guard fails naming the exact count).                                                                                                                                       |
| **Upstream status**  | Same class as [`sunrise#799`](https://github.com/human-centric-engineering/sunrise/issues/799) — a literal roster a fork edits on every sync — which proposes `appRawSqlSites` on `lib/app/ci.ts`. Daybreak carries the same edit for the same reason ([`upstream-asks.md`](../framework/upstream-asks.md)).           |
| **Deletion trigger** | #799 ships and merges through Daybreak: move the row to `lib/app/leaf-ci.ts`, delete this edit and this row, pin the new `defaults.test.ts` row.                                                                                                                                                                       |

## Row 20 · `tests/unit/lib/orchestration/llm/cost-log-fk-attribution.test.ts` — the crisis context check's cost row

|                      |                                                                                                                                                                                                                                                                                                                                    |
| -------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **File**             | `tests/unit/lib/orchestration/llm/cost-log-fk-attribution.test.ts` (Sunrise's test; Daybreak already diverges it with its supervisor row, so the blob differs from `sunrise/main` and ours sits beside Daybreak's).                                                                                                                |
| **Change**           | Two allowlist entries: `lib/app/safety/context-check.ts` — no agent, no conversation, no execution, `userId` guarded (the pre-signup path has none) — with its `why` (f-safety t-58); and `app/api/v1/app/agent/transcribe/route.ts` — her agent's id read by slug, the session's `userId`, no conversation (f-conversation t-67). |
| **Forced by**        | The crisis context check writes a cost row so the meter counts it (`.context/app/safety.md`). The guard derives every `logCost` call site in the tree and compares it with a literal roster that has no fork seam.                                                                                                                 |
| **On conflict**      | Keep ours: take upstream's file and re-add both `LELAÑEA` entries beside Daybreak's. If a call site's arguments change, the guard prints the exact string to pin.                                                                                                                                                                  |
| **Upstream status**  | Same class as [`sunrise#799`](https://github.com/human-centric-engineering/sunrise/issues/799) — a literal roster a fork edits on every sync. Daybreak carries the same edit for the same reason (`.context/framework/upstream-asks.md`).                                                                                          |
| **Deletion trigger** | #799 (or a sibling) gives this roster a fork-declared list, and it merges through Daybreak: move the entry there, delete the edit and this row.                                                                                                                                                                                    |

## Row 21 · `app/api/v1/chat/stream/route.ts` + `app/api/v1/chat/agents/route.ts` + `lib/orchestration/chat/consumer-exclusions.ts` — she is kept off the consumer chat route

|                      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| -------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Files**            | Both routes (Sunrise; each blob is identical between `v0.12.0` and `daybreak-v0.4.0`, and `chat/agents` is also unchanged on `sunrise/main`), and a **new** file in Sunrise's tree, `lib/orchestration/chat/consumer-exclusions.ts`.                                                                                                                                                                                                                                                                                                                                                                                                                     |
| **Change**           | The new file is a `globalThis`-backed registry: `excludeFromConsumerChat(slug)`, `isExcludedFromConsumerChat(slug)` and `resetConsumerChatExclusions()`. There is no Lelañea vocabulary in it. `chat/stream`: one import, and `if (!agent)` becomes `if (!agent \|\| isExcludedFromConsumerChat(agent.slug))`. A registered slug gets the same 404, code and message as an unknown agent, after the same DB lookup. `chat/agents`: one import. The query result is renamed `listed` and filtered into `agents` with the same predicate, so the query is untouched. Lelañea registers `VOICE_AGENT_SLUG` from `lib/app/leaf-bootstrap.ts`.                |
| **Forced by**        | f-safety t-61 (owner ruling, 19 September 2026). §08 made her `public` for the facilitation surface. That also opened Sunrise's consumer route, where a turn skips the turn hook: no crisis check, no ceiling, no turn id and no record. The route asks the authorization seam about no resource, so no leaf policy can refuse her. No other seam gates that route by agent (checked per `HB5`).                                                                                                                                                                                                                                                         |
| **On conflict**      | Keep ours: take upstream's routes and re-apply the two edits (the import, the `\|\| isExcludedFromConsumerChat(agent.slug)` clause, and the `listed` filter). `sunrise/main` has already refactored `chat/stream`'s invite-token block. That refactor doesn't touch our line, so expect it to re-apply cleanly. A new Sunrise file at the same path, or any per-agent consumer-route seam, means upstream shipped it: use theirs, re-point `leaf-bootstrap.ts`, and delete our registry file, our route edits and this row. The at-rest half of `tests/unit/app/api/v1/chat/consumer-exclusions.test.ts` is the neutrality proof, so it must still pass. |
| **Upstream status**  | [`sunrise#817`](https://github.com/human-centric-engineering/sunrise/issues/817), `upstream-gap`, filed with this implementation as the reference. Filed with Sunrise rather than Daybreak because the blobs are identical (`daybreak.filing`).                                                                                                                                                                                                                                                                                                                                                                                                          |
| **Deletion trigger** | Sunrise ships an equivalent seam (#817 or its like) and it merges through Daybreak: register her through it, then delete the registry file, both route edits and this row.                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |

## Row 22 · `lib/framework/data-slots/sync.ts` + `index.ts` — global slots through a provider the leaf registers

|                      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| -------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **File**             | `lib/framework/data-slots/sync.ts` and its barrel `lib/framework/data-slots/index.ts` (Daybreak; both absent from `sunrise/main`, blobs unchanged between `daybreak-v0.4.0` and `daybreak/main`). Tests: `tests/unit/lib/framework/data-slots/sync.test.ts` (Daybreak's, appended to) and a **new** `tests/integration/lib/framework/data-slots/global-slots.test.ts`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| **Change**           | The module pass's reconcile body moves unchanged into `reconcileSlotDefinitions(tx, definitions, partition)`, and the default-resolving into `resolveSlotDefinition`. New: a `globalThis`-backed `registerGlobalSlotDefinitionProvider(async () => SlotDefinitionInput[])`, and `syncGlobalSlotDefinitions()`, which reconciles what the provider returns as `scope = global`. `syncRegisteredSlotDefinitions()` runs the module pass and then the global pass. The global pass deactivates only `global` rows, skips (with a warning) a slug another scope holds active — a slug a module retired is free to take — writes nothing when the provider returns nothing, and at boot logs a provider failure instead of throwing. Called on demand it throws. Re-syncs run one at a time in-process, each reading the provider on its turn, so two edits saved back to back cannot commit out of order; two server instances still can, and the next edit or boot repairs it. The barrel exports the two functions and their types. No Lelañea vocabulary. **At rest** (no provider) the sync issues exactly the module pass's calls: the "neutral at rest" block in `sync.test.ts` proves it, and fails if the no-provider guard is removed. |
| **Forced by**        | f-slots t-69 (owner ruling at claim, 19 September 2026: "global slots go through a seam we carry"). Most of what she learns about a person (a relationship, their health, how they like to be asked) belongs to no module. Daybreak accepts only module-declared slots stamped `module:<slug>`, so without this the taxonomy would have to be mis-scoped as Onboarding's. Daybreak's own plan names this exact seam and defers it (`.context/framework/planning/f-slots.md`, "Global (app-seeded) slots"). The provider is async because our taxonomy is admin-edited data, and it can be re-run on demand because an edit has to reach the rows without a restart.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| **On conflict**      | Keep ours: take upstream's `sync.ts` and re-apply the extraction (module pass → `reconcileSlotDefinitions` with `MODULE_PARTITION`) and the global block below it. Then re-add the four exports to the barrel. Both test files must still pass. If upstream changed the module pass's write shape, the "neutral at rest" block is the check that ours still matches it. If upstream now ships its own global registration, use theirs: re-point all three call sites — `lib/app/leaf-bootstrap.ts` (the registration), `prisma/seeds/app-lelanea/011-slot-taxonomy.ts` (the re-sync after the one-time load) and the admin edit's re-sync — then delete this row.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| **Upstream status**  | [`daybreak#266`](https://github.com/human-centric-engineering/daybreak/issues/266), filed with this implementation as the reference. Filed with Daybreak because both files are absent from `sunrise/main` (`daybreak.filing`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| **Deletion trigger** | Daybreak ships global slot registration in a release we sync. Register the taxonomy through it, delete our provider code and the global tests, and delete this row.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |

## Row 23 · `tests/unit/prisma/seeds/capability-class-seed-parity.test.ts` — the app's own capability joins the parity check

|                      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| -------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **File**             | `tests/unit/prisma/seeds/capability-class-seed-parity.test.ts` (Sunrise; byte-identical across `sunrise/main`, `daybreak/main` and `HEAD` at 21 September 2026). Companion, ours: `tests/helpers/app/capability-parity.ts`.                                                                                                                                                                                                                                                       |
| **Change**           | Two lines, each marked `DIVERGENCE`: an import of `APP_CAPABILITY_PAIRS` from our helper, and `...APP_CAPABILITY_PAIRS` spread at the end of `PAIRS`. Nothing else in the file moves.                                                                                                                                                                                                                                                                                             |
| **Forced by**        | f-resources t-77. `suggest_resource` is the app's own capability, so its `ai_capability` row is seeded by our own unit on the `012-run-workflow` pattern — and the test scans every seed directory on purpose, finds the name, and asserts it is in a `PAIRS` list that only Sunrise's built-ins can be added to. The invariant is right for a fork's capability too; the list just has no seam. Every capability this app seeds a row for goes in our helper, never in the test. |
| **On conflict**      | Keep ours: take upstream's `PAIRS` wholesale and re-add the import and the trailing spread. If upstream has grown a seam of its own for fork pairs, move our entries into it and delete this row.                                                                                                                                                                                                                                                                                 |
| **Upstream status**  | Already open as [`sunrise#646`](https://github.com/human-centric-engineering/sunrise/issues/646), whose second ask is exactly this seam; our evidence is [a comment there](https://github.com/human-centric-engineering/sunrise/issues/646#issuecomment-5767514774) rather than a second issue. Filed with Sunrise because the file is Sunrise's at every tier (`daybreak.filing`).                                                                                               |
| **Deletion trigger** | `sunrise#646` lands a fork seam for parity pairs, in a Daybreak release we sync. Move the entries in `tests/helpers/app/capability-parity.ts` into it, delete that file, and delete this row.                                                                                                                                                                                                                                                                                     |
