# Upstream asks — Daybreak → Sunrise

Fork-first seams Daybreak built ahead of (or composing with) a Sunrise seam that
hasn't landed yet. On every Sunrise pull ([`CUSTOMIZATION.md` §9](../../CUSTOMIZATION.md)),
check each **open** row: if its upstream issue has landed, **delegate to the
upstream resolver and delete the fork shim**, then close the row. This is the
"delegate when it lands" trigger that would otherwise be lost in feature-plan prose.

**Two different things live here.** [The ledger](#the-ledger) tracks seams where
Daybreak carries code it intends to delete — every row has a shim and a delegate
action. [Carried upstream defects](#carried-upstream-defects--no-fork-shim) tracks
the opposite case: a Sunrise-owned defect with **no seam a fork can reach**, so
there is nothing to shim and nothing to delete — only something to know about and
re-check. Both are checked on a sync; only the first has work attached.

## Running the check

Twice now the ledger has gone stale by asserting from memory rather than looking
(the two reconciliation notes below). The check is three commands, and it needs
the `upstream` remote and its tags — which a fresh clone does **not** have; see
[`CUSTOMIZATION.md` §9](../../CUSTOMIZATION.md).

```bash
git fetch upstream --tags

# 1. Is the issue closed?
gh issue view <n> -R human-centric-engineering/sunrise --json state,title

# 2. Is the seam actually in the tree you have? (a closed issue whose fix
#    landed AFTER your tag is NOT in your tree — the usual trap)
git show "v<tag>:path/to/core-file.ts" | grep 'theSeamSymbol'

# 3. Does the fork still carry a shim, or has it already delegated?
git diff --stat v<tag> HEAD -- path/to/core-file.ts   # empty ⇒ no fork edit
```

**A closed issue is not a landed seam, and a landed seam is not a deleted shim.**
Those are three states; record which one a row is in, not just "filed".

**This is not a boundary-breach log.** A _breach_ is editing a Sunrise-owned file
because no seam exists (the [`CLAUDE.md`](../../CLAUDE.md) banner's "keep the edit
minimal and add a follow-up" case) — those get a `keep-mine` follow-up, not a row
here. This ledger is the _sanctioned_ fork-first case: the code is clean
framework-tier, but its final home is an upstream seam. See
[`planning/planning-retro.md` B5/B7](./planning/planning-retro.md) for the
fork-first-informs-upstream working model this indexes, and the feature plans /
[`plan.md` Work-completed log](./planning/plan.md) for the narrative each row
summarises.

## The ledger

| Daybreak seam                                                                                                                                                                                                                                                                                                                                                                                                                       | Composes with                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Feature                                                                                                         | Delegate-when-it-lands action                                                                                                                                                                                                                                              | Status                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `system.daybreakVersion` on `GET /api/v1/admin/stats` + a "Daybreak framework" row on `components/admin/system-info.tsx`, and the `daybreakVersion` field on `SystemStats` in `types/admin.ts`                                                                                                                                                                                                                                      | Sunrise **#531** — the version-disclosure fix, which moved `SUNRISE_VERSION` off the unauthenticated `/api/health` onto the admin stats payload and the overview card. It assumed TWO version tiers (the fork's app, the platform); a framework-layer fork has three, and there is no seam for a tier to contribute a `system` row or a card row.                                                                                                                                                                                                                                                                                  | Sunrise v0.11.1 sync                                                                                            | Delete the three one-line edits (`types/admin.ts`, the stats route, `system-info.tsx`) and declare the row through the upstream seam instead.                                                                                                                              | **to file** — ask for a `lib/app/system-info.ts`-shaped seam: a fork tier registers `{ label, value }` rows that the stats route folds into `system` and the card renders. Daybreak already carried the same shape on `/api/health` before this sync and removed it there for #531's own reason — the framework release number names the exact set of published Daybreak issues to any anonymous caller, which is true of every Daybreak-derived deployment rather than one. The three core edits are the minimal "keep mine" CLAUDE.md sanctions; they are three merge conflicts a year until the seam exists.                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `scopeNote` folded into the declared `description` in `initFrameworkSubjectSources()` (`lib/framework/privacy/export-sources.ts`)                                                                                                                                                                                                                                                                                                   | Sunrise **#533**'s registry: `AppSubjectDataSource` carries `model` / `section` / `disposition` / `description` and no `scopeNote`, though core's own `SubjectDataSource` has one and `meta.exported` discloses it. So a fork-tier source that returns only SOME of the subject's rows has nowhere to say so, and `meta.app` has nowhere to show it.                                                                                                                                                                                                                                                                               | Sunrise v0.11.1 sync                                                                                            | Drop the fold, declare `scopeNote` as its own field, and let `meta.app` carry it the way `meta.exported` already does.                                                                                                                                                     | **to file** — no framework source narrows today, so this is a guard against the next one rather than live behaviour, and the fold keeps the note in front of the subject meanwhile. Worth filing because the failure it prevents is silent: a partial answer with no note reads exactly like a complete one, which is the same defect as a missing section at row granularity — and it is the defect core's own manifest added `scopeNote` to prevent.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| [`lib/framework/shared/access.ts`](../../lib/framework/shared/access.ts) — `canRead` / `subjectScope`                                                                                                                                                                                                                                                                                                                               | Sunrise [#367](https://github.com/human-centric-engineering/sunrise/issues/367) (intra-tenant ownership scope) · [#366](https://github.com/human-centric-engineering/sunrise/issues/366) (org-admin / tier axis, carried via `scope.tier`)                                                                                                                                                                                                                                                                                                                                                                                         | [f-journey-state (09)](./planning/f-journey-state.md)                                                           | Replace the self-read + explicit admin-support body with a call to the upstream ownership resolver, passing `scope`; drop the fork-local `JourneyViewer` / `AccessScope` types if the resolver supplies them; keep the `canRead` ⇔ `subjectScope` parity test.             | **filed** — fork-note on [#367](https://github.com/human-centric-engineering/sunrise/issues/367#issuecomment-4885272729) (+ tier cross-ref on [#366](https://github.com/human-centric-engineering/sunrise/issues/366#issuecomment-4885273235)); awaiting the upstream resolver                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| the two framework **surface** stream routes ([`modules/[slug]/chat/stream`](../../app/api/v1/framework/modules/[slug]/chat/stream/route.ts) · [`facilitation/[role]/chat/stream`](../../app/api/v1/framework/facilitation/[role]/chat/stream/route.ts)) — each re-does the consumer streaming route's plumbing (auth, the two rate limiters, SSE framing, `streamChat` wiring) around a **server-resolved** agent and context tuple | Sunrise — **to file**: a consumer chat entry point that accepts a caller-supplied, **server-resolved** `(agentId, contextType, contextId)` instead of a client-supplied `agentSlug`. #415 gave the consumer route `scope`, which was never the blocker; the blocker is that the consumer route deliberately refuses the context tuple (admin-only concept), so a surface cannot tag — and therefore cannot resume — its own conversation through it.                                                                                                                                                                               | [f-guidance (12)](./planning/f-guidance.md) · [f-facilitation-agents (13)](./planning/f-facilitation-agents.md) | Collapse both routes onto the new entry point, keeping only the surface resolution (`resolveModuleSurface` / `resolveFacilitationSurface`) and the `module.entered` emit.                                                                                                  | **to file** — replaces the closed #415 row (v1.3 Phase 1 t-1.3). This is the honest ask; #415's "collapse to a thin scope-populating wrapper" was not.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| per-agent binding freshness — the slot exposure allowlist ([`data-slots/capabilities/exposure.ts`](../../lib/framework/data-slots/capabilities/exposure.ts)) now reads `CapabilityContext.customConfig`, so it inherits the dispatcher's per-process binding cache                                                                                                                                                                  | Sunrise — **to file**: `capabilityDispatcher.clearCache()` (`lib/orchestration/capabilities/dispatcher.ts`) is **process-local**, and the per-agent binding cache has a 5-minute TTL with no cross-instance invalidation — no pub/sub, no `updatedAt` version stamp. On a multi-instance deployment an admin binding write is only applied by the instance that served it; the others serve the stale binding until the TTL expires. Affects `isEnabled` and `customRateLimit` identically, and has since those were cached — surfaced here because delegating #411 moved a **fine-grained access allowlist** onto the same cache. | [f-slot-capture (10)](./planning/f-slot-capture.md) · v1.3 Phase 1 t-1.1                                        | Drop the multi-instance caveat from the CHANGELOG entry and this module's header once binding writes invalidate across instances.                                                                                                                                          | **to file** — found by `/security-review` on the t-1.1 PR (#204), confidence 8/10. Accepted deliberately: the coarse controls already had this window, so the fork would be papering over a core property rather than fixing it. Recorded in the CHANGELOG so a leaf operator can plan around it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| module capability PII contract — the framework can no longer accept a `processesPii` capability whose `redactProvenance()` is inherited from an intermediate base class or declared as a class-property arrow ([`modules/capabilities/register.ts`](../../lib/framework/modules/capabilities/register.ts) delegates the check to core)                                                                                              | Sunrise — **to file**: `isRedactorOverridden` (`lib/orchestration/capabilities/dispatcher.ts:771`) is an own-property check on the instance's **direct** prototype, so it reads "inherits a redactor" and "declares one as an own instance property" as "does not redact". Walking the prototype chain up to (but not including) `BaseCapability.prototype`, plus an own-property check on the instance, would accept both while still catching the real case. Affects core capabilities equally — a shared abstract base with one redactor is ordinary TypeScript.                                                                | [f-module-bindings (07)](./planning/f-module-bindings.md) · v1.3 Phase 1 t-1.2                                  | Drop the authoring constraint from the CHANGELOG and the two `rejects a redactor …` tests in `tests/integration/lib/framework/modules/capabilities/register.test.ts` (which exist to make the relaxation visible).                                                         | **to file** — surfaced by delegating to #398: the fork's own re-assertion compared by identity against the base method and was therefore laxer. Not a regression in core, but the fork stopped hiding it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| module capability handler staleness — a capability refused on THIS boot can keep a handler registered by a PREVIOUS one, in the same process ([`modules/capabilities/register.ts`](../../lib/framework/modules/capabilities/register.ts))                                                                                                                                                                                           | Sunrise — **to file**: `CapabilityDispatcher` exposes `register()` but no `unregister()`, and its handler map is `globalThis`-backed so it survives a re-boot in one process (dev HMR, a second `initApp()`). `register()` also throws its PII error _before_ `handlers.set()`, so a capability that starts violating the contract keeps the previous boot's handler and goes on dispatching without the redaction the check exists to force. An `unregister(slug)` — or having a failed `register()` evict any handler under that key — would close it.                                                                           | [f-module-bindings (07)](./planning/f-module-bindings.md) · v1.3 Phase 1 t-1.2                                  | Call `unregister()` for each refused capability in `registerRegisteredModuleCapabilities()`'s catch block.                                                                                                                                                                 | **to file** — found by `/code-review` on #205. **Production is unaffected**: each deploy boots a fresh process, so there is no previous handler to inherit; this bites dev HMR. Mitigated meanwhile — the row reconcile keys on the registration report rather than `dispatcher.has()`, so a refused capability's `ai_capability` row deactivates even while a stale handler lingers, and it stops being admin-grantable.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `initFramework()` → `registerProactiveGuidanceStep()` / `registerEvalSweepStep()` ([`lib/framework/index.ts:91`](../../lib/framework/index.ts), `:95`), landing in core's [`executor-registry.ts:31`](../../lib/orchestration/engine/executor-registry.ts) `Map`                                                                                                                                                                    | Sunrise **#462 follow-on** — the boot-vs-request module-graph split under Next 16 + Turbopack. #462 backed core's chat context contributors and the capability dispatcher with `globalThis` but stopped there; these registries were not in the sweep and are still module-scoped. Daybreak fixed the same class in its OWN registries in [Phase 0 t-0.1](./planning/issue-backlog.md), so the fork-side evidence is in hand.                                                                                                                                                                                                      | [issue-backlog](./planning/issue-backlog.md) Phase 0 (found)                                                    | On the Sunrise release that backs `executor-registry` with `globalThis`: nothing to delete — the registrations are already correct. Re-test that an operator-scheduled workflow using `framework_proactive_guidance` / `framework_eval_sweep` executes, and close the row. | **filed pending** — core's own executors self-register at module import via the barrel, so they survive the split; these two register only from `initFramework()` and do not. **Symptom:** a workflow using either step type throws _unknown step type_ at execution. Core-owned file, no seam to extend — the fix belongs upstream.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `initFramework()` → `registerAgentAccessContributor()` ([`lib/framework/index.ts:70`](../../lib/framework/index.ts)), landing in core's [`resolveAgentDocumentAccess.ts:59`](../../lib/orchestration/knowledge/resolveAgentDocumentAccess.ts) `accessContributors` `Map`                                                                                                                                                            | Sunrise **#462 follow-on** — the boot-vs-request module-graph split under Next 16 + Turbopack. #462 backed core's chat context contributors and the capability dispatcher with `globalThis` but stopped there; these registries were not in the sweep and are still module-scoped. Daybreak fixed the same class in its OWN registries in [Phase 0 t-0.1](./planning/issue-backlog.md), so the fork-side evidence is in hand.                                                                                                                                                                                                      | [f-module-bindings (07)](./planning/f-module-bindings.md)                                                       | Same: nothing to delete, re-test that an agent's module-scoped knowledge resolves on a cold server, then close.                                                                                                                                                            | **filed pending** — note this is the seam whose ask (#403) legitimately **landed** (see the Landed table): the seam is upstream and the fork delegates correctly. What does not work is the _realm_, so module knowledge scope silently resolves to nothing at request time.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `initFramework()` → `registerGuardFloorContributor()` ([`lib/framework/index.ts:74`](../../lib/framework/index.ts)), landing in core's [`guard-floor.ts:75`](../../lib/orchestration/chat/guard-floor.ts) `contributors` `Map`                                                                                                                                                                                                      | Sunrise **#462 follow-on** — the boot-vs-request module-graph split under Next 16 + Turbopack. #462 backed core's chat context contributors and the capability dispatcher with `globalThis` but stopped there; these registries were not in the sweep and are still module-scoped. Daybreak fixed the same class in its OWN registries in [Phase 0 t-0.1](./planning/issue-backlog.md), so the fork-side evidence is in hand.                                                                                                                                                                                                      | [f-policies (13)](./planning/f-policies.md)                                                                     | Same: nothing to delete, re-test that facilitation guard minimums apply on a cold server, then close.                                                                                                                                                                      | **filed pending** — **Symptom:** facilitation guard minimums silently do not apply to a request-path chat turn. Fails open, which is the worse direction for a guard floor.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `initFramework()` → `registerGuardEventContributor()` ([`lib/framework/index.ts:78`](../../lib/framework/index.ts)), landing in core's [`guard-events.ts:64`](../../lib/orchestration/chat/guard-events.ts) `contributors` `Map`                                                                                                                                                                                                    | Sunrise **#462 follow-on** — the boot-vs-request module-graph split under Next 16 + Turbopack. #462 backed core's chat context contributors and the capability dispatcher with `globalThis` but stopped there; these registries were not in the sweep and are still module-scoped. Daybreak fixed the same class in its OWN registries in [Phase 0 t-0.1](./planning/issue-backlog.md), so the fork-side evidence is in hand.                                                                                                                                                                                                      | [f-overlays (17)](./planning/f-overlays.md)                                                                     | Same: nothing to delete, re-test that a guard event triggers facilitation escalation, then close.                                                                                                                                                                          | **filed pending** — **Symptom:** `handleFacilitationGuardEvent` never runs, so escalation does not fire on a request-path guard event.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| One line in Sunrise's `vitest.config.ts` `coverage.exclude`: `'scripts/boundary/check.ts'`                                                                                                                                                                                                                                                                                                                                          | Sunrise **#759** — no fork-owned seam for coverage exclusions. The per-file floor (#647) gates every _changed_ file at 80%, and a `tsx` CLI entry point (`main()` at module scope, nothing importing it) is structurally 0% — invisible in a full run, surfacing only the first time anyone edits it. Upstream already hand-names three instances of this shape (`scripts/smoke/`, `scripts/db/`, `lib/app/eslint.config.mjs`), two of them found the hard way (#671, #687).                                                                                                                                                       | #157 (framework import ban exemptions)                                                                          | Delete the one line and declare it through the seam (`lib/app/coverage.ts` or equivalent).                                                                                                                                                                                 | **filed — awaiting the seam.** A **`keep-mine` breach**, not a fork-first seam: `vitest.config.ts` had **zero** Daybreak edits before this, so it is a new merge conflict on every sync until #759 lands. The cost is **three** platform files, not one — all the same shape, a core CI registry with no fork tail: `vitest.config.ts` (coverage exclusion), `scripts/ci/missing-tests.ts` (the drift guard that fails until the exclusion is declared), and `scripts/ci/scoped-tests.ts` (`ALWAYS_RUN_TESTS`, which #158's boot-seed ordering test must join because nothing imports a seed file). All three had zero Daybreak edits before this. No alternative existed — `scripts/boundary/check.ts` is filesystem + ESLint I/O whose real proof is `npm run framework:boundary` in CI, and mocking it into a unit test to clear a gate is the green-bar anti-pattern `.context/testing/` exists to prevent. Its pure half (`lib.ts`) stays gated, and `isCoreSource` moved there when this landed — gaining 20 test cases, having had none. |

## Landed — delegated, no longer carried

Rows closed on a Sunrise pull. Kept (not deleted) so the record of what the fork
carried, and when it stopped, survives — a reviewer asking "why does the
framework register into a core seam that has no fork shim?" gets the answer here.

| Upstream ask                                                                                                                                                                                                                                                                                                  | Landed in      | What the fork carries now                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Sunrise [#533](https://github.com/human-centric-engineering/sunrise/issues/533) — a contributor seam for subject access, so a tier between core and the leaf can declare its own Art. 15 sources without editing the core manifest or consuming the ONE `lib/app/data-export.ts` seam the leaf is entitled to | Sunrise 0.10.0 | **Nothing in core.** `tests/unit/lib/privacy/export-sources.test.ts` is byte-identical to upstream again — the `FRAMEWORK_DECLARED` filter the fork carried in it is deleted, and core's guard now learns the framework tier from `getAccountedAppModels()`. Delegated on the v0.11.1 sync via `registerAppSubjectSources({ tier: 'framework' })`, called from `initFrameworkSubjectSources()`. **Three things did NOT land the way the row predicted, and are worth knowing.** (1) The bridge STAYS: upstream requires a framework tier to register from the leaf's lazy `initAppSubjectSources()`, not from `initFramework()` at boot — the registry re-runs only the lazy seam, so a boot-time contribution is lost the first time anything resets it. So `lib/app/data-export.ts` is still filled and `lib/app/leaf-data-export.ts` is still the leaf's seam, now with an `initLeafSubjectSources()` beside its collector. (2) The bundle shape CHANGED: sections are flat under `app`, not nested under `app.framework`, and the hand-rolled `meta` block in `lib/framework/privacy/export.ts` is gone because core emits `meta.app` and folds our exclusions into `meta.excluded`. (3) Full accounting replaced the userId heuristic, which forced a real gap into the open — `FrameworkConversationEval` is reached by JOIN and had been silently absent from every export. See the CHANGELOG for the breaking-change note a leaf fork needs. |
| Sunrise [#412](https://github.com/human-centric-engineering/sunrise/issues/412) — per-user context contributors (`ContextRequest { userId }` + user-partitioned cache)                                                                                                                                        | Sunrise 0.7.0  | Nothing in core. `lib/orchestration/chat/context-builder.ts` is byte-identical to upstream; the framework keeps only `loadModuleContext` and its `initFramework()` registration.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| Sunrise [#413](https://github.com/human-centric-engineering/sunrise/issues/413) — the guard-floor seam (`registerGuardFloorContributor`)                                                                                                                                                                      | Sunrise 0.7.0  | Nothing in core. `lib/orchestration/chat/guard-floor.ts` is upstream's; the framework registers `resolveFacilitationGuardFloor` into it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| Sunrise [#414](https://github.com/human-centric-engineering/sunrise/issues/414) — the guard-event seam (`registerGuardEventContributor`)                                                                                                                                                                      | Sunrise 0.7.0  | Nothing in core. `lib/orchestration/chat/guard-events.ts` is upstream's; the framework registers `handleFacilitationGuardEvent` into it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| Sunrise [#403](https://github.com/human-centric-engineering/sunrise/issues/403) — the generic agent-access-contributor seam (`registerAgentAccessContributor`)                                                                                                                                                | Sunrise 0.7.0  | Nothing in core. `lib/orchestration/knowledge/resolveAgentDocumentAccess.ts` is byte-identical to `v0.8.1` and exports the seam itself (`:75`); the fork-carried `lib/orchestration/knowledge/agent-access-contributors.ts` no longer exists. The framework keeps only `resolveModuleKnowledgeForAgent` and its `initFramework()` registration (`lib/framework/index.ts:70`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| Sunrise [#410](https://github.com/human-centric-engineering/sunrise/issues/410) — relocate `runStructuredCompletion` out of `evaluations/` and widen `phase` to an open string                                                                                                                                | Sunrise 0.7.0  | Nothing in core. `lib/orchestration/llm/structured-completion.ts` is upstream's, and [`data-slots/capabilities/extract.ts`](../../lib/framework/data-slots/capabilities/extract.ts) both imports the relocated module and passes `phase: 'slot-extraction'`, so a slot capture's LLM span is no longer tagged `evaluation` (span attributes only — the runner persists nothing) (v1.3 Phase 1 t-1.5). `tryParseJson` stays imported from `evaluations/parse-structured` — that is its upstream home, not a shim.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| Sunrise [#411](https://github.com/human-centric-engineering/sunrise/issues/411) — surface the resolved binding's `customConfig` (+ `isEnabled`) onto `CapabilityContext`                                                                                                                                      | Sunrise 0.7.0  | Nothing in core. `lib/orchestration/capabilities/types.ts` is upstream's and the dispatcher populates both fields. [`data-slots/capabilities/exposure.ts`](../../lib/framework/data-slots/capabilities/exposure.ts) no longer queries `AiAgentCapability`: `loadExposureConfig` became the pure `resolveExposureConfig(context, slug)`, deleting one indexed lookup from every slot capture and every `get_state` (v1.3 Phase 1 t-1.1). Two deliberate consequences: the allowlist now shares the dispatcher's 5-minute binding cache, and a context with **no** resolved binding (`customConfig: undefined` — an execution that bypassed the dispatcher) fails closed rather than degrading to permissive.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| Sunrise [#398](https://github.com/human-centric-engineering/sunrise/issues/398) — `register(cap, { slug?, guard? })`: a handler-slug override + a generic pre-execute guard                                                                                                                                   | Sunrise 0.7.0  | Nothing in core. `lib/orchestration/capabilities/dispatcher.ts` is upstream's. The `NamespacedModuleCapability` wrapper, its dummy `schema` and its `redactProvenance` re-assertion are deleted; [`modules/capabilities/namespace.ts`](../../lib/framework/modules/capabilities/namespace.ts) is now pure derivations (`moduleCapabilityIdentity` + `moduleScopeGuard`) that `register.ts` hands to the seam and `sync.ts` reuses for the `ai_capability` row (v1.3 Phase 1 t-1.2). The author's real instance is registered, so core's own PII guard sees the true prototype — which is why the fork's re-assertion could go. An out-of-module call now returns `capability_guard_denied`, refused before the rate limiter.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| Sunrise [#416](https://github.com/human-centric-engineering/sunrise/issues/416) — a core resume-by-`(contextType, contextId)` conversation lookup                                                                                                                                                             | Sunrise 0.7.0  | Nothing in core. `lib/orchestration/chat/resume-conversation.ts` is upstream's, and both framework surfaces — [`guidance/surface.ts`](../../lib/framework/guidance/surface.ts) and [`facilitation/agents/surface.ts`](../../lib/framework/facilitation/agents/surface.ts) — call `findResumableConversation` instead of hand-rolling `aiConversation.findFirst` (v1.3 Phase 1 t-1.4). The `userId` scoping that keeps one user's surface conversation out of another's is now derived in one place.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| Sunrise [#415](https://github.com/human-centric-engineering/sunrise/issues/415) — `scope` on `consumerChatRequestSchema` / the consumer chat route                                                                                                                                                            | Sunrise 0.7.0  | **Landed — no carry.** `consumerChatRequestSchema` carries `scope` (`lib/validations/orchestration.ts:4057`) and a _leaf_ can now use it; Daybreak simply is not the consumer. The row assumed the two framework stream routes were scope-threading shims. They are not (v1.3 Phase 1 t-1.3 rechecked): they resolve the agent **server-side** from the module/role, tag `contextType`/`contextId` — which the core consumer route deliberately refuses as admin-only — and the module route emits `module.entered`. A thin wrapper would drop the context tuple, so new surface conversations would go untagged and **resume would break**. The facilitation route threads no `scope` at all. And here `scope` is server-derived (`encodeScope({ moduleSlug })`); delegating would move it onto a client-supplied field. The honest remaining gap — a consumer entry point that accepts a **server-resolved** context tuple — is filed as its own row above.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| Sunrise [#539](https://github.com/human-centric-engineering/sunrise/issues/539) — a CI guard that detects a squash-merged sync PR before it silently resets the fork's merge base                                                                                                                             | Sunrise 0.9.0  | Nothing. The fork-carried `framework:sync-ancestry` npm guard (`scripts/release/sync-ancestry.ts` + `sync-ancestry-check.ts` + its unit tests) and the `app:ci-checks` entry that invoked it are **deleted**; the tree relies on Sunrise's [`Fork Sync Integrity`](../../.github/workflows/fork-sync-integrity.yml) workflow and `scripts/ci/check-sunrise-ancestry.sh`. The shim ran on every CI job, the workflow runs on push to `main` — a deliberate loss of PR-time detection, taken because the hazard is only detectable _after_ the squash lands anyway, and because the shim hardcoded Sunrise's clone URL while the workflow takes `SUNRISE_UPSTREAM_URL`. That difference is what decided it: Daybreak now has a leaf fork (Reclaim Your Week) whose upstream is **Daybreak, not Sunrise**, so a hardcoded-URL guard is the wrong shape one tier down. One mechanism now runs at every tier.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| Sunrise [#382](https://github.com/human-centric-engineering/sunrise/issues/382) — a fork-owned CI-checks seam (`npm run app:ci-checks --if-present`) so a fork adds build gates without editing `ci.yml`                                                                                                      | Sunrise 0.8.1  | Nothing. Daybreak had added its **own** invocation of the seam to the `lint` job; upstream's shipped in the same job with the identical condition and command, so `app:ci-checks` ran **twice per lint run** from the v0.8.1 sync until it was spotted on the v0.9.0 sync. Daybreak's copy is deleted and the tree now uses upstream's step alone. Nothing was lost — the duplicate was pure repetition, not extra coverage. Worth noting how it hid: a fork-first shim that upstream lands _verbatim_ leaves no conflict and no failure, just a silently doubled step, so neither the merge nor CI ever complained.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |

> Reconciled during the **Sunrise 0.8.0** sync. All three had in fact landed in
> 0.7.0 and been delegated on that merge, but the rows were never closed — so the
> ledger claimed the fork was carrying three core seams it had already given up.
> Verified before closing: each seam exists upstream with the expected exports,
> the corresponding core file is byte-identical to `v0.8.0`, and the framework
> consumers still register into it (`lib/framework/index.ts`). **If a row here is
> ever reopened, check that pair — "the file matches upstream" alone can also mean
> a fork edit was silently dropped by a merge, which looks identical in a diff.**

> Reconciled again during **Framework v1.3 Phase 0** (t-0.2), which found the same
> failure one release later and worse: **six rows still read "filed — awaiting the
> seam" for seams that landed in Sunrise 0.7.0** — #398, #403, #410, #411, #415,
> #416. The 0.8.0 pass above covered #412/#413/#414 and stopped there. #403 moves
> here (the fork delegates already); the other five stay open with corrected
> statuses, because the seam landing and the shim being deleted are two different
> events and only the first has happened. **Verified for each**: the seam exists at
> the named symbol in `v0.7.0`, `v0.8.0` and `v0.8.1`; the core file is
> byte-identical between `v0.8.1` and this tree; and the fork shim is still present
> at the line the row names. Two structural defects were fixed in the same pass —
> the #416 and #533 rows shared one physical line, so **#533 rendered nowhere at
> all** (a five-column table drops the overflow cells), and #403's row was lifted
> whole rather than retyped.

> Closed during **Framework v1.3 Phase 1** (t-1.1 + t-1.5): the two slot-capability
> rows moved here by the third state actually happening — the shim deleted and the
> caller delegating. Both seams had been landed-but-carried since Sunrise 0.7.0.

> Closed during **Framework v1.3 Phase 1 t-1.2**: #398 moved here by the same third
> state — the wrapper deleted and `register.ts` calling the seam. It too had been
> landed-but-carried since Sunrise 0.7.0. Two rows remain open against 0.7.0 seams
> (#415 · #416); they are the rest of Phase 1.

> Closed during **Framework v1.3 Phase 1 t-1.3 + t-1.4**: #416 by delegating both
> surface resumes to the core lookup, and #415 as **landed — no carry**. The two are
> worth reading together: the row that looked like a deletion (#416) was one, and the
> row that looked like a bigger deletion (#415) turned out to rest on a premise that
> did not survive reading the routes. A seam landing upstream is not proof the fork's
> code was a shim for it.

> Closed during the **Sunrise 0.9.0** sync: #539 is the first row here whose shim
> was deleted _on the same merge that landed the seam_ — the ledger's three states
> (filed / landed / delegated) collapsed into one pass because the guard's whole
> subject is that merge. It is also the first row decided by the **leaf tier**
> rather than by core: two guards for one hazard was arguably defensible in
> Daybreak alone, and stopped being so once a fork of Daybreak had to inherit
> them both. Note this row had **no entry at all** before now — the shim shipped
> in v1.3 Phase 0 (t-0.3) and was recorded only in `plan.md` prose, which is
> exactly the gap this ledger exists to close.

## Carried upstream defects — no fork shim

Sunrise-owned defects Daybreak **runs as-is** because no seam exists to work
around them. There is no shim to delete, so these are not ledger rows: the
delegate-when-it-lands action is simply "confirm the fix is in the tag, then close
the row".

They are recorded because the alternative is worse than it looks. A defect with no
fork-side code leaves no trace in this repo at all — no shim, no comment, no
failing test — so the only thing standing between it and being forgotten is
whoever happened to read the upstream issue. **Where a defect is gated behind a
config flag, this table is also the warning attached to setting that flag**, which
is the case the row below exists for.

| Upstream defect                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Filed                                                                                                                  | Daybreak's exposure                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Re-check trigger                                                                                                                                                                                                                                                                                 |
| ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Sunrise [#628](https://github.com/human-centric-engineering/sunrise/issues/628) — `executeHttpRequest` (`lib/orchestration/http/fetch.ts:89`, `:177`) validates its host allowlist once against the initial URL, then `fetch()`es with no `redirect` option, so undici follows `Location` unvalidated. The model chooses the initial URL and the response body is returned to it, making it a read primitive. Sunrise #534 hardened the four outbound-fetch sites guarding with `checkSafeProviderUrl`; this fifth one uses the `ORCHESTRATION_ALLOWED_HOSTS` allowlist instead and was outside that sweep. | 2026-08-18, on the **v0.9.0** sync. Pre-existing — `fetch.ts` is blob `1be1e9e6` at v0.8.1, v0.9.0 and current `main`. | **None today, and that is a config fact rather than a code one.** `ORCHESTRATION_ALLOWED_HOSTS` is unset in every env file and an empty set denies every call, so the path is inert. **Setting that variable to enable `call_external_api` or the workflow `external_call` step activates the gap** — read #628 first, and note `allowedUrlPrefixes` is applied only to the model-supplied URL, so it does not survive a redirect either. Credential exposure on a hop is narrow but real: `Authorization` is stripped cross-origin by the fetch spec, but `api-key` with a custom `apiKeyHeaderName` is not, and `query-param` auth puts the secret in the URL. | Each Sunrise pull: `gh issue view 628 -R human-centric-engineering/sunrise --json state`. **Closed is not enough** — confirm the fix is in the tag being merged (`git show "v<tag>:lib/orchestration/http/fetch.ts" \| grep redirect`), the same trap the "Running the check" section describes. |

## Adding a row

**Which section?** If Daybreak carries code it plans to delete, it is a ledger row
(below). If Daybreak carries nothing and is only waiting on an upstream fix, it is
a [carried upstream defect](#carried-upstream-defects--no-fork-shim) — file the
Sunrise issue, add the row with its exposure and re-check trigger, and stop there.

When a feature builds a seam whose final home is an upstream Sunrise seam:

1. Add a row here (seam file, upstream issue(s), owning feature, the concrete
   delegate action, status).
2. File / update the fork-perspective note on the existing Sunrise issue as the
   feature's own Done-when deliverable (retro B7) — a **new** issue only if none
   already tracks the seam.
3. Close the row on the Sunrise pull that lands the seam, once the shim is deleted
   and callers delegate.
