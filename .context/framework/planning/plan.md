---
name: Daybreak
category: expert-led-apps framework
status: in flight
host_platform: sunrise (separate fork)
sunrise_baseline: Sunrise v0.5.0 (f-seams landed via #373)
opened: 2026-06-23
restructured: 2026-06-24
renamed: 2026-06-30 (→ Daybreak)
spec: framework-architecture.md (rev 16)
epic: Framework v1
---

# Daybreak — development plan

> The working plan for building **Daybreak** — the expert-led-apps framework layer (Modules + Facilitation Structures + Data-Slots) — on a **separate fork of Sunrise**. The authoritative design is [[framework-architecture|framework-architecture.md]] (rev 16); this is the _build breakdown_. Structured to match the [[v1-requirements|HCE Hub]] working model — until the Hub exists, this markdown is the system of record.

## How to read this — the working model

This plan uses the Hub's levels, with each level meaning exactly what it means in the Hub:

- **Task = one PR.** The unit of work. Not a commit (commits live below this plan's resolution). A task is a cohesive, reviewable change that merges in one sitting.
- **Feature = the unit of ownership.** One owner, a coherent capability, ~2–5 tasks, with explicit `depends on` edges. **This is the working atom** — the thing you claim, prioritise, and advance. Features are a _flat list_; their order emerges from dependencies, not from any grouping.
- **Phase = an epic.** Coarse and organisational, _not_ gating and _not_ a dependency unit ([[v1-requirements#10. Initial data model sketch|per the Hub spec]]). **This entire build is one phase: `Framework v1`.** Later efforts (the relationship/cohort overlay; adjacent commercial/identity/comms components) are separate **parked** phases.

**On the spec's "six phases".** [[framework-architecture#10. Suggested build sequence|§10 of the spec]] sequences the build in six steps. That is a _suggested build order_, not a Phase structure — it re-expresses itself here as the **dependency graph between features**. Don't let it impose six buckets; there are no buckets, just features and their dependencies (the spec sequence is why `f-engine` depends on `f-map`, etc.).

- **Intent over prescription.** Each feature captures _what_ and _why_. The binding _how_ lives in [[framework-architecture]] (Appendix A). Implementation choices are made at the moment of work by the owner + Claude.
- **Stable identifiers.** Features use semantic slugs (`f-engine`, `f-slots`); tasks are `t-N` under their feature (matching the Hub). Reference a feature by slug: _"let's plan f-engine."_
- **Decisions and work-to-date are first-class** — see the logs at the end. Append, don't rewrite. The plan is allowed to be wrong; edit as insight arrives.

## Project

| Field                   | Value                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ----------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Name                    | **Daybreak** (the expert-led-apps framework)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| Active epic             | **Framework v1.3 · leaf-consumer hardening** — v1 (01–20), v1.1 (21–23) and v1.2 (24) are closed; the open work is the six phases of [[issue-backlog]] (25–30)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| Spec                    | [[framework-architecture]] rev 16 (Binding decisions in Appendix A)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| Repo                    | `human-centric-engineering/daybreak` — fork of `human-centric-engineering/sunrise` (tracking `upstream`, at **Sunrise v0.8.1** — synced 2026-08-14, #196; merge-base repaired #201)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| Placement               | Separate fork of Sunrise, **not** core ([[placement-decision-memo                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | decision 2026-06-23]]) |
| Relationship to Sunrise | [[building-on-sunrise]], one level up: fix-in-place → classify → promote generic upstream; Hub-coordinated                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| First app               | **`reclaim-your-week`** (John) — the first real leaf fork, live and sharing Daybreak history; Lelanea (transcendental coaching) follows                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| Lead                    | Simon Holmes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| Status                  | **`Framework v1 + v1.1 COMPLETE`** — all 20 v1 features **shipped**, and all three v1.1 follow-on features (21 `f-engagement-analytics` · 22 `f-admin-surfaces` · 23 `f-governance-plus`) **shipped**. `f-engagement-analytics` (21, John) shipped last (#140 · #141 · #142 · #143). The v1 list: `f-seams` · `f-bootstrap` · `f-module-core` · `f-map` · `f-slots` · `f-journey-state` · `f-engine` · `f-slot-capture` · `f-module-bindings` · `f-module-config` · `f-guidance` · `f-facilitation-agents` · `f-policies` · `f-emergence` · `f-eval` · `f-ops-views` · `f-overlays` · `f-engagement` · `f-atlas` · `f-map-editor`. **The v1 + v1.1 board is closed, and so is v1.2.** **24 `f-release`** shipped (#187) and Daybreak cut its **first tagged release, `daybreak-v0.1.0`** — the framework is now versioned and consumable, so a leaf fork merges a named release rather than whatever `main` happens to be. **Framework v1.3 · leaf-consumer hardening** is now open (25–30, detailed plan [[issue-backlog]]) — driven by the first leaf's ten issues and the finding that Daybreak carries five shims for Sunrise seams that landed in 0.7.0. **25 · Phase 0** shipped (#201) — the leaf-blocking registry-realm bug is fixed, the ledger tells the truth, and the fork's sync ancestry is repaired and CI-guarded. **26 · Phase 1** is in flight (John). The parked phases remain (the relationship/cohort overlay; adjacent commercial/identity/comms components). |

---

## Concept and intent

The framework adds a layer _above_ Sunrise's orchestration platform that turns agents, capabilities, knowledge, and workflows into a coherent, guided, personalised experience. It is **domain-agnostic**: every app is configuration and content, never framework code. Three new platform domains compose the two that already exist (Knowledge Base, Agents):

- **Modules** — registered, bounded feature units with universal controls + per-module Zod-schema parameters; agents/workflows/knowledge attach by _binding_, never ownership.
- **Facilitation Structures** — a versioned typed-graph **map**, per-user **journey state** (insert-only event log), a deterministic **engine** (sole writer of state), an advisory **guidance** layer, and **governance**.
- **Data-Slots** — what the system learns about the user: insert-only versioned values with confidence, source type, and per-version provenance.

Two organising decisions (spec §3): **definition vs state everywhere**; and **code-first modules**. See [[framework-architecture]] for the full design and Appendix A (every binding decision).

## Relationship to Sunrise

The framework lives in its **own fork/repo of Sunrise** ([[placement-decision-memo|2026-06-23]]). Two generic seams land _in Sunrise core_ first (`f-seams`), then the fork is taken. The framework touches Sunrise only through registration seams, enforced by an ESLint + CI boundary from the start ([[framework-architecture#Appendix B]], X6) — for _merge-survivability_, not deletability. When the framework needs something generic: fix-in-place → classify → promote the generic part upstream.

### Inherited Sunrise improvements (assumed landed before forking)

The earlier nine open Sunrise issues are assumed cleared before the fork. Five are _leveraged_:

| Sunrise issue                                                                     | Where the framework uses it                                                                                                                                                                                                                                                                       |
| --------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| #307 provider-enforced structured output                                          | `f-slot-capture` — `fill_slot` forwards the slot's Zod schema, killing field-name drift                                                                                                                                                                                                           |
| #304 `runtimePromptManaged` honesty flag                                          | every agent that builds its prompt per-call (`f-module-bindings`, `f-facilitation-agents`, `f-emergence`) sets it                                                                                                                                                                                 |
| #303 `isSystem:false` app-agent seed scaffold                                     | all framework agent seeds use the scaffold, never copy a core seed                                                                                                                                                                                                                                |
| #305 `NEXT_PUBLIC_APP_NAME` brand seam                                            | `f-bootstrap` and each app rebrand via env var                                                                                                                                                                                                                                                    |
| **#368 `executeTransaction` accepts tx options** (timeout/maxWait/isolationLevel) | boot-time bulk upserts — module sync (`f-module-core`), slot-definition registration (`f-slots`), map snapshot writes (`f-map`) — are the _same_ many-row interactive-transaction shape that hit P2028 on ConQuest; Daybreak raises the ceiling via this option instead of carrying a local patch |

### Three new fork-readiness issues (#366 / #367 / #368) — sequencing

Filed against Sunrise on 2026-06-30 while building ConQuest. They split cleanly by whether they gate Daybreak's fork:

- **#368 — land upstream _before_ forking.** A real bug, tiny, additive, backward-compatible, generic. Daybreak provably hits the identical P2028 (bulk module/slot/map writes at boot), so it joins `f-seams` at the "must land before the fork" bar. Then Daybreak pulls it in on day one rather than patching `lib/db/utils.ts` locally.
- **#366 / #367 — do _not_ block the fork; merge down when landed.** Authorization-scoping seams (#366 org/tier axis; #367 intra-tenant ownership-scope). Reasons not to gate: both are still `proposal`-status with an open design decision (the better-auth `organization`-plugin fork-in-the-road); they touch `lib/auth/` + ~190 call sites (slow); and they are **largely irrelevant to Daybreak v1**, which is single-user, single-tenant (Lelanea). They are designed as inert seams (no-ops in single-tenant mode), so when they land they merge down as pristine no-ops — zero friction, because Daybreak never edits `lib/auth/` either way.

> **Coordination, not a blocker — #367 ↔ `f-journey-state`.** #367's resolver and Daybreak's `canRead(viewer, subject, scope)` ([[framework-architecture#8. The relationship & cohort overlay — a designed seam, not a built feature|§8]], X2) are the **same predicate** at two layers: _one predicate, three orthogonal inputs_ — tier (#366), ownership (#367), org (multi-tenancy playbook). When we build [[#09 · `f-journey-state` — journey state + access discipline|f-journey-state]], `canRead` must be shaped to _compose with_ that upstream resolver, **not** as a framework-private parallel scope-check that later needs reconciling. #367's driver (different leaders each owning their own resources in one install) is the direct precursor to §8's per-subject and cohort-facilitator visibility, so the seam is shaped for it now even though Lelanea doesn't exercise it.

---

## Features (epic: Framework v1)

A flat list, shown in rough dependency order (most-ready first), the way the Hub would sort it. Order is _emergent from `depends on`_, not prescriptive — the [[#3. Human-centric principles|exploratory-ordering principle]] holds. PR counts are indicative sizing. The **Owner** and **Status** columns are the at-a-glance board: who holds what, and what's free to claim (see [[#Board — status &amp; claiming|the board legend]] below the table).

| #   | Feature                 | Owner           | Status                                                      | Depends on                                                   | ~PRs | Capability                                      |
| --- | ----------------------- | --------------- | ----------------------------------------------------------- | ------------------------------------------------------------ | ---- | ----------------------------------------------- |
| 01  | `f-seams`               | Simon (Sunrise) | **shipped** (v0.5.0)                                        | —                                                            | 2    | Two generic Sunrise core seams (pre-fork)       |
| 02  | `f-bootstrap`           | Simon Holmes    | **shipped** (#4/#6/#8/#9)                                   | f-seams                                                      | 4    | Fork + framework skeleton + enforced boundary   |
| 03  | `f-module-core`         | Simon Holmes    | **shipped** (#10 / #11 / #12)                               | f-bootstrap                                                  | 3    | Module definition, registry, seam, liveness     |
| 04  | `f-map`                 | Simon Holmes    | **shipped** (#16 / #20 / #21)                               | f-bootstrap ✅                                               | 4    | Facilitation map: schema, versioning, format    |
| 05  | `f-slots`               | John            | **shipped** (#19 / #22 / #24)                               | f-bootstrap ✅                                               | 3    | Slot definitions + insert-only values           |
| 06  | `f-module-config`       | Simon Holmes    | **shipped** (#56 / #58)                                     | f-module-core ✅                                             | 4    | Generic Zod config form + config versioning     |
| 07  | `f-module-bindings`     | Simon Holmes    | **shipped** (#33 / #35 / #50 / #53)                         | f-module-core ✅                                             | 4    | Agent / workflow / knowledge bindings           |
| 08  | `f-engagement`          | John            | **shipped** (#103 / #105 / #106 / #107)                     | f-module-core ✅                                             | 3    | Engagement event stream + stats + feedback      |
| 09  | `f-journey-state`       | John            | **shipped** (#27 / #28)                                     | f-map ✅                                                     | 3    | Journey state models + access discipline        |
| 10  | `f-slot-capture`        | John            | **shipped** (#42 / #43 / #44 / #45 / #46)                   | f-slots ✅                                                   | 3    | `fill_slot` / `get_state` capture capabilities  |
| 11  | `f-engine`              | John            | **shipped** (#34 / #36 / #37 / #38)                         | f-map ✅, f-journey-state ✅                                 | 5    | Deterministic engine + GraphStore               |
| 12  | `f-guidance`            | John            | **shipped** (#49 / #51 / #52 / #57 / #59 / #61)             | f-engine ✅, f-slot-capture ✅                               | 6    | Guidance service, capabilities, chat injection  |
| 13  | `f-facilitation-agents` | John            | **shipped** (#68 / #70)                                     | f-guidance ✅                                                | 2    | Facilitation agent family + surface-scoping     |
| 14  | `f-map-editor`          | John            | **shipped** (#110 / #111 / #112 / #115 / #120 / #121)       | f-map ✅, f-engine ✅                                        | 5    | Map editor + journey dry-run simulator          |
| 15  | `f-ops-views`           | Simon Holmes    | **shipped** (#66 / #69 / #83 / #84 / #86 / #89 / #94 / #97) | f-module-config ✅, f-module-bindings ✅, f-journey-state ✅ | 5    | Module admin + journey explorer                 |
| 16  | `f-atlas`               | Simon Holmes    | **shipped** (#104 / #113 / #114 / #116)                     | f-module-bindings ✅, f-facilitation-agents ✅, f-slots ✅   | 4    | Framework atlas (composition view)              |
| 17  | `f-policies`            | John            | **shipped** (#73 / #74 / #75 · t-4 #78)                     | f-facilitation-agents ✅                                     | 3    | Typed facilitation policy kinds                 |
| 18  | `f-emergence`           | John            | **shipped** (#78 / #80 / #82)                               | f-engine ✅, f-facilitation-agents ✅                        | 3    | Proposal pipeline (F17) + escalation (F15)      |
| 19  | `f-overlays`            | John            | **shipped** (#93 / #95 / #96 / #98)                         | f-guidance ✅                                                | 3    | pgvector similarity + proactive guidance        |
| 20  | `f-eval`                | John            | **shipped** (#88 / #90)                                     | f-facilitation-agents ✅, f-guidance ✅                      | 2    | Eval scoring + supervisor over framework convos |

**Critical path:** `f-seams → f-bootstrap → f-module-core/f-map → f-journey-state → f-engine → f-guidance → f-facilitation-agents`. Admin (14–16) and governance (17–19) hang off that spine and parallelise once it exists.

### Board — status & claiming

**Legend.** `shipped` — merged to `main`. `in flight` — an owner is actively building it (its promoted tasks live in the feature's detailed plan). `available` ▲ — every dependency is shipped and no one owns it: **free to claim now**. `blocked → X` — waiting on feature X to ship.

**Claimable right now (▲) — nothing is claimable, and nothing is in flight.** The v1 epic (01–20), all three v1.1 follow-on features (21–23), and the v1.2 distribution epic (24 `f-release` — #187, released as `daybreak-v0.1.0`) are all **shipped**. Every board is closed; what remains are the parked phases. `f-admin-surfaces` (22) is **shipped** (John — #124 / #125 / #126 / #127; detailed plan [[f-admin-surfaces]]); `f-governance-plus` (23) is **shipped** (John — #131 / #134 / #136 / #137; detailed plan [[f-governance-plus]]) — the pure-backend additive layer (module_config/policy proposal subjects + `submit_proposal` capability + a scheduled eval sweep with a framework rubric judge + auto-embed-on-publish + an env-gated webhook nudge channel); and `f-engagement-analytics` (21) — the last follow-on — is now **shipped** (John — #140 / #141 / #142 / #143; detailed plan [[f-engagement-analytics]]): the deferred observability layer (collective per-node heat/drop-off over a map via a new overlay host slot + a stream `groupBy nodeKey` aggregate, the dwell metric on the module Stats tab, and the deferred emit sites — `module.completed` / `module.status_changed` → workflow trigger; `session.started` dropped on its hot-path cost). **What remains are the parked phases** (the relationship/cohort overlay; adjacent commercial/identity/comms components) — see "Follow-on features" below (added 2026-07-08 from the deferred-surfaces sweep) and the parked-phases note. For the v1 epic: **`f-emergence` (18)** is now **shipped** (#78 / #80 / #82; detailed plan: [[f-emergence]]) — the F17 structure-change proposal gate (propose → validate/invariant-check → admin approve → **publish a new map version** with agent authorship preserved, race-hardened) plus the picked-up escalation kind (F15, t-1). Its **eval-wiring thread was split into its own feature, `f-eval` (20)** at close-out (governance observability, separable from the gate; now **shipped** — #88 / #90). **`f-policies` (17)** shipped before it (#73 / #74 / #75; detailed plan: [[f-policies]]) — the typed governance-policy kinds under one `FacilitationPolicy` table (a `kind` discriminator + per-kind Zod payload, F14): the spine + **auto-approval** stored knob (t-1), **relevance/maturity gating** (#73 / #74 / #75; detailed plan: [[f-policies]]) — the typed governance-policy kinds under one `FacilitationPolicy` table (a `kind` discriminator + per-kind Zod payload, F14): the spine + **auto-approval** stored knob (t-1), **relevance/maturity gating** enforced at the facilitation surface (t-2), and **guard-minimums** per scope via a fork-carried core guard-floor seam (t-3). Governance was **reuse, not reinvent** — the only new machinery is the policy data; guards/supervisor/approvals/audit are Sunrise-core, untouched. That completes the **facilitation critical-path spine** (`f-map → f-journey-state → f-engine → f-guidance → f-facilitation-agents → f-policies`). **`f-facilitation-agents` (13)** shipped just before it (#68 / #70; detailed plan: [[f-facilitation-agents]]) — the binding + surface, mechanism-only. **`f-module-bindings` (07)** + **`f-module-config` (06)** are **shipped** (Simon; #33/#35/#50/#53 and #56/#58), and unblocked **`f-ops-views` (15)** (now **shipped** by Simon — #66 / #69 / #83 / #84 / #86 / #89 / #94 / #97; detailed plan: [[f-ops-views]] — the module admin + structural journey explorer). **`f-eval` (20)** — the eval-wiring thread split from f-emergence — is now **shipped** (#88 / #90; detailed plan: [[f-eval]]): metric scoring (t-1 #88) + the post-hoc supervisor (t-2 #90) over the framework's own `facilitation`/`module` conversations, reusing the existing core eval judges. **`f-overlays` (19)** — pgvector similarity + proactive guidance, the deferred F9/F13 half of guidance — is now **shipped** (#93 / #95 / #96 / #98; detailed plan: [[f-overlays]]): the per-node embedding store + drift bridge (t-1), similarity filling the advisory `related` slot strictly downstream of eligibility (t-2, F9), and the proactive-guidance sweep + throttled email nudge + schedulable step type (t-3, split at build into t-3a #96 / t-3b #98, F13). No feature depends on `f-overlays`, so nothing new unblocks. **`f-engagement` (08)** — the module event stream + stats + feedback (spec §4.3 / A9) — is now **shipped** (#103 / #105 / #106 / #107; detailed plan: [[f-engagement]]): the `recordModuleEngagement` emit seam that **finally fires f-module-bindings' shipped-but-unwired `runModuleWorkflowBindings`** (t-1), `record_feedback` + a plain feedback API on the same stream (t-2), and module stats-from-the-stream (A9, never counters) + the detail Stats tab (t-3, split at build into t-3a #106 / t-3b #107). No feature depends on `f-engagement`, so nothing new unblocks. **`f-atlas` (16)** — the read-only composition view (X8, zero new schema; detailed plan: [[f-atlas]]) — is now **shipped** (#104 / #113 / #114 / #116): one endpoint projecting the whole config (modules + bindings + slots + workflows + knowledge + facilitation + published map) onto the read-only `@xyflow` canvas (t-1 #104), a static canvas + deep-links (t-2a #113), semantic zoom (t-2b #114), and cross-cutting lenses (t-3 #116). No feature depends on `f-atlas`. **`f-map-editor` (14)** — the map editor + journey dry-run simulator (spec §5.6 / F18; detailed plan: [[f-map-editor]]) — is now **shipped** (#110 / #111 / #112 / #115 / #120 / #121): the 5-task authoring UI over the shipped map backend + pure engine (canvas + draft t-1, typed edges + regions t-2/t-2b, node inspector + condition builder + live validation t-3, version controls t-4) plus the one new dry-run endpoint (F18, t-5) — pure framework-tier, no migration, no core edit, as the recon predicted. **That ships the last feature — all 20 features of the Framework v1 epic are now shipped.** Shipped so far: `f-seams` · `f-bootstrap` · `f-module-core` · `f-map` · `f-slots` · `f-journey-state` · `f-engine` · `f-slot-capture` · `f-module-bindings` · `f-module-config` · `f-guidance` · `f-facilitation-agents` · `f-policies` · `f-emergence` (#78 / #80 / #82) · `f-eval` (#88 / #90) · `f-ops-views` (#66 / #69 / #83 / #84 / #86 / #89 / #94 / #97) · `f-overlays` (#93 / #95 / #96 / #98) · `f-engagement` (#103 / #105 / #106 / #107) · `f-atlas` (#104 / #113 / #114 / #116) · `f-map-editor` (#110 / #111 / #112 / #115 / #120 / #121).

**To claim a feature:** put your name in its **Owner** cell + set **Status** to `in flight`, then write its detailed plan (`.context/framework/planning/<feature>.md`, following [[f-module-core]] / [[f-map]]) and promote its first tasks — and **push the claim + plan as a standalone docs PR _before_ starting any task work** (so the claim is visible and two owners don't start the same feature; see [[building-a-feature]] step 1). Flip the feature to `shipped` when its last task's PR merges. One owner per feature (the unit of ownership); tasks within a feature are the PR-sized units that owner advances. Suggested split with **John** joining: the claimable features are mutually independent, so different owners can build them in parallel without stepping on each other — e.g. one takes `f-slots`, another `f-module-config`.

### Cross-cutting follow-ups (own PRs — not tied to one feature)

Deferrals that outgrew the feature they surfaced in — recorded **here** on the active board, not buried in a shipped feature's doc, so they stay actionable (a deferral needs an actionable home, not a graveyard). Each is its own small PR when someone picks it up; all are **non-blocking** (nothing depends on them).

- **Shared read-only `@xyflow` canvas primitive** (surfaced f-atlas t-2a) — the `Background`/`Controls`/`MiniMap` config + `useTheme`/`colorMode`/`defaultEdgeOptions`/`proOptions` boilerplate is now **rule-of-three** across three canvases: `journey-explorer/journey-canvas`, `map-builder/map-canvas` (f-map-editor 14), `atlas/atlas-canvas`. Extract the shared decorations (e.g. `components/admin/framework/canvas/`), keeping per-canvas the interaction flags + `nodeTypes`. Touches three **shipped** canvases → coordinate.
- **Shared `stitchById` / `stitchAgents` batch-stitch helper** (surfaced f-atlas t-1) — the collect-ids → `findMany where id in` → `Map` → `?? null` stitch recurs in ≥4 readers (`modules/bindings`, `modules/workflow-bindings`, `modules/knowledge`, `facilitation/agents/binding-queries`, `atlas/queries`). Extract to `lib/framework/shared/`; the agent-stitch (`{id,name,slug,isActive,deletedAt}`) is byte-identical in several. Refactors shipped readers → its own PR.
- **Atlas region-container nodes** (deferred from f-atlas t-2b) — `@xyflow` v12 native parent/child container nodes for map regions + per-module satellite clusters (visual grouping + collapse/expand), additive on the shipped atlas canvas. Pairs with the re-fit-on-toggle nicety (f-atlas.md follow-ups).
- **Shared hydration-safe UTC date formatters** (surfaced f-admin-surfaces t-3 `/code-review`) — the `new Date(iso).toISOString().slice(0,10)` (`formatDate`) and `.slice(0,16).replace('T',' ')` (`formatDateTime`) one-liners are now byte-identical across ~10+ framework admin components (`policies-table`, `modules-table`, `maps-table`, `journeys-table`, `slot-values-browser`, `versions-tab`, `version-history`, and f-admin-surfaces' `proposals-queue` + `proposal-review`). Extract to `lib/framework/shared/` (e.g. `formatDateUtc` / `formatDateTimeUtc`) and rewire all call sites in one pass — a partial rewire would _add_ inconsistency, so it must touch every shipped copy → its own PR, coordinate.

### Follow-on features (epic: Framework v1.1 — deferred surfaces & enhancements) — **all shipped ✅**

A 2026-07-08 sweep of every shipped feature's Follow-ups / Open-questions / does-NOT-do sections surfaced real, pickup-able work that had been deferred into shipped-feature docs (graveyards) rather than onto the board — the recurring pattern being _"deferred to `f-ops-views` (15),"_ which then shipped a narrower scope and left several admin UIs behind (the [[planning-retro#B28|B28]] pattern one level up). Those items were grouped here into **follow-on features** so they ran through the same claim-first → per-task gate loop as v1. **All three (21 · 22 · 23) are now shipped** (John — the epic closed 2026-07-10 with `f-engagement-analytics`); the sections below are retained as the as-built record.

| #   | Feature                  | Owner | Status                                  | Depends on                                                | ~PRs | Capability                                                                |
| --- | ------------------------ | ----- | --------------------------------------- | --------------------------------------------------------- | ---- | ------------------------------------------------------------------------- |
| 21  | `f-engagement-analytics` | John  | **shipped** (#140 / #141 / #142 / #143) | f-engagement ✅, f-ops-views ✅, f-atlas ✅               | 3    | Collective heat/drop-off overlays + dwell + the missing emit sites        |
| 22  | `f-admin-surfaces`       | John  | **shipped** (#124 / #125 / #126 / #127) | f-slots ✅, f-policies ✅, f-emergence ✅, f-ops-views ✅ | 4    | The admin UIs orphaned by the "deferred to 15" handoff + roster UX polish |
| 23  | `f-governance-plus`      | John  | **shipped** (#131 / #134 / #136 / #137) | f-emergence ✅, f-eval ✅, f-overlays ✅                  | 4    | Additive backend enhancements deferred by default across governance/eval  |

#### 21 · `f-engagement-analytics` — the deferred observability layer

_Status:_ **shipped** (John — t-1a #140 / t-1b #141 / t-2 #142 / t-3 #143; detailed plan: [[f-engagement-analytics]])

The analytics half of `f-engagement` (08) + the overlay surface `f-ops-views` (15) promised but shipped without. Shipped as three tasks: **t-1 (#140 / #141)** the journey-canvas **`overlay` host slot** + collective **heat / drop-off** aggregation (`getMapHeat` — `groupBy nodeKey` over `JourneyEvent`, map-scoped through `UserJourney`) + a map-scoped heat page/legend, closing the host-hook gap the [[f-ops-views]] "host-first, overlay-plugs-in" promise left open; **t-2 (#142)** the **dwell** metric (sessionised `module.entered`→`node_completed`, gap-capped) on `getModuleStats` + a Stats-tab card; **t-3 (#143)** the deferred **emit sites** — `module.completed` (all-nodes-complete, idempotent, from the transition caller, engine untouched) + `module.status_changed` → `runModuleWorkflowBindings` trigger. **`session.started` was dropped** (its session-gap check is an unbounded un-indexed full-stream scan on the hot chat path — the plan's own "runs hot → drop" trigger; promote to its own feature if a real trigger appears). Pure framework-tier, **no new table**. No feature depends on 21. Source deferrals: [[f-engagement]] Open-questions/does-NOT-do; [[f-ops-views]] decision 1.

#### 22 · `f-admin-surfaces` — the orphaned admin UIs + ops-views UX polish

_Status:_ **shipped** (John · #124 / #125 / #126 / #127; detailed plan: [[f-admin-surfaces]]) — the four orphaned admin surfaces now exist: slot admin (t-1 #124), policy admin (t-2 #125), proposal/emergence review (t-3 #126), and the ops-views UX polish — searchable `?q=` roster pickers + per-binding `config` editing (t-4 #127). All UI-over-shipped-API except t-1's one new sensitivity-masked slot-values endpoint. No feature depends on 22, so nothing new unblocks.

The admin surfaces deferred _to_ `f-ops-views` (15) that it shipped without (module admin + journeys only). Read APIs all exist — this is UI-over-shipped-API (the 03/06/07 split again). Indicative tasks: **(t)** **slot admin** — definitions browser + values read surface ([[f-slots]] / [[f-slot-capture]] does-NOT-do); **(t)** **policy admin** — `FacilitationPolicy` CRUD UI over the shipped API ([[f-policies]] out-of-scope); **(t)** **proposal / emergence review** UI — approve/reject `StructureChangeProposal`s ([[f-emergence]] out-of-scope); **(t)** **ops-views UX polish** — searchable `?q=` roster pickers (the `ROSTER_LIMIT=100` no-search cap, worst for Knowledge) + per-binding `config`-override editing ([[f-ops-views]] Follow-ups / t-4a).

#### 23 · `f-governance-plus` — additive backend enhancements

_Status:_ **shipped** (John — #131 / #134 / #136 / #137; t-1 review-fix #132; docs #133) · **detailed plan: [[f-governance-plus]]**

The "additive when wanted, deferred by default" enhancements scattered across the governance/eval/overlays features. **Pure backend, no UI, nothing depends on it** — claimed as the quickest/lowest-risk of the two remaining follow-ons (every piece reuses a shipped primitive; no canvas work, no open product-semantics question — contrast 21). Recon split the board's three indicative items into **four** promoted tasks ([[planning-retro#B25]]): **(t-1)** **module_config / policy proposal subjects** — widen the `subjectType` CHECK + pipeline/approval branches, reusing `saveModuleConfig` / `createFacilitationPolicy` ([[f-emergence]] seam); **(t-2)** a **`submit_proposal` authoring capability** granted to the `facilitator` seat (an agent that _writes_ proposals); **(t-3)** a **scheduled eval sweep** — a `framework_eval_sweep` **custom step type** (the fork-lawful scheduler seam, _not_ a bare `AiWorkflowSchedule`, mirroring f-overlays' proactive sweep) + a seeded **framework rubric judge** ([[f-eval]] decision 1); **(t-4)** **auto-embed-on-publish** (fire-and-forget after the four publish paths) + an **env-gated outbound-webhook nudge channel** ([[f-overlays]] open-questions). Two `framework_*` migrations (t-1 CHECK, t-3 `rubricScore`); t-2/t-4 none.

### Follow-on features (epic: Framework v1.2 — distribution)

Opened 2026-08-05, after the Sunrise v0.8.0 sync (#181) and the arrival of the first real leaf fork. v1 and v1.1 built the framework; this epic makes it **consumable by the apps that fork it**.

| #   | Feature     | Owner        | Status             | Depends on | ~PRs | Capability                                                              |
| --- | ----------- | ------------ | ------------------ | ---------- | ---- | ----------------------------------------------------------------------- |
| 24  | `f-release` | Simon Holmes | **shipped** (#187) | —          | 1    | Version constant, release/tag convention, changelog + guard, leaf guide |

#### 24 · `f-release` — Daybreak release & leaf distribution

_Owner:_ Simon Holmes · _Status:_ **shipped** (#187 — one PR, three commits) · _Released:_ **`daybreak-v0.1.0`** · _Depends on:_ — · **detailed plan: [[f-release]]**

Daybreak has 23 shipped features and a real dependent leaf (`reclaim-your-week`, John), but publishes **no releases, no version constant, no changelog, and no leaf-facing sync guide** — so a leaf can only merge `daybreak/main`, an unversioned moving target, and learns about leaf-contract changes only by reading the diff. That is not hypothetical: the v0.8.0 sync made `lib/app/data-export.ts` Daybreak-owned and added a reserved `lib/app/leaf-data-export.ts`, so a leaf filling the file Sunrise's docs point it at now collides with Daybreak on its next merge, in a GDPR path.

Three tasks, a strict chain: **t-1** the version contract (`lib/daybreak-version.ts` — forced to `lib/` root, since the ESLint boundary forbids core importing `@/lib/framework` — plus a `daybreak` field on `/api/health` and a Daybreak `VERSIONING.md` defining the leaf-facing public surface); **t-2** `CHANGELOG.md` seeded with the `0.1.0` entry (carrying the 0.8.0 leaf-contract change) + `building-on-daybreak.md`, the leaf's sync guide; **t-3** a changelog CI guard on the fork-owned `app:ci-checks` seam, so the changelog cannot quietly go stale. First release is **`daybreak-v0.1.0`** — tags are **prefixed**, because the ten `vX.Y.Z` tags in a dev clone are **Sunrise's** (origin has none) and an unprefixed tag would collide when Sunrise cuts its own. Expected no migration, no `framework_*` schema, no `lib/framework/` runtime code — one deliberate 2-line core edit to the health route. Cutting the release and onboarding the leaf are process steps, not tasks.

> **Not on this board (deliberately).** **Blocked** items keep their existing home, not a claimable row: subject-scoped stats + cross-user journey visibility waited on Sunrise **#366/#367** (tenancy — [[upstream-asks]] ledger); those landed in Sunrise 0.12.0 (synced 2026-09-16) and the delegation is now board row **31** (Phase 6), and the **auto-approval risk taxonomy** (§9.2) waits on real proposal data. **Upstream Sunrise asks** are now **all filed** — the [[upstream-asks]] ledger's ten rows (#367/#366, #398, #403, #410, #411, #412, #413, #414, #415, #416) each carry a fork-perspective note on the Sunrise issue; the fork shim is live and correct meanwhile. The remaining action is **not** a filing pass but the standing **"delegate-when-it-lands" check on every Sunrise pull** ([`CUSTOMIZATION.md` §9](../../CUSTOMIZATION.md)): when an issue lands, delete the shim and delegate to the upstream resolver, then close the row. **Latent / scale-gated** deferrals (GraphStore CTE swap, engagement index migration, per-graph facilitation, guard-minimums module scope, node-events-fire-bindings, richer config descriptors, boot-sync scaling) stay in their feature docs until a trigger fires.

### Follow-on features (epic: Framework v1.3 — leaf-consumer hardening)

Opened 2026-08-14. v1.2 made Daybreak **distributable**; v1.3 makes it **usable by
the app that took the distribution**. The trigger is real consumption: the first
leaf (`reclaim-your-week`) has filed ten issues against this repo, and the
[[issue-backlog]] reconciliation found Daybreak carrying five shims for Sunrise
seams that landed in **0.7.0**.

**The detailed plan for every phase below is [[issue-backlog]]** — it holds the
per-task specs, "done when" criteria and the sequencing rationale, so these
phases do not each get their own `f-*.md`.

| #   | Phase                                   | Owner | Status        | Depends on         | ~PRs | Capability                                                                                          |
| --- | --------------------------------------- | ----- | ------------- | ------------------ | ---- | --------------------------------------------------------------------------------------------------- |
| 25  | v1.3 Phase 0 — stop the bleeding        | John  | shipped       | —                  | 1    | Registry realm fix (#160), ledger reconciliation, sync-ancestry repair + guard (#539)               |
| 26  | v1.3 Phase 1 — delegate landed seams    | John  | **in flight** | 25 (ledger truth)  | 3    | Delete five shims for seams that landed in Sunrise 0.7.0 (#398/#410/#411/#415/#416)                 |
| 27  | v1.3 Phase 2 — run-scoped slot spine    | —     | available ▲   | —                  | 2    | `runId` provenance + batched history read (#156/#162), run context on writes (#167)                 |
| 28  | v1.3 Phase 3 — journey & seeding seams  | —     | available ▲   | —                  | 2–4  | `createJourney` (#159), `recordNodeProgress` (#168), seed ESLint + boot (#157/#158)                 |
| 29  | v1.3 Phase 4 — enforcement & authoring  | —     | blocked → 26  | 26 (t-1.1 + t-1.2) | 2    | Facet enforcement + slug-level facets (#169), array config descriptors (#161)                       |
| 30  | v1.3 Phase 5 — upstream backlog hygiene | —     | available ▲   | —                  | 1    | Ledger rows for the nine upstream fixes awaiting a sync + the leaf-gotchas doc                      |
| 31  | v1.3 Phase 6 — delegate the auth seam   | —     | available ▲   | —                  | 1    | `access.ts` `canRead`/`subjectScope` → core `lib/auth/authorization.ts` (#366/#367, Sunrise 0.12.0) |

#### 25 · v1.3 Phase 0 — stop the bleeding, establish truth

_Owner:_ John · _Status:_ **shipped** (#201) · _Depends on:_ — · **detailed plan: [[issue-backlog]] Phase 0**

Three tasks, taken as **one PR** rather than the plan's two — they are independent
of each other and none is a PR's worth alone. **t-0.1** backs the module and
framework-capability registries with `globalThis` (#160): under Next 16 +
Turbopack the instrumentation graph is a different realm from the route/RSC
graph, so a boot-populated registry was empty on every request and the entire
generic module-config surface was dead for any leaf module — the one leaf-blocking
item in the epic. **t-0.2** reconciles [[upstream-asks]], which had six rows still
reading "filed — awaiting the seam" for seams that landed in Sunrise **0.7.0**, and
adds the "Running the check" recipe so it stops going stale. **t-0.3** repairs the
merge base that the squash-merged v0.8.1 sync (#196) reset, and adds the
`framework:sync-ancestry` guard to `app:ci-checks` so it cannot happen unnoticed
again (Sunrise #539).

> ⚠️ **This PR must be merged with a merge commit, not a squash** — it contains the
> `-s ours` commit that records the v0.8.1 ancestry, and squashing would discard
> exactly the parent it exists to restore. The new guard fails the build on `main`
> if that happens, which is the intended safety net.

---

#### 26 · v1.3 Phase 1 — delegate the landed seams

_Owner:_ John · _Status:_ **in flight** · _Depends on:_ 25 (ledger truth) · **detailed plan: [[issue-backlog]] Phase 1**

Five tasks, taken as **three PRs** grouped by the surface they touch rather than
one PR per task — t-1.5 is a single argument and t-1.4 two call sites; neither is
a PR's worth alone (the [[building-a-feature]] sizing self-check).

- **PR A — t-1.1 + t-1.5 · the slot capabilities.** `loadExposureConfig` stops
  re-querying `AiAgentCapability` and reads `context.customConfig` (#411), which
  also deletes one indexed lookup from every slot capture and every `get_state`;
  `extract.ts` passes `phase: 'slot-extraction'` (#410) so slot-capture spans stop
  being tagged `evaluation`. Both live in `data-slots/capabilities/`.
- **PR B — t-1.2 · module capability namespacing.** Delete
  `NamespacedModuleCapability` and register the author's capability directly via
  the core seam `register(inner, { slug, guard })` (#398). This is the one with
  real structure: `sync.ts` also consumes the wrapper (for the namespaced slug and
  the rewritten `functionDefinition.name`), so the wrapper's derivations become
  pure functions used by both call sites. Gates **29 · Phase 4**, where facet
  enforcement (#169) becomes another `guard` rather than a second wrapper.
- **PR C — t-1.4 + t-1.3 · the two surface layers.** Both surfaces
  (`guidance/surface.ts`, `facilitation/agents/surface.ts`) hand-roll the same
  resume query; both delegate to `findResumableConversation` (#416). t-1.3 rides
  along because it is the same two surfaces and — see the decision below — is now
  a documentation correction rather than a route rewrite.

**Reconciliation against the tree (2026-08-14), before building.** Three of the
five rows survived verification unchanged (t-1.1, t-1.4, t-1.5). Two moved:

1. **t-1.2 is wider than "delete the wrapper".** The ledger names `register.ts`
   as the only consumer; `sync.ts` uses `namespaceModuleCapability` too, to derive
   the `ai_capability` row's slug/name/`functionDefinition`. The wrapper can only
   go if those derivations survive as pure functions. Also a deliberate behaviour
   change to record: an out-of-scope call now returns the core's
   `capability_guard_denied` instead of the fork's `out_of_module_scope`, and is
   refused **before** the rate limiter rather than inside `execute()` — strictly
   better, but it is a visible error-code change.
2. **t-1.3's premise does not survive contact with the routes — the two framework
   stream routes are not shims and are not being collapsed.** #415 landed, so
   `consumerChatRequestSchema` carries `scope` and the stated reason for the
   shadow (decision 6: "the core consumer route/schema can't carry `scope`") is
   genuinely stale. But the routes never only threaded scope: they resolve the
   agent **server-side** from the module/role, tag the conversation with
   `contextType`/`contextId` — which the core consumer route deliberately does not
   accept ("no contextType/contextId/entityContext (admin-only concepts)") — and
   the module route emits the `module.entered` engagement event. A thin wrapper
   over the core route would drop the context tuple, so new surface conversations
   would go untagged and **resume would break**. The facilitation route threads no
   `scope` at all, so it was never a #415 shim in the first place. And the security
   note the backlog asks us to honour argues the same way round: today `scope` is
   **server-derived** (`encodeScope({ moduleSlug })`), and delegating would move it
   onto an untrusted client field. So t-1.3 becomes: correct the now-false
   decision-6 rationale in both route headers and in `guidance/surface.ts`, close
   the #415 row in [[upstream-asks]] as **landed — no carry** (the seam is real and
   a _leaf_ can now use it; Daybreak simply isn't the consumer), and record what
   core still lacks for a surface route — a consumer entry point that accepts a
   **server-resolved** context tuple — as the honest remaining ask.

---

### 01 · `f-seams` — Sunrise core seams (pre-fork)

_Owner:_ Simon (Sunrise) · _Status:_ **shipped** (Sunrise v0.5.0) · _Depends on:_ — · _~2 PRs_

The framework's only two core touch-points, PR'd to Sunrise _before_ forking (verified absent, June 2026). Generic, so they belong upstream.

- **t** — `CapabilityContext.scope?: Record<string,string>` added (`lib/orchestration/capabilities/types.ts`), populated by the dispatcher; CHANGELOG + contract. Generic map, no framework vocabulary.
- **t** — Context-contributor registry on `buildContext()` (`registerContextContributor(type, loader)`); fork-owned `lib/app/context-contributors.ts` scaffold + `initAppContextContributors()`; CHANGELOG + contract.

_Done when:_ both merged to Sunrise `main`; vanilla behaviour unchanged.

### 02 · `f-bootstrap` — fork + skeleton + boundary

_Owner:_ Simon Holmes · _Status:_ **shipped** (#4 / #6 / #8 / #9) · _Depends on:_ f-seams · _~4 PRs_ · **detailed plan: [[f-bootstrap]]**

The framework repo's home and its enforced boundary.

- **t** — Fork Sunrise (post-seams, with #368 `executeTransaction` options landed); apply `NEXT_PUBLIC_APP_NAME` (#305); document the `framework ← Sunrise` upstream-merge procedure (`.context/framework/upstream.md`).
- **t** — `lib/framework/` skeleton (`modules/`, `facilitation/`, `data-slots/`, `shared/`); `shared/scope.ts` owns the scope vocabulary; empty `framework-*.prisma` files registered.
- **t** — Boundary enforcement (X6): ESLint rule over all framework paths both directions + CI; migration-hygiene CI check; "zero framework vocab in Sunrise code" check. Must _fail_ on a deliberate cross-boundary import.
- **t** — `initFramework()` wiring (registers an empty context contributor); `.context/framework/` doc namespace; unit + integration test scaffolding.

_Done when:_ fork builds/boots; boundary checks green and provably catch violations; empty schema migrates clean.

### 03 · `f-module-core` — module definition, registry & liveness

_Owner:_ Simon Holmes · _Status:_ **shipped** (t-1 #10 · t-2 #11 · t-3 #12) · _Depends on:_ f-bootstrap · _~3 PRs_ · **detailed plan: [[f-module-core]]**

The code-first module spine (spec §4): code defines the module; the DB row holds only operator config.
**First pure framework-tier feature — no Sunrise touch-point, so no upstream issue.**

- **t-1** — Registration → row: `ModuleDefinition` + `registerModule()` + registry **+** `Module` model (`framework_module`, free-form `String` status per X1) + boot-time upsert-by-slug sync (`syncFramework()` after `initLeafApp()`) + `isRegistered` handling + register→row proof test (+ the plan doc, folded in).
- **t-2** — Module liveness: pure `isModuleLive(module, flags, now)` (status × flag × window, A5) with an optional entitlement-predicate seam (C1); permutation tests.
- **t-3** — Read API (`GET /api/v1/admin/framework/modules`) + demo fixture **tests-only** (no live row), proving registration → row → admin visibility end-to-end.

_Sizing: the rev-16 spec's four indicative tasks fold to **three** promoted PRs — the registry-only task was commit-sized and inert without its sync ([[planning-retro#B1 · Sizing self-check when promoting tasks]]), so it ships with the model+sync as one "code → row" vertical._

Three forkability reconciliations vs the rev-16 spec (decided 2026-07-02, see decisions log): demo is
tests-only (fork boots to an empty modules table); admin visibility is a read API not a page (UI →
`f-ops-views`); the leaf registers modules from the single `initLeafApp()` hook, not a per-concern
`lib/app/modules.ts` scaffold.

### 04 · `f-map` — facilitation map

_Owner:_ Simon Holmes · _Status:_ **shipped** (t-1 #16 · t-2 #20 · t-3 #21) · _Depends on:_ f-bootstrap · _~4 PRs (3 promoted)_ · **detailed plan: [[f-map]]**

The authored typed-graph, whole-map snapshot versions (F1/F2). Pure framework-tier (no Sunrise
touch-point, no upstream issue). Shipped the models + version service (draft/publish/rollback) + the
node/edge/region/condition **format** + format-level publish validation + the admin API; the canvas
editor is `f-map-editor` (14) and graph-invariant checks (cycles/reachability) are `f-engine` (11).
**Unblocks `f-journey-state` (09).**

- **t** — `FacilitationGraph` + `FacilitationGraphVersion` models (mirror `AiWorkflowVersion`); draft-on-edit / publish / rollback.
- **t** — Node/edge JSON format: stable `key`s, node types, `completionMode`, `onFirstArrival`, **region containers** first-class (F5).
- **t** — Typed edges (four only, F3) + the family-tagged `condition` format (`state|slot|temporal`, F4) with publish-time rejection of unknown families.

### 05 · `f-slots` — slot definitions + values

_Owner:_ John · _Status:_ **shipped** (t-1 #19 · t-2 #22 · t-3 #24) · _Depends on:_ f-bootstrap · _~3 PRs_ · **detailed plan: [[f-slots]]**

The data-slot shape the other layers need (spec §6); deep capture-loop design deferred (§9.1).

- **t** — `SlotDefinition` (scope/visibility/mode/dataType/sensitivity/priorityWeight).
- **t** — `SlotValue` insert-only (version/value/valueJson/confidence/sourceType/provenance/supersededAt) + indexes incl. `@@index([userId, capturedAt])`; erasure hooks + cascade.
- **t** — Module-declared slot registration: `slotDefinitions` in `ModuleDefinition` upserted at boot, scoped `module:<slug>`.

### 06 · `f-module-config` — config form + versioning

_Owner:_ Simon Holmes · _Status:_ **shipped** (t-1 #56 · t-2 #58) · _Depends on:_ f-module-core ✅ · _~4 PRs (2 promoted)_ · **detailed plan: [[f-module-config]]**

Generic admin config from each module's Zod schema (A4) + config versioning (A10). **API-first: ships
the config-validation + versioning engine (incl. the server-side Zod→field-descriptor walker that is
A4's substance); the client generic-form + version-history tab are `f-ops-views` (15)** — the same
read-API-here / UI-there split 03 and 07 made. **Unblocks `f-ops-views` (15).**

- **t-1** — `ModuleVersion` snapshot table + config-versioning service: validate an operator write
  against the registered module's `configSchema` (A4), write `Module.config`, snapshot a point-in-time
  version (the `AiAgentVersion` model — no draft buffer; **restore**, not rollback); `createdBy` hand-FK; audit.
- **t-2** — Zod→descriptor walker (schema → renderable field descriptors, the A4 engine) + config +
  version-history admin APIs (`[slug]/config` GET/PUT · `[slug]/versions` GET · `.../[version]/restore` POST).

### 07 · `f-module-bindings` — agent / workflow / knowledge bindings

_Owner:_ Simon Holmes · _Status:_ **shipped** (#33 / #35 / #50 / #53) · _Depends on:_ f-module-core ✅ · _4 PRs_ · **detailed plan: [[f-module-bindings]]**

Attach by binding, not ownership (A6); the module spine's second half — makes a registered module _functional_. **API-first: ships the binding APIs; the binding UI is `f-ops-views` (15)**, the same split f-module-core made (read API here, page there). Unblocks `f-atlas` (16).

- **t-1** — `ModuleAgentBinding` pivot (role/seat, `isPrimary`, `config`) + `ModuleDefinition.agentRoles` + bind-time role validation + admin binding **API** (seat-binding UI → f-ops-views); `runtimePromptManaged` (#304) honoured, framework agent seeds are f-facilitation-agents.
- **t-2** — Module-declared capabilities into the global registry namespaced `module-slug.tool` (A8), registered from the framework's own boot path; a scope-refusal helper reads `scope.moduleSlug` via the `f-seams` map so a capability can refuse out-of-scope (the live `scope` population is f-guidance's X5 surface-scoping — seam shaped here).
- **t-3** — `ModuleWorkflowBinding` (event → workflow) mirroring `AiWorkflowTrigger`, dispatched via `drainEngine` (the hook system is outbound-only); the module-lifecycle event _source_ is coordinated with f-engagement (08).
- **t-4** — Knowledge grants: bound agents get doc/tag access via the existing restricted-access system ("no new mechanism at all") — thin, may fold into t-1.

### 08 · `f-engagement` — event stream + stats + feedback

_Owner:_ John · _Status:_ **shipped** (t-1 #103 · t-2 #105 · t-3a #106 · t-3b #107) · _Depends on:_ f-module-core ✅ · _~3 PRs (4 shipped — t-3 split)_ · **detailed plan: [[f-engagement]]**

Stats from an insert-only stream, never counters (A9). Shares `JourneyEvent` with the journey log (§4.3 = §5.4). Promoted-task table lives in [[f-engagement]].

- **t-1** ✅ (#103) — the `recordModuleEngagement` **emit seam** (insert a `JourneyEvent` **and** fire `runModuleWorkflowBindings`, each limb best-effort/isolated) wired at the module surface chat entry (`module.entered` on a fresh conversation). **The first production caller of f-module-bindings' shipped-but-unwired receiver** — so "when a user enters this module, run workflow Y" finally fires. Via `drainEngine`, not the outbound-webhook-only hook system (07 reconciliation #4).
- **t-2** ✅ (#105) — `record_feedback` framework capability (scope-trusted module attribution, comment masked in provenance) + a plain `withAuth` feedback API; both write `module.feedback` to the same stream (no core feedback table — `AiMessage.rating` is the wrong grain).
- **t-3a** ✅ (#106) — `getModuleStats` (unique users / entries / completions / returning / ratings summary) **derived from the stream, never counters** (A9), subject-scope-shaped; `GET …/modules/[slug]/stats` (`withAdminAuth`). **t-3b** ✅ (#107) — the read-only Stats tab on the shipped module-detail host. _(Dwell deferred — an honest metric needs sessionization.)_

> **Note (schema/emit reconciliations at build).** `JourneyEvent` was **created by f-journey-state (09)** and already stamps `moduleSlug` on `node_*` events, so 08 added only new `type` values (X1, no migration) + the read side. Operator lifecycle is **not** a per-user engagement event (`userId` is NOT NULL), so v1 emits `module.entered` + `module.feedback` only; module progression is read from the existing `node_completed` events. See the decisions log.

### 09 · `f-journey-state` — journey state + access discipline

_Owner:_ John · _Status:_ **shipped** (t-1 #27 · t-2 #28) · _Depends on:_ f-map · _~3 PRs (2 promoted)_ · **detailed plan: [[f-journey-state]]**

Per-user state on the satellite convention + the access seam that makes §8's relational features a one-function change later (X2/X3).

> **Design-time awareness — Sunrise #367 (and #366).** This feature builds `canRead`, which is the framework-layer instance of the **same** authorization predicate Sunrise #367 (intra-tenant ownership-scope) and #366 (org/tier axis) generalise upstream: _one predicate, three orthogonal inputs_ — tier / ownership / org. **Build `canRead` to compose with that resolver, not as a private parallel check.** Concretely: the `scope` argument is the seam that carries #367's ownership/subject input; keep it an open, structured value (not a hard-coded `viewer === subject`) so that when #367 lands, wiring it in is supplying an input to an existing predicate, not a rewrite. Check #367's resolved shape before finalising the signature — if it has landed upstream by then, delegate to it; if not, mirror its _one predicate, three inputs_ contract so the later merge is additive. This is the wider-application requirement (multi-leader, owner-scoped, eventually cohort-facilitator visibility) designed in from day one even though Lelanea is single-user. See [[framework-architecture#8. The relationship & cohort overlay — a designed seam, not a built feature|§8]] and the [[#Three new fork-readiness issues (#366 / #367 / #368) — sequencing|sequencing note]] above.

- **t** — `UserJourney` (non-nullable `contextKey @default("")`, X3) + `UserNodeState` projection + extend `JourneyEvent` use; satellite FK + cascade + erasure hook.
- **t** — `canRead(viewer, subject, scope)` single access function; route every journey/slot read through it. **Shape `scope` to admit #367's ownership input** (own / team / all) and #366's tier input so the function composes with the Sunrise resolver rather than duplicating it; one user equals subject today, but the contract is the three-input predicate, not a hard equality.
- **t** — Subject-scope filter on analytics queries from the start (one user now; owner/team/cohort later — the same #367 axis at the analytics layer).

### 10 · `f-slot-capture` — capture capabilities

_Owner:_ John · _Status:_ **shipped** (#42 / #43 / #44 / #45 / #46) · _Depends on:_ f-slots ✅ · _5 PRs_ · **detailed plan: [[f-slot-capture]]**

Silent capture riding the existing tool loop (D5); leverages #307.

- **t** — `get_state()` + `fill_slot(...)` as `BaseCapability`s; validate against definition or mint open-mode slug; write next version; silent in conversation; `processesPii=true` + `redactProvenance`.
- **t** — Sensitivity-driven masking-before-storage (`special_category` strictest); `fill_slot` extraction forwards the slot's Zod schema as enforced structured output (#307).
- **t** — Selective per-agent exposure via grant `customConfig` (which groups/scopes an agent may read/write), enforced inside the capability.

### 11 · `f-engine` — deterministic engine + GraphStore

_Owner:_ John · _Status:_ **shipped** (t-1 #34 · t-2 #36 · t-3 #37 · t-4 #38) · _Depends on:_ f-map, f-journey-state · _~5 PRs (4 promoted)_ · **detailed plan: [[f-engine]]**

The deterministic spine (spec §5.3), pure and LLM-free; sole writer of state (F11). One coherent capability.

- **t** — `GraphStore` interface + Postgres recursive-CTE impl (reachability, neighbours, paths) + tests (F8).
- **t** — Availability computation: typed-edge + state- + slot-predicate evaluation, with explainable lock reasons.
- **t** — Temporal predicates + timezone-resolved `now` per journey (C7).
- **t** — Transition validation (`applyEvent`): once/repeatable semantics, single-transaction event + projection write, structured rejections.
- **t** — Publish-time invariant validation: cycles, unreachable-required, live-state key-removal warnings (shared with `f-emergence`).

### 12 · `f-guidance` — guidance service, capabilities & chat injection

_Owner:_ John · _Status:_ **shipped** (#49 / #51 / #52 / #57 / #59 / #61) · _Depends on:_ f-engine ✅, f-slot-capture ✅ · _6 PRs_ · **detailed plan: [[f-guidance]]**

Engine computes what's possible; guidance ranks what's wise; agents narrate (F12). First moment it's _felt_ in conversation.

- **t** — `guidance.ts` ranking already-eligible options using recency-weighted slot reads; reasons in payload.
- **t** — Capability family: `get_journey_state`, `get_next_steps`, `get_progress_synopsis`, `suggest_focus`, `request_transition` — built-in `BaseCapability`s, granted per agent.
- **t** — Register the framework context contributor via the `f-seams` registry; inject module scope + journey position + fresh slots per turn.
- **t** — Surface-scoped conversations (X5): each module/facilitation surface its own `AiConversation` with its bound agent; continuity carried by state, not threads. **This is where `scope.moduleSlug` gets _populated_** (set `ChatRequest.scope` when opening a module surface) — which **completes the enforcement half of [[#07 · `f-module-bindings` — agent / workflow / knowledge bindings|f-module-bindings (07)]]'s** scope-refusal seam: 07 shipped (in #35) the `isInModuleScope` predicate that _reads_ `scope.moduleSlug`, but a module capability only actually refuses out-of-scope once this task writes it (07 reconciliation #3). 07's interim posture is absent-scope → allow; leave it (a global flip to refuse-on-absent is `f-policies`' call and breaks 07's tests — see [[f-guidance]] decision 7).
- **t** — Boundary test: a strip of the framework leaves `buildContext()` with one fewer contributor (proves the seam stayed clean).

### 13 · `f-facilitation-agents` — facilitation agent family

_Owner:_ John · _Status:_ **shipped** (t-1 #68 · t-2 #70) · _Depends on:_ f-guidance ✅ · _2 PRs_ · **detailed plan: [[f-facilitation-agents]]**

Same binding pattern as modules, second scope (one mechanism, two scopes). Shipped **mechanism-only** — a fork brings its own facilitation agents; Daybreak seeds no family.

- **t-1** (#68) — `FacilitationAgentBinding` (flat `(agentId, role)`, hand-FK cascade + `@@unique([role])`, role validated against a framework-owned `FACILITATION_ROLES` constant) + the bind/update/unbind/list service + `withAdminAuth` binding API. Mirrors `ModuleAgentBinding`, minus the module. Capabilities are granted through the existing `AiAgentCapability` pivot (no new mechanism).
- **t-2** (#70) — the facilitation **surface**: `resolveFacilitationSurface(userId, role)` (mirrors `resolveModuleSurface` — role-keyed, `public`-visibility gated, **no scope** since guidance caps are scope-agnostic) + the consumer chat route `…/framework/facilitation/[role]/chat/stream` tagging `contextType='facilitation'`.
- **~~t-3~~** (dropped) — seed a default facilitation family. Resolved **mechanism-only**: a fork seeds its own via the #303 `isSystem:false` scaffold. The **per-scope guard settings** the board's third indicative task named are deferred to **`f-policies` (17)** (they need that feature's `FacilitationPolicy` typing).

### 14 · `f-map-editor` — map editor + dry-run simulator

_Owner:_ John · _Status:_ **shipped** (#110 / #111 / #112 / #115 / #120 / #121) · _Depends on:_ f-map, f-engine · _~5 PRs_ · **detailed plan: [[f-map-editor]]**

Authoring tools on the engine (spec §5.6). **Note:** Sunrise's workflow builder is built on **`@xyflow/react`** (v12) — `components/admin/orchestration/workflow-builder/` (`ReactFlow` + `Background`/`Controls`/`MiniMap`, `node-types/`, `edge-types/`, `workflow-mappers.ts`); reuse those primitives. _(Corrects an earlier "custom canvas, not React Flow" note — re-verified 2026-07-06 against `package.json` + `workflow-canvas.tsx` while planning `f-ops-views`.)_ Promoted-task table lives in [[f-map-editor]].

- **t** — Confirm Sunrise's builder primitives (canvas, palette, edges, config panel) + the reuse seam.
- **t** — Facilitation node/edge palette + typed-edge drawing; region collapse/expand.
- **t** — Per-node config panel (gating conditions, first-arrival, module binding); pre-publish validation surfacing (from `f-engine`).
- **t** — Version-history / publish / rollback controls.
- **t** — Journey dry-run simulator: synthetic user (completions, slots, clock) vs a _draft_ map — available/locked/why, guidance ranking, temporal-gate + policy preview (F18).

### 15 · `f-ops-views` — module admin + journey explorer

_Owner:_ Simon Holmes · _Status:_ **shipped** (#66 / #69 / #83 / #84 / #86 / #89 / #94 / #97 — 5 tasks, t-4 split a/b/c + t-5 split a/b at build) · _Depends on:_ f-module-config, f-module-bindings, f-journey-state · **detailed plan: [[f-ops-views]]**

The operational admin surfaces — the UI half three shipped features deferred here **API-first**: `f-module-core` (03) shipped the module read API and deferred its page; `f-module-config` (06) shipped the config-validation + version engine and deferred the client form + history tab; `f-module-bindings` (07) shipped the binding APIs and deferred their management views.

**Scoped to its 08-independent core (the 08 question, settled — see the 2026-07-06 decisions-log entry).** The earlier sketch bundled `f-engagement` (08) deliverables — "stats from the event stream", "collective heat / drop-off overlays" — with no dependency edge; those are 08's stats-from-the-stream (A9), not shipped. So this feature builds the **operational admin** (module list/detail, config form, binding views, module lifecycle writes) + a **structural** journey explorer (read-only map + _individual_ replay over 09, whose events `f-engine` already writes). 08's **analytics surfaces move to 08**, mounting **additively** onto this feature's host surfaces later — **host-first, overlay-plugs-in**: 15 leaves the extension points (a tab-array slot + a canvas overlay prop), and 08's free-form `JourneyEvent.type`s (X1) flow into the timeline with zero rework.

Promoted to **5 tasks** along the cleanest seam — UI-over-shipped-API vs builds-one-new-endpoint (isolating the security-sensitive `DELETE` + invalidation as its own task). Promoted-task table lives in [[f-ops-views]].

- **t-1** — Framework admin **scaffolding + nav seam** (fill `lib/app/admin-nav.ts` → a new client-safe `lib/framework/admin-nav.ts`, delegating to a leaf-reserved `lib/app/leaf-admin-nav.ts` — the boot-seam pattern applied to client nav) + the module **list** page (`app/admin/framework/modules/`, the first framework admin page) over `GET /modules`.
- **t-2** — Module **detail** shell (tabbed, the `agent-form` multi-tab model) + **Config** tab (renders 06's `FieldDescriptor[]`, saves via `PUT /config`) + **Versions** tab (list + restore) — pure UI over 06's shipped API; ships the tab-array host.
- **t-3** — Module **lifecycle writes** (the endpoints this feature builds): `PATCH /modules/[slug]` (edit status / audience / window / name) + `DELETE /modules/[slug]` wired to `invalidateAllAgentAccess()` (the 07 hard-delete invalidation gap — the DB cascade runs no app code) + a **Settings** tab + danger-zone delete.
- **t-4** — **Binding** management tabs (Agents / Workflows / Knowledge) over 07's `/modules/[slug]/{agents,workflows,knowledge}` endpoints — the binding UI deferred from [[#07 · `f-module-bindings` — agent / workflow / knowledge bindings|f-module-bindings (07)]] ([[f-module-bindings]] reconciliation #1).
- **t-5** — **Journey explorer**: the journey **read endpoints** this feature builds over 09's `canRead`-guarded queries (viewer = `{ userId, isAdminSupport: true }` from the admin session, not `role === 'ADMIN'`) + a read-only `@xyflow/react` canvas (reuse the workflow-builder canvas primitives) + individual-journey replay from the event log; subject-scope-ready queries.

### 16 · `f-atlas` — framework atlas

_Owner:_ Simon Holmes · _Status:_ **shipped** (t-1 #104 · t-2a #113 · t-2b #114 · t-3 #116) · _Depends on:_ f-module-bindings ✅, f-facilitation-agents ✅, f-slots ✅ · _4 PRs_ · **detailed plan: [[f-atlas]]**

A pure projection, zero new schema (X8): the zoomable composition view.

- **t-1** (#104) — One read-only endpoint assembling the composition graph from registries + binding pivots + slot definitions + workflow bindings + knowledge grants + published map (all-modules aggregate readers over the shipped batch-stitch; normalized `CompositionProjection`).
- **t-2a** (#113) — Static read-only `@xyflow` canvas + hub-and-spoke mapper + click-through deep-links to the real editors (agent/workflow/module/map; slot/facilitation degrade). **t-2b** (#114) — semantic zoom (zoom in → satellites unfold; "Show all detail" toggle). _Region-container nodes deferred (see cross-cutting follow-ups)._
- **t-3** (#116) — Cross-cutting lenses (agent/slot/workflow-centric): pick an entity → highlight it + its 1-hop connections, dim the rest.

### 17 · `f-policies` — typed facilitation policy kinds

_Owner:_ John · _Status:_ **shipped** (t-1 #73 · t-2 #74 · t-3 #75) · _Depends on:_ f-facilitation-agents ✅ · _3 PRs_ · **detailed plan: [[f-policies]]**

Typed policy kinds under one `FacilitationPolicy` table, not a rules blob (F14) — governance is **reuse, not reinvent** (only the policy data is new; guards/supervisor/approvals/audit are Sunrise-core). Sized to **3 PRs** cut along the _build-cost_ seam, not one-per-kind: the thin auto-approval knob folded into the spine; the three enforced kinds each stand alone because their machinery differs.

- **t-1** (#73) — `FacilitationPolicy` table (free-string `kind` + CHECK, per-kind `z.discriminatedUnion('kind')` payload, `createdBy` hand-FK) + validate-on-write CRUD service + `withAdminAuth` API + audit; the **auto-approval** kind (`autoApprove: none`, stored-only — f-emergence reads it; §9.2 taxonomy deferred).
- **t-2** (#74) — **relevance/maturity gating** kind (graph-scoped `{stage|region} → allowed roles`) + `deriveCurrentStageRegion` + the gate at `resolveFacilitationSurface` (fail-open; a gated role → 404).
- **t-3** (#75) — **guard-minimums-per-scope** kind (F16) via a **fork-carried core guard-floor seam** (`registerGuardFloorContributor` in `streaming-handler.ts`; generic, vocab-free, empty-registry = prior behaviour; a floor only raises a guard), ledgered [[upstream-asks]].
- **t-4** ✅ **(escalation, F15 — delivered under `f-emergence` (18) t-1, #78)** — the `escalation` kind + a **second fork-carried core seam** (a post-detection guard-event contributor, `registerGuardEventContributor` at the 3 guard sites): when an inline guard fires on a facilitation role's surface, an `escalation` policy turns it into a notify + log pathway. First **deferred** at this feature's close-out, then picked up first under f-emergence (its natural home — escalation needed the guard-event seam f-emergence owns). The kind's code lives in this feature's `kinds.ts`/`escalation.ts`; only the build effort moved. v1 response = notify + log (workflow-rerouting + resources are a documented follow-up).

### 18 · `f-emergence` — structure-change proposal pipeline + escalation

_Owner:_ John · _Status:_ **shipped** (t-1 #78 · t-2 #80 · t-3 #82) · _Depends on:_ f-engine ✅, f-facilitation-agents ✅ · _3 PRs_ · **detailed plan: [[f-emergence]]**

Emergence through one narrow gate (F17); the spine is never written raw. Also **picked up f-policies' deferred escalation kind (F15) as t-1** (shared approval/notify machinery). The **eval-wiring thread split out to [[f-eval]] (20)** at close-out.

- **t-1** ✅ (#78) — **Escalation-pathway kind (F15)** + a 2nd fork-carried core seam (a post-detection guard-event contributor). Delivered f-policies' deferred t-4.
- **t-2** ✅ (#80) — `StructureChangeProposal` model + the **map-subject** propose pipeline (Zod → `validateGraphInvariants` → risk-class → its own approval state; `createdBy="agent:<slug>"`); v1 map-only (module-config/policy additive).
- **t-3** ✅ (#82) — proposal approve/reject + **publish** (a new map version, author preserved) + the `withAdminAuth` API + the `autoApprove` reader (ships `none`). Race-hardened (optimistic claim + in-transaction base-version re-check).
- **~~t-4~~ / ~~t-5~~** — eval wiring + judge seeds → **split to `f-eval` (20)** (governance observability, separable from the gate).

### 19 · `f-overlays` — similarity + proactive guidance

_Owner:_ John · _Status:_ **shipped** (t-1 #93 · t-2 #95 · t-3a #96 · t-3b #98) · _Depends on:_ f-guidance ✅ · _~3 PRs (4 shipped — t-3 split)_ · **detailed plan: [[f-overlays]]**

Advisory overlays; authored edges alone drive eligibility (F9). The deferred F9/F13 half of `f-guidance` (12) — that feature shipped a labelled-empty `related` slot and deferred pgvector "related" + proactive guidance here. Promoted-task table lives in [[f-overlays]].

- **t-1** ✅ (#93) — `framework_node_embedding` (`vector(1536)` + hand-appended HNSW; per published map version, `--create-only`) + the node-text composer + on-demand admin re-embed trigger; the framework's **first drift probe**, wired via a new **third core→framework bridge** (`lib/app/db-drift.ts` → `lib/framework/db-drift.ts` → reserved-empty leaf).
- **t-2** ✅ (#95) — Guidance consults similarity to fill the `related` slot with "related places", clearly labelled **advisory** (never feeds eligibility — F9; a byte-identical-availability test is the load-bearing guard).
- **t-3a** ✅ (#96) — the sweep's compute half: a system-scoped stalled-active-journeys enumerator + `runProactiveGuidanceSweep()` (per journey under a self-viewer) + an admin **preview** trigger.
- **t-3b** ✅ (#98) — delivery: the `framework_journey_nudge` throttle table (GDPR cascade) + one-email-per-user nudge (a dedicated `proactive-nudge.tsx` template) + `registerStepType('framework_proactive_guidance')` so an operator can schedule it; no workflow/schedule row seeded (F13).

### 20 · `f-eval` — evaluation wiring over framework conversations

_Owner:_ John · _Status:_ **shipped** (t-1 #88 · t-2 #90) · _Depends on:_ f-facilitation-agents, f-guidance · _~2 PRs_ · **detailed plan: [[f-eval]]**

Governance's post-hoc face (spec §5.5, F14 — reuse over reinvention): runs the existing named-metric scoring (t-1) + the post-hoc supervisor (t-2) over the framework's own (`facilitation`/`module`) conversations. **Split out of `f-emergence` (18)** at its close-out — governance _observability_, separable from the emergence _gate_. The scoring/supervision machinery is entirely Sunrise-core; f-eval is the thin **conversation-native adapter** (read turns from `AiMessage`, run the core scorer/supervisor, persist to `FrameworkConversationEval`). No `f-eval` dependents, so nothing new unblocks.

Promoted-task table lives in [[f-eval]]. Key reconciliation (2026-07-07): the planned "judge seeds / `#303` scaffold" task is **dropped** — `scoreResponse` reuses the existing core eval judges (016 seeds, `isSystem:true`) by slug, so no new judge is needed; and judges are platform infrastructure (`isSystem:true`), not the `isSystem:false` app-agent scaffold. Sized to **2 PRs**.

- **t-1** — Metric-scoring adapter + store + admin API: read framework conversation turns from `AiMessage` (pair user→assistant, citations from `provenance`; reuse the `capture.ts` pairing pattern), score via `scoreResponse` (existing judges), persist to a **new** `FrameworkConversationEval` table (framework-owned; `messageId` hand-FK → `ai_message` CASCADE, so erasable via the message); `withAdminAuth` on-demand trigger + read endpoints.
- **t-2** — Post-hoc supervisor (optional): a conversation→`stepOutputs` adapter + a framework rubric + the engine-free `llmCall` shim (copied from the executions review route) → `runSupervisorAssessment`; store the verdict on the eval row.

---

## Parked phases (future epics)

Per the Hub's `parked` Phase status — carried so they're not lost, kept out of the active view:

- **Relationship & cohort overlay** (spec §8) — cohorts, membership, `JourneyLink` with consent (legal-reviewed, §9.3). Enabling seams (`contextKey`, `canRead`, subject-scope) ship inside `f-journey-state` / `f-ops-views`, so this epic is additive when an app triggers it.
- **Adjacent components** (spec Appendix C) — commercial layer, identity/consent, comms channels, media/voice, trust & safety ops, product analytics. Each a sibling component with a narrow interface to the framework; none scheduled until an app needs it. Two interfaces are pre-wired: entitlements (module liveness) and timezone-resolved `now`.

---

## How features and tasks work

> This section is the **structure** (levels, statuses, sizing). For the **execution rhythm** — how to
> take a feature from claim to shipped (plan-first → per-task gate loop → close-out) — see
> [[building-a-feature]]. New contributors (and their agents): read that first.

### Status vocabulary

- **Features:** `not started | in flight | blocked | shipped`. Owner + `depends on`; `blocked` lists what's blocking.
- **Tasks (when promoted):** `backlog | available | claimed | done`. **No `in-pr` state** — a promoted task flips straight to `done` when its PR merges. Tracking an "in-PR" step in a Markdown board wastes a second commit to flip it and, more often, gets forgotten (the status goes stale). One transition, nothing to forget.

### Task sizing — one PR, not one commit

A **task is one meaningful PR**: cohesive, reviewable, merge-in-one-sitting (often ~200–600 lines). Never a single commit — commits are below this plan's resolution. If a task reads like "add one field + test", it's a commit _inside_ a PR; fold it in. Occasionally a task is honestly larger than one PR — say so explicitly rather than pretending it's small.

### Feature sizing — the unit of ownership

A **feature is owned by one person** and is a coherent capability of ~2–5 tasks, with dependencies. It's the atom you claim, prioritise and advance — matching the grain the Hub's own project page uses. The `~PRs` figure on each feature is indicative sizing, there so the size reads at a glance.

### Indicative vs promoted tasks

The `t` bullets under each feature are **indicative** — a planning aid sketching PR-sized scope, _not commitments_. A task becomes **promoted** when the owner declares it with a stable id (`t-N`), files-likely-to-touch, deps, and status — the deliberate "this is real work now" gesture. Indicative tasks reshape on promotion; that's the point.

Promoted-task format under a feature in flight (mirrors the Hub board):

```
| ID  | Task                                   | Files                                   | Deps | Status    | PR |
|-----|----------------------------------------|-----------------------------------------|------|-----------|-----|
| t-1 | GraphStore interface + Postgres impl   | lib/framework/facilitation/graph-store/ | —    | available | —   |
| t-2 | Availability computation + reasons     | lib/framework/facilitation/engine/      | t-1  | backlog   | —   |
```

### Asking Claude to plan a feature

> "Let's plan **f-engine**. Read this plan and §5.3 + the F-decisions in [[framework-architecture]], then propose the approach."

Claude reads this doc for intent and the spec for the binding _how_, then produces an implementation plan for review.

---

## Decisions log

Append-only. Newest at the top.

- **2026-08-14 — v1.3 **Phase 0** (25) **shipped** (#201 — one PR, three tasks) — the leaf is unblocked, the ledger tells the truth, and the fork's ancestry is repaired.** **t-0.1** (Sunrise #160) backed the module and framework-capability registries — and the map publish-listener registry found alongside them — with `globalThis` instead of module-level `Map`s. Under Next 16 + Turbopack the instrumentation graph is a **different module realm** from the route/RSC graph, so a registry populated at boot read back **empty on every request**: the entire generic module-config surface was dead for any leaf module, the one genuinely leaf-blocking item in the epic. **t-0.2** reconciled [[upstream-asks]], which still read "filed — awaiting the seam" for six seams that had landed in Sunrise **0.7.0** (#398 · #403 · #410 · #411 · #415 · #416) — the 0.8.0 pass had covered #412/#413/#414 and stopped — and added a "Running the check" recipe so the ledger stops going stale between syncs. It also surfaced a rendering bug: #416 and #533 shared one physical table line, so **#533 rendered nowhere at all**. **t-0.3** (Sunrise #539) repaired the merge base that the **squash-merged** v0.8.1 sync (#196) had stranded at v0.8.0 — recorded with an `-s ours` commit — and added a `framework:sync-ancestry` guard to the fork-owned `app:ci-checks` seam so the next squashed sync fails the build instead of silently costing the fork its upstream history. Reviewed twice into a follow-up: the guard's exit-code classification was extracted to be testable, and it was fixed to work on a **bare CI checkout** (a shallow clone has no tags to reason about). The PR was merged with a **merge commit, not a squash** — squashing it would have discarded exactly the parent it exists to restore, and the new guard would have caught that on `main`. Detail: [[issue-backlog]] Phase 0.

- **2026-07-08 — `f-map-editor` (14) **shipped** (t-1 #110, t-2 #111 / t-2b #112, t-3 #115, t-4 #120, t-5 #121) — the map editor + journey dry-run simulator; feature complete, and the LAST feature of the Framework v1 epic → the epic is now fully shipped (all 20 features).** The authoring UI over `f-map`'s already-shipped backend + `f-engine`'s pure availability, exactly as the recon predicted: **pure framework-tier, no migration, no core edit**, one new endpoint (the dry-run). **5 tasks, split once at build (t-2/t-2b):** **t-1** (#110) the editable `@xyflow` canvas forked from the workflow-builder shell — `maps` list + `[slug]` editor + "Maps" nav item, map↔ReactFlow mappers with `meta._layout` persistence + `layoutJourney` auto-layout, palette + node CRUD, draft save/discard. **t-2** (#111) the 4 typed edges + edge inspector; **t-2b** (#112) region containers via `@xyflow` v12 parent/child (the net-new canvas integration — parent/child worked, no fallback needed). **t-3** (#115) the node config inspector + the descriptor-driven 3-family (`state`/`slot`/`temporal`) condition builder + a live client preflight ringing nodes the pure `validateMapFormat`/`validateGraphInvariants` flag; `/code-review` caught a missing-`FieldHelp` gap. **t-4** (#120) version controls (publish + `changeSummary`, history, rollback) ported from the workflow-builder's publish/history/revert; `/code-review` caught 3 real client-state bugs — the biggest: rollback doesn't clear the server draft, so optimistic `hasDraft=false` resurrected a stale draft on reload (fixed by re-reading fresh server state, the workflow-builder's revert pattern). **t-5** (#121) the dry-run (F18) — a pure `lib/framework/facilitation/dry-run.ts` adapter + `POST /maps/[slug]/dry-run` (`withAdminAuth`, a DB-throwing test proxy proving zero DB) + the simulator panel; `/code-review` caught a stale-result-on-reopen bug (the same class as t-4's) + a confidence-`NaN` 400. Every task through the full gate loop; `/code-review` earned its keep on t-3/t-4/t-5. Lessons: [[planning-retro#B27|B27]] (recon classified each named surface shipped-vs-missing, so the spine was reuse, not plumbing — held) + a new §B entry on the recurring lifted-dialog stale-state bug. Detail: [[f-map-editor]].
- **2026-07-08 — `f-map-editor` (14) claimed (John) + planned (build-ready); the last feature claimed.** Detailed plan: [[f-map-editor]]. The map editor + journey dry-run simulator (spec §5.6, F18). A three-sweep reconnaissance (the shipped map backend + schema; engine/guidance purity for the dry-run; the workflow-builder + journey-explorer UI reuse) settled the reconciliations: **(1) the map backend is 100% shipped** — `f-map`'s `saveDraft`/`PATCH /maps/[slug]` already accepts a whole edited `definition` (or `null` to discard), and `GET`/`publish`/`rollback`/`versions` + the **pure, client-callable** `validateMapFormat`/`validateGraphInvariants` all exist; **the only new backend is the dry-run endpoint**, everything else is UI (the 03/06/07 read-API-here / UI-there split again). **(2)** the editor **forks the core workflow-builder's _editable_ shell** (state ownership + `onConnect` + palette drag + save/publish/discard/rollback handlers that map 1:1 onto the shipped map verbs), **not** the read-only journey canvas — a pattern-fork into a framework-tier `map-builder/` tree, swapping the workflow leaves for the map vocabulary (4 node types / 4 edge types / 3 condition families); the DRY alternative (a shared generic canvas) is a Sunrise-core refactor, ledgered as a fork-first upstream-ask, not gated on. **(3)** maps store **no x/y** — the editor persists canvas layout in a reserved **`meta._layout`** (schema-compatible `z.record`, **no migration**), and reuses the journey-explorer's `layoutJourney` (Kahn) for auto-layout + seeding. **(4) the dry-run (F18) is genuinely cheap** — the engine was built for it (`availability.ts`'s header names F18 as why it stays pure); `computeAvailability` + `rankMoves` are pure over an in-memory `AvailabilityInput`, so ship `POST /maps/[slug]/dry-run` (`withAdminAuth`) that builds `inMemoryGraphStore(definition)` from the **body** definition (simulate unsaved edits) + synthetic `{ completions, slots, now }`, runs the two pure functions, returns per-node verdicts (+ every `lockReason`) + ranked moves; **never** `applyEvent`/`loadGuidance`, **zero DB**. **(5) region collapse/expand is the one net-new canvas integration** — `@xyflow` v12 parent/child (nothing in the repo uses it yet); fallback to a non-interactive grouping if fiddly. Expected **pure framework-tier / no migration / no core edit** (confirm B17). Sizing: **5 PRs** (t-1 canvas + draft load/save = anchor · t-2 typed edges + regions = highest-risk-new · t-3 node config + condition builder + validation surfacing = most intricate · t-4 version controls = ports cheaply · t-5 dry-run = thin). Open for the owner: editor base (default fork the shell + file the upstream observation), layout persistence (default `meta._layout`), dry-run subject (default body-carried), edge-draw UX (default draw-then-inspect), regions (default parent/child), live-key-removal warning (default best-effort). **This claims the last of the 20 features — the board is now fully claimed (18 shipped, `f-atlas` + `f-map-editor` in flight).** Detail: [[f-map-editor]]. Lesson applied: [[planning-retro#B27|B27]] (a UI-over-shipped-backend feature's spine is the reuse, so recon classifies each named surface as shipped-vs-missing first).
- **2026-07-07 — `f-engagement` (08) claimed (John) + planned (build-ready).** Detailed plan: [[f-engagement]]. The module event stream + stats + feedback (spec §4.3, A9 — _derive stats from an insert-only stream, never counters_) — instrumenting the shipped module spine. A three-sweep reconnaissance (the `JourneyEvent` stream + erasure; the `runModuleWorkflowBindings` receiver + the hook system; the capability seam + feedback precedent + the f-ops-views UI seams) settled the reconciliations: **(1)** the event stream **already exists and already carries `moduleSlug`** — `applyEvent` stamps `moduleSlug: node.moduleSlug ?? null` on `node_entered`/`node_completed` ([`apply-event.ts:181`](../../lib/framework/facilitation/engine/apply-event.ts)), so **module progression/completion is already in the stream**; f-engagement only adds new `type` values (`module.entered`/`module.feedback` — not migrations, X1) + the read side, and erasure is a pure `userId` cascade (already smoke-tested for a null-`journeyId` engagement event). **(2)** `JourneyEvent.userId` is NOT NULL, so operator lifecycle (status changes — no subject user) is **not** an engagement event and stays out of the erasable per-user stream; v1 emits the two events that have a real `(userId, moduleSlug)` at runtime — **`module.entered`** from the module surface chat route on a _fresh_ conversation ([`chat/stream/route.ts`](../../app/api/v1/framework/modules/[slug]/chat/stream/route.ts)) and **`module.feedback`** from `record_feedback`. **(3)** the emit seam `recordModuleEngagement(...)` has two isolated best-effort limbs — insert a `JourneyEvent` **and** fire-and-forget `runModuleWorkflowBindings` (the receiver f-module-bindings shipped **with no production caller**; dispatches via `drainEngine`) — so this feature is what **finally makes module→workflow bindings fire**; **not** via `emitHookEvent` (verified outbound-webhook-only, no `module.*` in its enum → would force a core edit). **(4)** no core feedback table exists (`AiMessage.rating` is the wrong grain) — feedback lives on the stream as `module.feedback`; `record_feedback` mirrors `fill-slot.ts` (`processesPii` + `redactProvenance`), registered via a new `engagementCapabilities` array in `initFramework()`, plus a plain `withAuth` `POST …/feedback` endpoint. **(5)** stats mirror the shipped `groupBy`+`_count` precedent ([`journey/admin-queries.ts`](../../lib/framework/facilitation/journey/admin-queries.ts)) over `JourneyEvent` (the **first cross-user aggregate** over the table — all existing reads are single-subject), **subject-scope-shaped** for the #367 analytics axis, surfaced as a Stats tab appended to the module-detail `tabs` array (the real in-file host seam). **(6)** expected **pure framework-tier / no upstream issue** — except t-3's `moduleSlug`-filtered stats query may want a `framework_*`-scoped `(moduleSlug, occurredAt)` index (B13), confirm at build (B17). Sizing: **3 PRs** (t-1 emit seam + binding fire = anchor · t-2 feedback cap + API · t-3 stats + Stats tab, the largest — may split 3a/3b at build per B25). Open for the owner: v1 emit sites (default `module.entered` + `module.feedback`; `module.completed` deferred pending completion semantics), rating scale (default 1–5), node-events-fire-bindings (default no — keep the engine pure), collective journey-heat overlay (default deferred — the overlay prop doesn't exist yet), stats subject-scope (default admin cross-user, seam shaped). Detail: [[f-engagement]].
- **2026-07-07 — `f-ops-views` (15) **shipped** (t-1 #66, t-2 #69, t-3 #83, t-4a #84, t-4b #86, t-4c #89, t-5a #94, t-5b #97) — the module admin + structural journey explorer; feature complete.** The UI half three shipped features deferred here **API-first** (03 module page, 06 config form + version history, 07 binding management), plus the new module-lifecycle-write + journey-read endpoints and the framework's **first admin UI** (so it also stood up the sidebar **nav seam** + the `app/admin/framework/` route tree). **Scoped to its 08-independent core** (the settled "08 question"): operational admin + a **structural** explorer (read-only map + _individual_ replay over 09), with 08's analytics surfaces (module stats, collective heat/drop-off) deliberately left to mount later on the extension points this feature leaves — a **tab-array slot** on the module detail + a **canvas overlay prop** on the explorer (**host-first, overlay-plugs-in**). **5 planned tasks → 8 PRs, split twice at build on the same UI-over-shipped-API vs builds-one-new-endpoint seam:** **t-1** (#66) scaffold + **nav seam** — Daybreak fills `lib/app/admin-nav.ts` (the **2nd `lib/app/*` bridge** it fills, after `bootstrap.ts`; a static import since nav is sync) delegating to a reserved-empty `leaf-admin-nav.ts`; code-review caught the ownership docs still calling `admin-nav.ts` leaf-reserved → **reclassified all three docs in the same PR** (a bridge-fill must update the ownership docs). **t-2** (#69) tabbed detail **host** + generic config form (over 06's Zod→descriptor walker) + version history. **t-3** (#83) the security-sensitive **lifecycle writes** (`PATCH`/`DELETE /modules/[slug]`) isolated as its own task — `/code-review` corrected the DELETE from the planned blunt `invalidateAllAgentAccess()` to **per-agent eviction** (enumerate bound agents before the cascade), plus a datetime-minute-truncation false-audit bug and a tab-remount key bug. **t-4a/b/c** (#84/#86/#89) the binding tabs (agents/workflows/knowledge over 07), **split one-per-binding-kind** ([[planning-retro#B22|B22]] — each kind's distinct machinery, not reflexively) and **extracting the shared binding-tab primitives** (`useBindingRoster`/`useRowActions`/`RowConfirm`) at the rule-of-three in t-4c. **t-5a** (#94) the **journey read API** (gated `getJourneyById`/`listJourneys` + an `admin-queries` enrichment composing the `canRead`-guarded primitives; explicit `adminSupportViewer` for cross-user support reads). **t-5b** (#97) the **explorer UI** — the plan-time "reuse the workflow canvas" was light-on-library/heavy-on-feature (its node/edge types render step config; no layout lib), so a **Kahn longest-path** journey mapper + own read-only `@xyflow` node/canvas + a Live/Replay scrubber, on the raw primitives only. **Pure framework-tier throughout** — no upstream Sunrise issue, no migration (confirmed per [[planning-retro#B17|B17]] at each task). A **latent boundary gap** surfaced at t-5b: `isCoreSource` in `scripts/boundary/check.ts` (the vocab scan) hadn't been given `components/admin/framework/` when t-1 added it to the ESLint framework globs — the two allowlists are one tier-boundary in two representations; the journey components were the first framework components to use a flagged facilitation token → CI failed, fixed in-PR (2b2df0cb) + a sync note. Every build task through the full gate loop. Lessons: [[planning-retro#B22|B22]], [[planning-retro#B17|B17]], + the t-4/t-5 build-split and boundary-allowlist-sync entries (§B). Detail: [[f-ops-views]].
- **2026-07-07 — `f-overlays` (19) claimed (John) + planned (build-ready).** Detailed plan: [[f-overlays]]. The deferred **F9/F13** half of [[f-guidance]] (12) — advisory similarity overlays + proactive guidance, both strictly _downstream of eligibility_. A reconnaissance sweep (embedding/pgvector infra, the guidance service + map-node shape, the scheduler/hooks/notification paths) settled the reconciliations: **(1)** the `RankedMove.related` slot was **pre-stubbed by f-guidance for #19** (empty, commented) — fill it downstream of `rankMoves`; **F9 is enforced in three files** (`availability.ts`/`graph-store.ts`/`ranking.ts` all exclude similarity + `related_to` from eligibility), so all similarity stays downstream of `computeAvailability` (a byte-identical-availability test is the load-bearing guard). **(2)** map nodes are **JSON in `FacilitationGraphVersion.definition`, not rows**, so the embedding table keys on `(graphSlug, nodeKey, version)` (version-keyed ⇒ republish invalidates naturally, stale ⇒ empty `related`, safe) and the embed text is **composed** (`key` + module `name`/`description` + `meta`) since `MapNode` has no description field. **(3)** reuse core `embedText`/`embedBatch` (1536-dim, self-log cost) + the `AiMessageEmbedding` pgvector/HNSW template (hand-append `CREATE … USING hnsw`; **first framework migration to add its own HNSW index**, extending B13 from strip-only to strip+append); carry provenance + a dimension drift-guard. **(4)** the framework's **first drift probe** needs a **new third bridge** — `lib/app/db-drift.ts` → `lib/framework/db-drift.ts` + reserved-empty `lib/app/leaf-db-drift.ts` — mirroring the `bootstrap.ts`/`admin-nav.ts` bridges (keeps the leaf surface collision-free). **(5)** embedding trigger is **on-demand** (admin re-embed; cost-controlled, publish-decoupled — mirrors f-eval), auto-on-publish deferred. **(6)** proactive guidance rides an **operator-scheduled AiWorkflow via a framework-registered custom step type** (`registerStepType('framework_proactive_guidance')`, the `send_notification` seam) — there is **no framework periodic seam** (`run-tick.ts` is Sunrise-owned, hard-coded); ship the step type + a reusable sweep service + on-demand trigger, **seed no workflow/schedule row** (fork-clean). **(7)** a **new system-scoped active-journeys enumerator** (none exists; "active" derived from `UserNodeState`), running guidance per journey under a **self-viewer** (`canRead` passes, no new admin viewer). **(8)** nudge = **email to the journey owner** (no in-app store) via an extended `event-notification.tsx`, **deterministic copy** (guidance is LLM-free ⇒ cost-free), with a `framework_journey_nudge` **throttle table** (`userId` FK `ON DELETE CASCADE`, GDPR) to dedup re-nudging. Sizing: **3 PRs** (t-1 store + drift-bridge anchor · t-2 similarity→`related` · t-3 proactive sweep+nudge, the largest — may split 3a/3b at build per B1). Open for the owner: auto-on-publish embedding (default off), nudge channel (default email), nudge copy (default deterministic), and the nudge-worthy signal (default: stalled journey with an eligible move). Detail: [[f-overlays]].
- **2026-07-07 — `f-eval` (20) claimed (John) + planned; the judge-seed / `#303`-scaffold task dropped after recon.** Detailed plan: [[f-eval]]. Governance's post-hoc face (F14): run the existing named-metric scoring (+ optional supervisor) over the framework's own `facilitation`/`module` conversations — the machinery is all Sunrise-core, so this feature is a thin **conversation-native adapter**. A reconnaissance sweep settled the reconciliations: **(1) metric scoring REUSES the existing core eval judges** — `scoreResponse` resolves `eval-judge-faithfulness`/`-groundedness`/`-relevance` (core `016` seeds, `isSystem:true`) by slug, so no new judge is needed, and **the planned "judge seeds via the `#303 isSystem:false` scaffold" task is dropped** (judges are platform infrastructure = `isSystem:true`; `#303` is for a leaf's user-facing agents, and `isSystem:false` would miscount the setup gate + leak into user backups — a [[planning-retro#B17|B17]]-style "the plan's premise was wrong, found at build" correction). **(2)** the adapter reads `AiMessage` (framework convos emit no `AiEvaluationLog`), pairing user→assistant + citations-from-`provenance` via the existing `capture.ts` pattern, filtered by `contextType ∈ {facilitation, module}`. **(3)** results land in a **new framework-owned `FrameworkConversationEval` table** (natural surface/context keys; `messageId` hand-FK → `ai_message` CASCADE, erasable via the message) rather than the `userId`-owned `AiEvaluationSession`/`Log`. **(4)** the post-hoc **supervisor is optional (t-2)** — its core is reusable but workflow-shaped, so it needs a conversation→`stepOutputs` adapter + a rubric + the engine-free `llmCall` shim (copied verbatim from the executions review route). Sizing: **2 PRs** (t-1 metric-scoring anchor, t-2 optional supervisor) — the planned t-5 seed task dropped, not deferred. Open for the owner: on-demand vs scheduled trigger (default on-demand), and whether t-2 ships in v1. Detail: [[f-eval]].
- **2026-07-07 — `f-emergence` (18) **shipped** (t-1 #78, t-2 #80, t-3 #82) — the F17 emergence gate + escalation; eval thread **split to `f-eval` (20)**.** The framework's self-configuration gate (spec §5.5, F17) — _emergence is real but the deterministic spine is never written raw_. **t-1** delivered f-policies' deferred escalation kind (F15) + its 2nd fork-carried core seam (the post-detection guard-event contributor). **t-2** shipped the `StructureChangeProposal` propose pipeline (validate → invariant-check → pending), map-subject-only (a `subjectType` discriminator leaves module-config/policy additive), with its own approval state (the workflow approval queue isn't reusable) and `createdBy="agent:<slug>"` authorship. **t-3** shipped approve/reject/**publish** — an approval publishes a new map version with the author preserved (F17) — the `withAdminAuth` API, and the `autoApprove:none` reader; race-hardened after `/code-review` (optimistic pending→approved claim with rollback-on-publish-failure so no stuck `approved` orphan; an in-transaction `expectedBaseVersion` re-check so a concurrent map move aborts rather than silently overwrites — residual simultaneous-commit race documented, shared with `publishDraft`/`rollback`). Every task through the full gate loop; `/code-review` earned its keep on t-3 (the two concurrency findings). **Scope decision at close-out: the eval-wiring thread (ex-t-4/t-5) was split into its own feature, [[f-eval]] (20)** — it's governance _observability_ (scoring/supervising conversations), separable from the emergence _gate_ this feature shipped, and the heaviest/most-gap-laden part; f-emergence delivered a coherent whole (F15 + F17) at 3 PRs, and the eval recon was carried intact into `f-eval` (now **available ▲**). Lesson: [[planning-retro#B23 · When a large feature grows a separable second concern, shed it into its own feature at close-out rather than carry a heavy tail|B23]]. Detail: [[f-emergence]] / [[f-eval]].
- **2026-07-07 — `f-emergence` (18) t-1 **shipped** (#78) — escalation kind (F15); closes out `f-policies`' deferred t-4.** The deferred escalation kind, picked up **first** per the owner's ask — and building it validated the deferral (it wasn't laziness): escalation needed its **own second fork-carried core seam**. **The seam** — a NEW core `lib/orchestration/chat/guard-events.ts`: a generic `registerGuardEventContributor` + `emitGuardEvent(ctx, { guard, outcome })` fired **fire-and-forget** after each inline guard flags (the **post-detection** sibling of t-3's pre-detection guard-floor seam; the guard sites previously emitted no event). Empty registry = inert no-op; contributors run detached with throws swallowed, so it can't delay or break a turn; emitted at the 3 guard sites **only when the guard is armed** (`mode !== 'none'` — a review fix, so a disabled guard never escalates). **The kind** — `escalation` (`{ scope: facilitation_role, signal: { guard, outcome }, priority }`; `signal.outcome` is the minimum severity to fire on) + the framework `handleFacilitationGuardEvent` contributor that matches role+guard+severity and composes the shipped `notifyEscalation` + audit log — turning a silent guard block into a defined, auditable pathway. **v1 = notify + log**; workflow-rerouting (`drainEngine`) + user-facing resources are a documented follow-up. Both `/security-review` + `/code-review` came back clean (fire-and-forget inert, no unhandled rejection, no message-content/PII leak, correctly scoped, empty-registry byte-identical); the one code-review semantic flag (should a disabled guard escalate?) was fixed inline. Second fork-carried core seam ledgered [[upstream-asks]] (sibling to the guard-floor seam). **This is the first time a deferred item from a shipped feature was carried into another feature's plan and delivered** — `f-policies` t-4 now flips to **done (#78)**; the escalation code lives in f-policies' `kinds.ts`/`escalation.ts`, only the build effort moved to f-emergence. Detail: [[f-emergence]].
- **2026-07-07 — `f-emergence` (18) claimed (John) + planned; picks up the deferred escalation kind (F15) as t-1; v1 proposals scoped to the map subject.** Detailed plan: [[f-emergence]]. Three reconnaissance sweeps (version-publish / approval / eval / escalation machinery) surfaced real depth in all three threads, so the plan settled several reconciliations. **(1) v1 proposals target the MAP subject only** — the three proposal subjects are heterogeneous (map has draft/publish/rollback versioning + a standalone `validateGraphInvariants`; module-config is a different snapshot shape with no invariant analog; policy is plain CRUD with no version target), and the spec's driving concern is that the deterministic spine — the map — is never written raw; a `subjectType` discriminator leaves module-config/policy as additive later scope (B1, don't build three pipelines at once). **(2) f-emergence owns its proposal approval state** — the existing approval queue is hard-bound to `AiWorkflowExecution` (`paused_for_approval` + an `executionTrace` entry; not an execution-agnostic approvable), so a `StructureChangeProposal` carries its own `status` + approve/reject actions (mirroring `approval-actions.ts` minus the workflow machinery), not a synthetic workflow-execution shim. **(3) `createdBy="agent:<slug>"` needs no schema change** — all three `createdBy` columns are plain `String?` (no `User` relation, X6); `FacilitationGraphVersion.createdBy`'s comment already names the convention. **(4) Escalation (F15) needs a SECOND fork-carried core seam** — the t-3 guard-floor seam runs _before_ detection and returns a mode; the guard sites emit no event on a flag, so F15 needs a new **post-detection** `registerGuardEventContributor` + `emitGuardEvent` at the 3 guard sites (homed in `guard-floor.ts`, generic/vocab-free/inert-when-empty, ledgered) — `emitHookEvent` can't reach the internal `notifyEscalation`/`drainEngine` machinery (webhook-only). Escalation is **sequenced first (t-1)** per the owner's ask, delivering f-policies' deferred t-4. **(5) Eval wiring reads `AiMessage`** (framework conversations emit no `AiEvaluationLog`) and **builds the `#303 isSystem:false` seed scaffold**, which does not exist yet — f-emergence's judge seeds would be the first framework agent seeds (f-facilitation-agents shipped seed-free). **Sizing: ~5 PRs — a large feature, honestly** (the board's ~4 undercounts): t-1 escalation, t-2/t-3 the proposal core, t-4/t-5 the eval thread; t-1/t-2/t-4 parallelisable. Open for the owner: whether the eval thread (t-4/t-5) stays in f-emergence or splits to its own small feature (default: keep, sequenced last). This is the first feature to **pick up a deferred item from a shipped feature** — f-policies.md t-4 flips to done when t-1 here merges.
- **2026-07-07 — `f-policies` (17) **shipped** (t-1 #73, t-2 #74, t-3 #75 merged); escalation kind (t-4, F15) DEFERRED.** The typed governance-policy kinds under one `FacilitationPolicy` table (spec §5.5, F14) — governance as _reuse, not reinvent_: the only new machinery is the policy **data**; guards, supervisor/evals, the approval queue, and the audit log are all Sunrise-core, untouched. **t-1 (spine + auto-approval)** — one table, a free-string `kind` + a raw-SQL CHECK (§546 closed-vocab; extended per kind), a per-kind **`z.discriminatedUnion('kind')`** payload (the same-tier `conditionSchema` precedent), `createdBy` hand-FK; validate-on-write CRUD + `withAdminAuth` API + audit. The **auto-approval** knob (`autoApprove: none`) ships stored-only — no consumer yet (no `StructureChangeProposal`); `f-emergence` (18) reads it later. **t-2 (relevance/maturity gating, F14)** — a **graph-scoped** kind (`{stage|region} → allowed roles`) enforced at `resolveFacilitationSurface`: `deriveCurrentStageRegion` reads the user's `active`/reached node states, and a gated role yields no surface (→ 404). **Fail-open** and monotonic-narrowing (a policy only ever restricts; auth + the visibility gate stay the real ACL). **t-3 (guard-minimums, F16)** — the one task touching Sunrise core: a **fork-carried core guard-floor seam** (`registerGuardFloorContributor` in `chat/guard-floor.ts` + 3 apply-sites in `streaming-handler.ts`; the #385/#403 shape — generic, vocab-free, **empty-registry = prior guard-mode resolution byte-for-byte**, a floor only ever RAISES a guard), consumed by a framework contributor that reads `guard_minimum` policies for the turn's role. Ledgered [[upstream-asks]] (delete-when-it-lands). Every task through the full gate loop; **all six `/security-review` + `/code-review` passes came back clean** (no findings) — the biggest risk, the core guard edit, verified monotonic-up + behaviour-preserving + boundary-green. **Sizing — 4 promoted tasks folded to 3 shipped along the _build-cost_ seam** (not one-per-kind): the thin stored auto-approval knob folded into the t-1 spine, and the three enforced kinds each stood alone. **Escalation (t-4, F15) is DEFERRED** — a conscious carve-out, not a silent drop: declarative wiring over existing bridges that no current deployment needs, promotable later as a thin PR or folded into `f-emergence` (18). This completes the **facilitation critical-path spine** (`f-map → f-journey-state → f-engine → f-guidance → f-facilitation-agents → f-policies`). Detail: [[f-policies]]. Lesson: [[planning-retro#B22 · Size "typed kinds under one table" by each kind's enforcement machinery, not one-per-kind|B22]].
- **2026-07-06 — `f-policies` (17) planned (John); tasks promoted; guard-minimums confirmed NOT pure framework-tier.** Detailed plan: [[f-policies]]. Governance is _reuse, not reinvent_ (F14): three reconnaissance sweeps of the governance surfaces confirmed the spec's "machinery is entirely existing" — inline guards (`log_only | warn_and_continue | block`), the supervisor + named-metric evals, the approval queue (an `AiWorkflowExecution` `paused_for_approval` state, not a table), the audit log (`logAdminAction`, free-form `entityType`), and the shipped signal→workflow bridge (`drainEngine` / `runModuleWorkflowBindings`; hooks are outbound-webhook-only). The **one new piece** is `FacilitationPolicy`: one table, a free-form `String kind` + CHECK (never an enum, §546), a per-kind **`z.discriminatedUnion('kind')`** payload (the same-tier `conditionSchema` precedent), `createdBy` hand-FK. **Key finding — guard-minimums (F16) is the one task that touches Sunrise core** (the [[planning-retro#B17|B17]] "pure framework-tier is a build-time finding" pattern again): guard-mode resolution is hardcoded inline in core `chat/streaming-handler.ts` at three sites with no policy tier, so mandating `block` per scope needs a **fork-carried core seam** (`registerGuardFloorContributor`, generic + vocab-free, empty-registry = prior behaviour — the [[planning-retro#B19|B19]] / #385/#403 shape, ledgered [[upstream-asks]]). Sizing: **4 promoted PRs** re-cut along the _build-cost_ seam, not one-per-kind — the thin **auto-approval** knob (a stored value f-emergence reads; no consumer exists yet) folds into the **t-1** spine as the pattern-proving first kind, and the three enforced kinds each stand alone because their machinery is distinct: **t-2** relevance-gating (a gate at `resolveFacilitationSurface` + a new stage/region derivation, fail-open), **t-3** guard-minimums (the isolated core-seam task), **t-4** escalation (workflow/notify/audit wiring; "always logged" → audit log, not the f-engagement-owned `JourneyEvent`). Open questions were resolved inline per [[planning-retro#B20|B20]] — no separate refinement pass; two genuine product-scope forks (guard scope granularity, escalation response palette) recorded with defaults, flagged for the owner.
- **2026-07-06 — `f-facilitation-agents` (13) **shipped** (t-1 #68, t-2 #70 merged) + `f-policies` (17) claimed (John).** The second scope of the one binding mechanism (spec §5.4 — _one mechanism, two scopes_; the first is `f-module-bindings`' per-module scope): a **flat, deployment-wide** family of facilitation seats bound to `AiAgent` rows and reached through their own surface. **t-1 (binding)** — `FacilitationAgentBinding`, a FLAT `(agentId, role)` map (no facilitation entity to key on, unlike the module binding's `moduleId → Module`), carrying `ModuleAgentBinding`'s **hand-FK discipline** (`agentId` a plain scalar FK, no Prisma `@relation` → no reverse field on core `AiAgent`, `ON DELETE CASCADE` hand-written in the migration; reads batch-stitch the agent display fields) and enforcing **one agent per seat** via `@@unique([role])` (a role _is_ the seat — no `isPrimary`). Role validates against a **framework-owned `FACILITATION_ROLES` constant** (not a per-instance registry, since the roles are fixed framework vocabulary — mirroring `journey/vocabulary.ts`). Bind/update/unbind/list service (shared `mapPrismaWriteError` → clean 4xx on the `@@unique` race, `logAdminAction`) + `withAdminAuth` API; capabilities stay the ordinary `AiAgentCapability` grant (no new mechanism), no knowledge-scope cache to evict (facilitation feeds none). **t-2 (surface)** — `resolveFacilitationSurface(userId, role)` mirrors f-guidance's `resolveModuleSurface` but **keyed on role, with NO scope map**: the guidance caps a facilitation agent is granted are scope-agnostic (read `context.userId` + `args.graphSlug`, never `context.scope`), so surface-scoping here is purely _which agent answers_, not capability refusal — it carries the **`public`-visibility gate** (the same ACL fix f-guidance t-5 made: an `internal`/`invite_only` agent → no surface → 404) + the framework consumer chat route tagging `contextType='facilitation'`. **Shipped mechanism-only** — the seed-vs-mechanism question **resolved mechanism-only** (a fork brings its own facilitation agents via the #303 `isSystem:false` scaffold; Daybreak seeds no family a fork would re-persona), so the conditional **t-3 seed task was dropped** and the feature is an honest **2 PRs**, not padded to the board's indicative 3. The board's third indicative task (per-scope guard settings) was **deferred to `f-policies` (17)** — it needs that feature's `FacilitationPolicy` typing. Both tasks through the full gate loop; **both `/security-review` + `/code-review` came back clean (no findings) on both tasks** — the pattern was a faithful mirror of the reviewed-secure module surface, and the one code-review note (the scope-omission is safe only while facilitation seats stay bound to scope-agnostic caps) was recorded inline as a forward caveat, not a defect. Pure framework-tier, no upstream issue, no core edit (the consumer `framework` API segment f-guidance t-5 already registered in the boundary configs). **Was the last blocker on `f-atlas` (16), `f-policies` (17), `f-emergence` (18)** → 16 + 18 now **available ▲**; **17 claimed by John and in flight** (detailed plan: [[f-policies]]). Detail: [[f-facilitation-agents]]. Lesson: [[planning-retro#B21 · A family-of-agents feature is mechanism-only by default — ship the binding + surface + role→cap reference, not seeded personas|B21]].
- **2026-07-06 — `f-guidance` (12) **shipped** (t-1 #49, t-2 #51, t-3 #52, t-4 #57, t-5 #59, t-4b #61 all merged).** The advisory layer (spec §5.4, F12) — _the engine computes what is possible; guidance ranks what is wise; agents narrate_ — and the **first moment the experience is felt in a conversation**. **t-1** — the pure `guidance.ts` service: the `computeAvailability`/`applyEvent` **input assembler** the engine deliberately left to this feature (canRead-guarded), the recency-weighted **ranking** of the engine's already-eligible `validMoves` (reasons in the payload; a labelled-empty `related` slot for `f-overlays`; F12 never re-decides eligibility), and a deterministic **progress-synopsis** digest — no LLM (agents narrate). **t-2** — the read capability family (`get_journey_state` / `get_next_steps` / `get_progress_synopsis` / `suggest_focus`) via the framework capability seam; **t-3** — `request_transition`, the write cap over the engine's sole writer (a refusal is a structured "not yet", not an error). **t-4** — the `'module'` context contributor wired to real (user-agnostic) module context; **t-4b** — **per-user** slot injection, unblocked by a **fork-carried core-seam widening** of `buildContext` (generic `ContextRequest { userId? }` + a user-aware cache key — the #385/#403 pattern, ledgered [[upstream-asks]], empty-userId = prior behaviour, boundary CI green). **t-5** — the X5 framework-owned surface chat route that resolves a module's primary agent and **populates `scope.moduleSlug`**, completing f-module-bindings' `isInModuleScope` enforcement. Five design questions were **resolved inline (no Ultraplan)**: documented default ranking weights, deterministic synopsis, leave `isInModuleScope` allow-on-absent, one framework-owned surface route, no `dryRun`. Every task through the full gate loop; `/code-review` + `/security-review` caught real defects before merge on nearly every task — t-1 (ranking scored `related_to` edges), t-3 (`journeyStarted` doc), t-5 (**HIGH: agent-visibility bypass** — the surface exposed `internal` agents; gated on `public`; + per-agent `rateLimitRpm` ignored), t-4b (DB-error would blank the module block; + `special_category` auto-injection). Two scope reductions recorded: **pgvector "related" + proactive guidance (F13)** deferred to `f-overlays`/Phase 6, and **full journey-position-in-prompt** deferred (needs a module→graph resolve; agents get it via `get_journey_state`). **Was the last blocker on `f-facilitation-agents` (13) → now claimable, claimed by John; and `f-overlays` (19) → available ▲.** Lessons: [[planning-retro#B19 · The fork-carried core seam is the sanctioned escape hatch when no seam exists — mirror the #385/#403 shape, keep it generic, ledger it|B19]], [[planning-retro#B20 · Resolve a plan's open design questions inline, not via a separate refinement pass|B20]]. Detail: [[f-guidance]].
- **2026-07-06 — `f-ops-views` (15) claimed (Simon) + planned; scope settled to its 08-independent core, sized to 5 clean-seam tasks.** Detailed plan: [[f-ops-views]]. The operational admin surfaces — the UI half three shipped features deferred here API-first (03 module page, 06 config form + version history, 07 binding management). **Scope reconciliation (the 08 question, settled):** the board's indicative task 1 bundled `f-engagement` (08) deliverables — "stats from the event stream", "collective heat / drop-off overlays" — with no dependency edge; those are 08's stats-from-the-stream (A9), not shipped. So 15 is built to its **08-independent core** (operational admin + a **structural** journey explorer: read-only map + _individual_ replay, which `f-engine` already writes the events for), and 08's **analytics surfaces move to 08** where they mount additively onto 15's host surfaces later. **Principle — host-first, overlay-plugs-in:** a UI overlay depends on its host surface more than the host depends on the overlay's data source (the data plugs in late; an overlay is homeless without its host), so 15 (the host: module detail page + journey canvas) is built before 08's stats UI; 15 leaves the extension points (a tab-array slot + a canvas overlay prop), and 08's free-form `JourneyEvent.type`s (X1) flow into 15's timeline with zero rework — the same read-API-here / UI-there split 03/06/07 already made. **Sizing — 5 tasks along the cleanest seam** (UI-over-shipped-API vs builds-one-new-endpoint): t-1 scaffold + **nav seam** + module list; t-2 detail shell + config + versions (UI over 06); t-3 module **lifecycle writes** — `PATCH` + `DELETE /modules/[slug]` (the latter wired to `invalidateAllAgentAccess()`, the 07 hard-delete gap) — isolated as the one security-sensitive task; t-4 binding tabs (UI over 07); t-5 journey explorer — new read endpoints over 09's `canRead` queries + read-only `@xyflow` canvas + individual replay. **Nav seam resolved with no core change** (no upstream issue): Sunrise's `admin-sidebar.tsx` already calls `initAppNav()` from the empty, fork-owned `lib/app/admin-nav.ts` and its comment sanctions a fork populating the registry there — Daybreak fills it (the boot-seam #385 pattern applied to client nav; a static import since nav is sync, not the dynamic-import trick `bootstrap.ts` uses). **Expected pure framework-tier** — the two endpoint pairs 15 builds (module lifecycle writes, journey reads) are framework-tier; confirm per B17 at build. Corrects the board's stale "custom canvas, not React Flow" note for `f-map-editor` (14): Sunrise's workflow builder **is** `@xyflow/react` v12 (re-verified against `package.json` + `workflow-canvas.tsx`).
- **2026-07-06 — `f-module-config` (06) **shipped** (t-1 #56, t-2 #58 merged).** The operator-config half of the module spine (spec §4.1, A4 + A10) — closes the gap `f-module-core` left: `Module.config` + `ModuleDefinition.configSchema` existed, but nothing validated, versioned, or exposed a config edit. **t-1 (versioning spine)** — `ModuleVersion` snapshot table + the config-versioning service: a config write is validated against the module's own registered Zod schema (A4), stored on `Module.config`, and captured as a **point-in-time** `ModuleVersion` (the `AiAgentVersion` model, **not** the map/workflow draft-buffer — the live values _are_ `Module.config`, the newest version equals the live row, and "rollback" is a **restore** that snapshots forward). `createdBy` is the hand-FK-to-core pattern (plain scalar → `User`, `ON DELETE SET NULL`, no reverse field, X6); migration B13-stripped. **t-2 (the A4 engine + API)** — `describeConfigSchema`, a **Zod→field-descriptor walker** so the server serialises a module's schema to flat descriptors a client renders (the registry is server-only, so a browser can never hold the live Zod object). Built on **Zod 4's native `z.toJSONSchema()`** (not fragile `_def` introspection); bounded + total (string/number/boolean/enum, else a raw-`json` fallback, never throws). Plus the admin API: `GET/PUT /config`, `GET /versions`, `POST /versions/[version]/restore`, all `withAdminAuth`. **API-first — the client config _form_ + version-history tab defer to `f-ops-views` (15)** (the 03/07 read-API-here / UI-there precedent); the walker + `getModuleConfigForm` are the server engine those pages consume. **Pure framework-tier — no upstream Sunrise issue** (like f-module-core / f-map). Every task through the full gate loop; `/code-review` caught real defects on both (t-1: **dropped the lazy v1 "seed the pre-edit config" mechanism** — borrowed from `AiAgentVersion` but wrong for modules, whose pre-edit state is the empty `{}` boot default, so seeding fabricated an author and could produce an un-restorable v1; **+ a concurrent-save P2002→500** now mapped to a retryable error. t-2: **`z.number().int()` leaked Zod's ±MAX_SAFE_INTEGER sentinels** as form bounds; **`parseVersionParam` accepted a version above Postgres int4** → DB error/500, now a clean 400). **Unblocks `f-ops-views` (15)** — every one of its deps (f-module-config ✅, f-module-bindings ✅, f-journey-state ✅) is now shipped, so 15 is **available ▲**. Lessons: [[planning-retro#B18 · A precedent borrowed for its shape can carry a rationale that doesn't transfer — re-derive it from the new domain|B18]]. Detail: [[f-module-config]].
- **2026-07-06 — `f-module-bindings` (07) **shipped** (t-1 #33, t-2 #35, t-3 #50, t-4 #53 all merged).** The module spine's second half — a registered module becomes _functional_: it **binds** agents/capabilities/workflows/knowledge, never owns them (§4.2, A6/A7/A8). **t-1 (agent bindings)** — `ModuleAgentBinding` pivot binding an `AiAgent` into a named seat (`role` validated against `ModuleDefinition.agentRoles`), single-primary enforced by a partial unique index. **Established the hand-FK-to-core pattern that resolved the plan's back-relation open question with _no core edit_:** `agentId` is a plain scalar FK, `ON DELETE CASCADE` hand-written in the migration, no Prisma `@relation` → no reverse field on the Sunrise-owned `AiAgent` (the f-slots `SlotValue.userId` shape). t-3 reused it for `workflowId`/`createdBy`, t-4 for `documentId`/`tagId`. **t-2 (capabilities)** — module capabilities register into the **one** global dispatcher, namespaced + scope-aware. `/security-review` caught a feature-breaking dispatch bug: the handler key, `ai_capability.slug`, and `functionDefinition.name` **must be one identifier** (`module__tool`, `__` not `.` — OpenAI forbids `.`); reconciliation #2 corrected accordingly and the invariant is now unit-tested. Boot-reconcile of the `ai_capability` rows splits code-projected from operator-owned columns. **Filed Sunrise [#398](https://github.com/human-centric-engineering/sunrise/issues/398)** (a `register(cap, { slug, guard })` seam) — the namespacing wrapper's PII re-assertion is load-bearing until it lands (ledgered). **t-3 (workflow bindings)** — `ModuleWorkflowBinding` mirrors `AiWorkflowTrigger`; `runModuleWorkflowBindings(moduleSlug, eventType, payload)` dispatches each enabled binding's **published** workflow via `drainEngine` (reconciliation #4: the hook system is outbound-webhook-only, so not hooks). Added `createdBy` for run attribution; the event _source_ is coordinated with f-engagement (08), not depended-on. Also landed the **rule-of-three dedup** the t-1 review deferred: shared `route-params` + `prisma-errors` helpers, framework-tier (not core), with f-map + t-1 migrated. **t-4 (knowledge scope) — did NOT fold (B1 bet was wrong).** The premise "no new mechanism, thin, fold into t-1" was false: correct behaviour — a module owning a **durable knowledge scope** that its bound agents **inherit**, coexisting non-destructively with the operator's direct grants — is impossible to _materialise_ (the core `AiAgentKnowledgeDocument` pivot is `@@id([agentId, documentId])` with no provenance, so a module-grant and a direct grant of the same doc are the _same row_ → clobber-or-leak on unbind). So the scope is composed **live at resolve time** via a **generic core seam** `registerAgentAccessContributor` (built fork-first in `resolveAgentDocumentAccess`, minimal + generic, empty-registry = prior behaviour — the boot-seam #385 pattern), consumed by `resolveModuleKnowledgeForAgent`. **Filed Sunrise [#403](https://github.com/human-centric-engineering/sunrise/issues/403)** (ledgered); enforcement is live in the fork now. `knowledgeAccessMode` is never flipped (the agent owns its mode; the module contributes scope). **This makes the plan's "fourth pure framework-tier feature — no upstream issue" claim wrong: t-4 carries a generic core seam + one upstream issue** (t-1–t-3 were pure). Every task through the full gate loop; each `/security-review` + `/code-review` caught real defects fixed before merge (t-2 dispatch-key bug; t-3 P2003→500 + shared-helper parity; t-4 a **fail-to-revoke** window — bind/unbind must invalidate the resolver cache now that bindings feed knowledge — and a **sync-throw hole** in the seam's never-throws guard). Deferred to **f-ops-views (15)**: all binding/knowledge admin UI (API-first, the f-module-core precedent). Recorded invalidation-contract gaps for f-ops-views (module hard-delete must invalidate) + a retain-on-retire decision. **Unblocks `f-atlas` (16)** — now blocked only on f-facilitation-agents (13). Lessons: [[planning-retro#B17 · "Pure framework-tier / no upstream issue" is a build-time finding, not a plan-time fact — correct-behaviour-first can reveal a needed core seam|B17]].
- **2026-07-05 — `f-slot-capture` (10) **shipped** (t-1 #42, t-2 #43, t-3 #44, t-3b #45, t-4 #46 all merged).** The capture half of the slot subsystem — the two `BaseCapability` tools an agent invokes silently in the tool loop (D5) over the insert-only value engine `f-slots`. **t-1** — a reusable **framework capability-registration seam** (`registerFrameworkCapability` + an `ai_capability`-row sync wired into `syncFramework()`, marker-scoped so it can never hijack a Sunrise built-in or admin row), proven by `get_state` (the `canRead`-guarded read, X2; `processesPii`; silent). **t-2** — `fill_slot`, the write cap: targeted-slug validation / open-mode minting / P2002 retry over `appendSlotValue`; `redactProvenance` masks the value + a minted slug (model-authored free text that can encode PII). **t-3** — sensitivity masking-before-storage (`special_category` prose never lands at rest) + the local typed-value bridge (`SLOT_DATA_TYPE ↔ typed-value`; date restricted to ISO-8601 so the engine's lexicographic `gte`/`lte` stays chronological). **t-3b** — the #307-enforced prose→typed **extraction fallback** (a secondary `runStructuredCompletion` against the capturing agent's own provider/model, best-effort — never fails the write). **t-4** — per-agent read/write **exposure** via a grant's `customConfig` allowlist (groups/scopes; tri-state — absent = permissive, valid = enforced, malformed = fail-closed; strict facets so a typo can't silently widen access). Every task through the full `/pr-gates` suite; review caught + fixed real defects before merge on every task (t-1 sync-hijack asymmetry, t-2 minted-slug PII leak, t-3 minted-slug leak on write-failure + date lexicographic hazard, t-3b DB-lookup outside the best-effort try, t-4 fail-open facet typo). Pure framework-tier — no migration (`customConfig` already existed), no core edit (framework→core public imports only); two `upstream-asks` **ledgered** (identified, issue-to-file — relocate/widen `runStructuredCompletion`; surface binding `customConfig` into `CapabilityContext`). **Was the last blocker on `f-guidance` (12) → now available ▲**, the head of the remaining critical path. Lessons: [[planning-retro#B16 · A "masking + extraction" task splits cleanly at the LLM boundary — the pure map ships, the impure call defers|B16]].
- **2026-07-05 — `f-engine` (11) **shipped** (t-1 #34, t-2 #36, t-3 #37, t-4 #38 all merged).** The deterministic facilitation spine (spec §5.3, F11) — pure, LLM-free, the sole writer of journey state. **t-1** — the `GraphStore` interface + in-memory traversal over the parsed published map (in-memory, not recursive CTE — the F8 interface is the commitment, the impl a later swap; reachableFrom / neighbours / pathsBetween / findCycles / regions). **t-2** — `computeAvailability`, the pure explainable "what is possible now": the four F3 edge semantics + three F4 condition families (state/slot/temporal) intersected with module liveness (A5) + once-close (F6), returning per-node verdicts with every failing gate's reason (F12), `validMoves`, `firsts`; folded the board's temporal task in (B1); `resolveJourneyNow` reads `User.timezone` (§C7). **t-3** — `applyEvent`, the one validated write path: `enter` gated by availability, `complete` by an atomic conditional update (race-safe — code-review caught the double-increment), event + projection in one transaction (F10), `userId` on every event (erasure); proven by a real-DB smoke. **t-4** — publish-time graph invariants filling the seam `f-map` left in `validatePublishableMap` (prerequisite cycles + unreachable nodes, blocking; live-key-removal warning, non-blocking), standalone/callable for `f-emergence` (F17). Every task through the full `/pr-gates` suite; code-review caught real defects on t-1 (multigraph paths, NUL-separator cycle dedup), t-2 (the two access faces), t-3 (the writer race) — all fixed before merge. Pure framework-tier, no upstream issue, no migration (the tables shipped in f-journey-state). **Unblocks `f-map-editor` (14)** and clears the `f-engine` edge off `f-guidance` (12) + `f-emergence` (18). Lessons: [[planning-retro#B15 · The deterministic engine is where code-review pays for itself — budget for a review-fix commit per task|B15]].
- **2026-07-05 — `f-module-bindings` (07) claimed (Simon) + planned; five spec-vs-repo reconciliations settled.** Detailed plan: [[f-module-bindings]]. The module spine's second half — bind agents/capabilities/workflows/knowledge to a module, never own them (§4.2, A6/A7/A8). Three recon sweeps confirmed the spec's assumed precedents exist (`AiAgent.runtimePromptManaged`, the `AiAgentCapability` pivot with `customConfig`, `ModuleDefinition` explicitly reserving `agentRoles`/`capabilities`, `CapabilityContext.scope`) and surfaced five reconciliations: **(1) API-first — ship the binding _APIs_ here, defer the binding _UI_ to `f-ops-views` (15)**, whose task 1 already owns "bindings management views" (the f-module-core read-API-here / page-there precedent); this is the one reconciliation that reshapes the parent plan's indicative task 1. **(2)** Capability **slug** is namespaced `module-slug.tool` (registry key + audit), but the **LLM function name** can't contain `.` (OpenAI charset) → the name uses a provider-safe transform. **(3)** `scope.moduleSlug` is _populated_ downstream by f-guidance's X5 surface-scoped conversations (the only site that sets `ChatRequest.scope` today is the chat handler); f-module-bindings ships the namespaced registration + a scope-refusal helper (the consuming seam), proven by setting `context.scope` in tests — shape-the-seam-now, like f-map's `validatePublishableMap`. **(4)** `emitHookEvent` is outbound-webhook-**only** and cannot run a workflow (spec imprecision) — `ModuleWorkflowBinding` mirrors `AiWorkflowTrigger` and dispatches via `drainEngine`; the module-lifecycle event _source_ is f-engagement (08), coordinated not depended-on (the 08/09 `JourneyEvent` pattern). **(5)** `runtimePromptManaged` + framework agent _seeds_ are f-facilitation-agents (13) — this feature binds existing agents, seeds none. Sizing: 4 promoted PRs (t-1 agents = anchor/heaviest; t-2 capabilities+scope; t-3 workflows; t-4 knowledge = thin, "no new mechanism", may fold into t-1 per B1). Fourth pure framework-tier feature — no upstream issue; the one core touch is a possible minimal Prisma back-relation on `AiAgent`/`AiWorkflow` (confirm at build). **Unblocks `f-atlas` (16).**
- **2026-07-04 — `f-journey-state` **shipped** (t-1 #27, t-2 #28 merged).** The per-user runtime-state layer over the authored map + the framework's single read-access seam. **t-1** — three models (`UserJourney` / `UserNodeState` / insert-only `JourneyEvent`) on the satellite hand-FK convention, the node-state status vocabulary (free-string, X1), the `framework_add_journey_state` migration (two hand-FK `userId → "user"` cascades + Prisma `journeyId` relations), and the erasure smoke proving journey + node-state + both event kinds gone after `eraseUser()`. **t-2** — `canRead(viewer, subject, scope)` + `subjectScope` in `lib/framework/shared/access.ts` (async from day one, default-deny, open `scope` composing with #367/#366), the `canRead`-guarded journey read queries (`getJourney` / `getNodeStates` / `getJourneyTimeline`, gate-before-Prisma + in-query ownership guard), and the slot-read guarding path documented (shipped `getSlotHeads` untouched). No writer (that is `f-engine`, F11) — a fresh fork boots to empty journey tables. `/code-review` caught the two access faces diverging for admin-support viewers; fixed so `canRead` ⇔ `subjectScope` provably agree (parity test). Fork-first `canRead` contract recorded in [[upstream-asks]] against Sunrise #367/#366 (fork-note to file). **Unblocks `f-engine` (11).** Lesson: [[planning-retro#B14 · A fork-first seam that composes with an upstream issue needs a live ledger, not just plan prose|B14]].
- **2026-07-04 — `f-journey-state` claimed (John) + planned; `JourneyEvent` ownership + key-column + async-`canRead` resolved.** Detailed plan: [[f-journey-state]]. (1) **`JourneyEvent` is created by `f-journey-state` (09), not `f-engagement` (08)** — it is the §5.2 journey-spine model, and 09 is in flight now; 08 **extends its use** (module-lifecycle event types + stats), never its schema (`type` is free-string, X1, so new event kinds aren't migrations). Coordination note added to feature 08; no hard dependency edge. (2) **`JourneyEvent` is `userId`-keyed with a hand-written `ON DELETE CASCADE` FK + an _optional_ `journeyId`, resolving the spec's split sketch** — §4.3 keys it on `userId` (engagement events like `session.started`, module lifecycle — many with no journey), §5.2 keys it on `journeyId`; these are one stream but disagree on the key. The `userId` shape is the only one that holds §4.3's non-journey events _and_ keeps every row erasable: a `journeyId`-only table (or a nullable `journeyId` with no `userId`) leaves non-journey events with no FK path to the user, so they escape erasure — a GDPR hole. Matches §4.3 verbatim and §11's erasure list (which names `UserJourney`/`JourneyEvent`, not `UserNodeState`, as the hand-FK tables); costs one extra hand-FK line (the `SlotValue` pattern). Erasure topology: `UserJourney.userId` + `JourneyEvent.userId` are hand-FK cascades to `"user"`; `UserNodeState.journeyId` (and the optional `JourneyEvent.journeyId`) cascade via Prisma `@relation` to `UserJourney`. (3) **`canRead` / `subjectScope` are `async` from day one** — §8's `JourneyLink` grants need a DB lookup, so shaping the predicate async now avoids a later sync→async sweep of every caller (the churn X2 exists to prevent); `scope` stays an open structured value carrying #367's ownership + #366's tier inputs (#366/#367 verified not landed). (4) Sizing: the board's 3 indicative tasks fold to **2 promoted PRs** (models+erasure · access), per B1 — `canRead` and `subjectScope` are the same seam.
- **2026-07-02 — `f-module-core` planned; three forkability reconciliations vs the rev-16 spec.** Guiding principle: **ship nothing a fork has to delete** — a `git fork` of Daybreak boots clean (empty modules table, one empty leaf hook, zero example rows) while every layer is proven by integration tests against a real DB + real API. (1) **Demo module is tests-only** — a fixture registered through the real `registerModule()` + real `syncRegisteredModules()` in an integration test, not a permanent `demo` row every leaf inherits and strips. (2) **Admin visibility is a read API, not a page** — `GET /api/v1/admin/framework/modules` (API-first; spec §4.4 backend-only); the module list _page_ is `f-ops-views` (feature 15). (3) **Leaf registers modules from the single `initLeafApp()` hook**, not the spec's dedicated `lib/app/modules.ts` + `initAppModules()` scaffold — the forkable shape is _one leaf boot hook, many framework `registerX()` functions_, so the leaf fills exactly one file and `initApp()`'s shape stays frozen (`initFramework()` → `initLeafApp()` → `syncFramework()`) as the framework grows. Also: **first pure framework-tier feature** — everything lives in `lib/framework/modules/`, touches no Sunrise core seam, so it files **no upstream issue** (unlike f-seams/f-bootstrap). Detail: [[f-module-core]].
- **2026-07-02 — Framework boot hook: generic `initApp()` seam, built fork-first to inform upstream.** [[f-bootstrap]]'s last open question resolved. Daybreak's `initFramework()` is invoked at boot via a **generic** seam: Sunrise's `instrumentation.ts` calls `initApp()` from a reserved, empty-by-default `lib/app/bootstrap.ts`; Daybreak's _filled_ copy imports `@/lib/framework` and runs `initFramework()`, delegating to a fresh empty leaf hook. **Core never references `@/lib/framework`** — a static dynamic-import specifier resolves at _build_ time, so Sunrise/ConQuest (no such folder) would fail to build; the reference must be absent from core, living only in the fork-owned filled scaffold (the `lib/app/capabilities.ts` pattern, applied to boot). Built **fork-first as the final generic shape** (not an interim hack) so the eventual upstream PR is a clean extract; file the upstream Sunrise issue as/after `f-bootstrap` t-3, referencing the working impl. Couples t-2↔t-3: the boundary CI whitelists the boot file as the single sanctioned core→framework path.
- **2026-07-02 — Fork-first informs upstream (working model).** When Daybreak needs a generic capability Sunrise lacks, build it **correctly in the fork as its final generic shape**, prove it in situ, and use that to inform an upstream Sunrise PR — never an interim/throwaway. Upstream may refine for its own guardrails; propose from something real, adopt what lands. This is `building-on-sunrise` (fix-in-place → classify → promote upstream) stated as a working preference.
- **2026-07-02 — Framework schema naming: `framework_` tables + clean model names.** [[f-bootstrap]] reconciliation #3 resolved. Framework DDL uses the spec's `framework-*.prisma` files / `framework_` table prefix / `framework_`-named migrations (Appendix B), **not** Sunrise's generic leaf `app_` convention — in the three-tier model `app_` is the _leaf app's_ namespace, so it would tangle Daybreak's DDL with Lelanea's and break the boundary CI that keys on `framework_`. Prisma **model** names stay clean/unprefixed (`model Module { @@map("framework_module") }`) for client ergonomics; accepted low risk of a future Sunrise model-name collision, mitigated by a cheap framework-side rename.
- **2026-07-02 — Three-tier model: Daybreak reserves the leaf surface; its own docs move to `.context/framework/`.** Daybreak is a framework that apps fork (Sunrise → Daybreak → app), so it must apply Sunrise's fork discipline _one tier up_: own `lib/framework/` + **`.context/framework/`**, and keep the leaf surface — `lib/app/*`, `.context/app/`, `prisma/schema/app.prisma` — **empty and reserved** for its own forks (Lelanea et al.), never occupied. Daybreak registers into Sunrise's seams from within `lib/framework/` (via `initFramework()`), as Sunrise registers built-ins from core. Concretely: Daybreak's docs moved out of `.context/app/` into `.context/framework/` (this reverses the earlier f-bootstrap "reconciliation #2"); `.context/app/` is now empty. Full ownership table in [[README|.context/framework/README.md]]. Open follow-up: how `initFramework()` is invoked at boot without occupying a leaf `lib/app/*` file (see [[f-bootstrap]] Open questions; leaning toward a small generic upstream Sunrise boot-init seam).
- **2026-06-30 — Framework named _Daybreak_.** "Daybreak" is the proper name; "expert-led-apps framework" stays the descriptive category. Spec bumped to rev 16; folder `expert-led-apps/` retained as the category folder.
- **2026-06-30 — Three new Sunrise fork-readiness issues triaged (#366/#367/#368).** **#368** (`executeTransaction` tx options) lands upstream _before_ the fork — Daybreak hits the same bulk-write P2028, so it joins `f-seams` at the pre-fork bar. **#366/#367** (authz-scoping seams) do **not** gate the fork — proposal-stage, `lib/auth/`-heavy, and irrelevant to single-user Lelanea; they merge down as inert no-ops when landed. **#367** is wired into `f-journey-state` as a design-time constraint: build `canRead` to compose with the upstream _one-predicate-three-inputs_ resolver, not a private parallel check. See the Relationship-to-Sunrise section.
- **2026-06-24 — Restructured to a flat feature list under one epic.** Phases are _epics_ (coarse, non-gating), not a working layer — per the Hub project page and [[v1-requirements]]. The whole build is one epic, `Framework v1`. The spec's §10 six "phases" are a _build sequence_, re-expressed as feature dependencies, not as structure. Features use semantic slugs; tasks are PRs; commits sit below the model. Resolves the earlier sizing tangle (no phase-buckets to fill).
- **2026-06-23 — Task sizing: PR not commit; feature is the ownership unit.** Carried from the ConQuest overhead lesson; calibrated to the Hub's own ~2–5-tasks-per-feature grain.
- **2026-06-23 — P0 seams land in Sunrise core before forking.** `CapabilityContext.scope` + `buildContext()` contributor registry — verified absent, the framework's only two core touch-points, generic → upstream first (now `f-seams`).
- **2026-06-23 — Framework lives on a separate fork of Sunrise, not core.** See [[placement-decision-memo]] and [[framework-architecture]] rev 16.
- **2026-06-23 — Assume the nine open Sunrise issues are cleared before forking.** Four leveraged (#307/#304/#303/#305); rest are CI/ops hygiene. React Flow assumption in the spec corrected (Sunrise's builder is custom).

---

## Work completed to date

Append-only. Newest at the top.

- **2026-08-05 — `f-release` (24) **shipped** (#187 — one PR, three commits) and Daybreak cut its **first tagged release, `daybreak-v0.1.0`**, opening and closing the Framework v1.2 · distribution epic.** The framework had 23 shipped features and a real dependent leaf (`reclaim-your-week`, John) but published **no releases, no version constant, no changelog and no leaf-facing sync guide** — so a leaf could only merge `daybreak/main`, an unversioned moving target, and learned about leaf-contract changes only by reading the diff. Not hypothetical: the Sunrise v0.8.0 sync (#181) made `lib/app/data-export.ts` Daybreak-owned and added a reserved `lib/app/leaf-data-export.ts`, so a leaf filling the file Sunrise's own docs point it at would collide with Daybreak in a GDPR path. **t-1** the version contract — `DAYBREAK_VERSION` (forced to `lib/` root: the ESLint boundary bans core importing `@/lib/framework`, and the health route is core) + the `daybreak` field on `/api/health` + `VERSIONING.md` defining `0.x` semantics, the tight leaf-facing public surface, the **prefixed `daybreak-vX.Y.Z` tag convention** (the ten bare `vX.Y.Z` tags in a dev clone are **Sunrise's**; origin had none) and the release checklist. **t-2** `CHANGELOG.md` seeded with the `0.1.0` entry — headline **action-required**: `data-export.ts` changed hands — plus `building-on-daybreak.md`, the leaf's `CUSTOMIZATION.md`. **t-3** a changelog CI guard on the fork-owned `app:ci-checks` seam, gating a deliberately **narrower** set than the documented surface (a gate that cries wolf gets bypassed) and documented as a floor, not the ceiling. Two plan estimates were corrected at build and recorded rather than absorbed: the health field was **6 files, not 2 lines** (the response is typed AND Zod-mirrored AND pinned by three Sunrise test files), and the **three tasks shipped as one PR** — the owner caught that a strict _chain_ is not an argument for separate PRs. `/code-review` found two real defects in the new code (a test asserting a coincidence that would break when the two versions cross; three forward links). Release cut against the new checklist on a fully green `main`; verified the remote holds **exactly one tag** and none of Sunrise's leaked. Lesson: [[planning-retro#B33|B33]]. Detail: [[f-release]].

- **2026-07-10 — `f-engagement-analytics` (21) **shipped** (t-1a #140, t-1b #141, t-2 #142, t-3 #143; claim + close-out this PR) — closing the entire Framework v1.1 follow-on epic (21/22/23 all shipped).**
  The last v1.1 follow-on — the _analytics_ half of the deferred-surfaces sweep ([[planning-retro#B28|B28]], the
  observability graveyard): `f-engagement` (08) shipped per-module stats but deferred dwell + a collective heat
  overlay + three emit sites, and `f-ops-views` (15) promised an overlay host slot it never shipped. **Reuse over
  reinvention, no new table** (new event `type` strings are not migrations, X1): **t-1 (#140 / #141)** `getMapHeat`
  — a per-node cross-user `groupBy nodeKey` over the insert-only `JourneyEvent` stream (map-scoped through
  `UserJourney`, A9 never a counter) + a generic **`overlay` slot** on the journey canvas (the host hook 15 owed) +
  a map-scoped heat/drop-off page, split at build on the API/UI seam ([[planning-retro#B25]]); **t-2 (#142)** the
  **dwell** metric — a sessionised `module.entered`→`node_completed` fold (gap-capped, two-pointer, no
  double-count) on `getModuleStats` + a Stats-tab card; **t-3 (#143)** the deferred **emit sites** —
  `module.completed` (all-`module`-node-complete, idempotent per (user, module), fired from the transition caller
  `applyJourneyTransition` **after commit**, the pure engine `applyEvent` untouched — F11) + `module.status_changed`
  → `runModuleWorkflowBindings` (fired from `updateModuleSettings`, **no `JourneyEvent`** — operator action, no
  subject user, so a separate `MODULE_LIFECYCLE_EVENT_TYPE` map out of the erasable stream). **`session.started`
  dropped** (decision D's own drop trigger fired: its session-gap check is an unbounded, un-indexed full-stream scan
  on the hot module-chat path — decision E ships no supporting index — and its semantics are fuzzier than the two
  committed emits; the surface chat route stays untouched; promote to its own feature if a real trigger appears).
  Pure framework-tier, no core edit, no migration. No feature depends on 21. **This closes the board — Framework v1
  (01–20) + v1.1 (21–23) are all shipped.** Lesson [[planning-retro#B32]]: the recon that reconciles the board sketch
  against the tree pays off — the "overlay prop" 15 promised genuinely did not exist, so t-1's real first move was
  _adding the host slot_, exactly as the feature doc's reconciliation predicted. Detail: [[f-engagement-analytics]].
- **2026-07-09 — `f-governance-plus` (23) **shipped** (t-1 #131 + review-fix #132, t-2 #134, t-3 #136, t-4 #137; claim #130 / docs #133 / close-out this PR).**
  The second v1.1 follow-on — the "additive when wanted, deferred by default" backend enhancements the shipped
  governance/eval/overlays features each named as later scope ([[planning-retro#B28|B28]], on the _backend_ this
  time). **Pure backend, no UI, nothing depends on it**, every piece reusing a shipped primitive: **t-1** widened
  the `StructureChangeProposal` gate beyond `map` to **`module_config` + `policy`** subjects (the `subjectType` seam
  f-emergence stubbed), applying through `saveModuleConfig` / `updateFacilitationPolicy` — all three subjects change
  an _existing_ target; **t-2** the **`submit_proposal`** authoring capability (an agent can now write a pending
  proposal, granted to the `facilitator` seat); **t-3** a **`framework_eval_sweep`** custom step type cron-ing the
  f-eval scorers + a seeded **framework rubric judge** over recent never-scored conversations (the fork-lawful
  scheduler seam, not a bare schedule); **t-4** **auto-embed-on-publish** (via a `map/publish-hooks` seam) + an
  **env-gated outbound-webhook nudge channel** alongside email. Two `framework_*` migrations (t-1 CHECK widen, t-3
  `rubricScore`/`rubricReasoning`); pure framework-tier throughout, no core edit. **`/code-review` earned its keep
  on every task** — the reuse-read-side policy bug (t-1), the required-arg + not-found contract (t-2), the eval
  watermark + reasoning-column (t-3), and the map/overlays import cycle → hook seam (t-4): [[planning-retro#B30]] /
  [[planning-retro#B31]]. That leaves **`f-engagement-analytics` (21)** the last claimable v1.1 follow-on. Detail:
  [[f-governance-plus]].
- **2026-07-09 — `f-admin-surfaces` (22) **shipped** (t-1 #124, t-2 #125, t-3 #126, t-4 #127 all merged; claim + close-out this PR).**
  The first v1.1 follow-on feature — the four admin surfaces the "deferred to `f-ops-views` (15)" handoff
  orphaned when 15 shipped narrower ([[planning-retro#B28|B28]] one level up). **UI-over-shipped-API, as the
  recon predicted**: **t-1** slot admin (definitions browser + values browser) built the feature's _one_ new
  endpoint — a sensitivity-masked `GET …/slot-values` over `getSlotHeads`, the isolated PII trust-boundary
  slice; **t-2** policy admin (the `FacilitationPolicy` CRUD UI, per-kind union form) and **t-3** proposal /
  emergence review (approve→publish / reject-with-reason queue) were **pure UI over shipped APIs, no new
  endpoint**; **t-4** the ops-views UX polish — searchable `?q=` roster pickers (past the `ROSTER_LIMIT=100`
  no-search cap) + per-binding `config`-override editing on the agents tab. **No migration, no core edit** —
  pure framework-tier throughout, exactly the 03/06/07 read-API-here / page-there split one more level up.
  `/code-review` again paid for itself on the UI-over-backend task ([[planning-retro#B29|B29]]): t-4's fixes
  were all **client↔server state coordination** — the roster hook cached a filtered list + an error and never
  reset them on form reopen (a failed load stuck the picker), and a narrowing search stranded a stale
  selection the submit still posted — each fixed as a transparent follow-up by resetting cached view-state on
  open. No feature depends on 22; **`f-engagement-analytics` (21)** and **`f-governance-plus` (23)** remain the
  claimable v1.1 follow-ons.

- **2026-07-08 — Deferred-surfaces sweep → three claimable v1.1 follow-on features on the board.** With
  the v1 epic all shipped bar `f-map-editor` (14), a sweep of every shipped feature's Follow-ups /
  Open-questions / does-NOT-do sections (two parallel doc passes + grep verification of what's actually
  built) separated **genuinely-outstanding, pickup-able** deferrals from deliberate non-goals, resolved
  questions, and already-fulfilled handoffs. The recurring failure: _"deferred to `f-ops-views` (15),"_
  which shipped narrower and orphaned several admin UIs (slot / policy / proposal) — the [[planning-retro#B28|B28]]
  graveyard pattern one level up (feature, not task). The real residue was grouped into three claimable
  follow-on features — **21 `f-engagement-analytics`** (the heat/drop-off overlays + dwell + emit sites,
  incl. the overlay-host prop the explorer canvas never got), **22 `f-admin-surfaces`** (the orphaned
  admin UIs + ops-views roster/config-override polish), **23 `f-governance-plus`** (additive backend
  enhancements) — so they run through the same claim-first → gate-loop flow. **Blocked** (tenancy
  #366/#367, §9.2 taxonomy), **upstream-to-file** (ledger), and **latent/scale-gated** deferrals were
  triaged out (kept in their existing homes, not given claimable rows). No code — a board-grooming pass.

- **2026-07-08 — `f-atlas` **shipped** (t-1 #104, t-2a #113, t-2b #114, t-3 #116 all merged; claim #102,
  close-out this PR).** The read-only framework composition view (spec §5.6, X8 — _a pure projection,
  zero new schema_). **Reuse over reinvention, end to end**: **no table, no migration, no core edit** —
  t-1's one endpoint (`GET …/atlas`) assembles the whole config by _widening the shipped per-module
  batch-stitch readers to all-modules aggregates_ into a normalized `CompositionProjection`; the client
  (t-2a) lays it out on the read-only `@xyflow` canvas the journey-explorer/map-builder already
  established, with **hub-and-spoke** layout, type-namespaced node ids, `map_module` edges collapsed to
  `map→module`, and click-through deep-links to the real editors (agent/workflow/module/**map** — 14's
  maps route landed mid-build, so map nodes wire it, not degrade). **t-2 split at build** into t-2a
  (static) + t-2b (semantic zoom — satellites unfold on zoom-in via `applyDetail`; a "Show all detail"
  toggle); **t-3** the cross-cutting lenses (pick an entity → highlight it + its 1-hop connections, dim
  the rest — the "where else is this agent used?" question). Code-review caught a real bug per task
  (t-1 dangling `module_capability` edge; t-2a edge-dedup dropping a dual-role agent + duping a React
  Flow id; t-2b initial-render flash from the pre-`fitView` zoom; t-3 a stale lens dimming the whole
  canvas) — each fixed as a transparent follow-up. **Region containers, a shared read-only-canvas
  primitive, and a shared `stitchById` helper were deferred** — promoted to the cross-cutting follow-ups
  below (a shipped feature's doc is a graveyard; the board is the actionable home — the "deferrals need an actionable home" principle).
  That leaves **`f-map-editor` (14, John)** the last feature building; every other framework feature is shipped.

- **2026-07-08 — `f-engagement` **shipped** (t-1 #103, t-2 #105, t-3a #106, t-3b #107 all merged).**
  The module event stream + stats + feedback (spec §4.3, A9 — _stats from an insert-only stream,
  never counters_), instrumenting the shipped module spine. **Reuse over reinvention**: it added
  **no table** — the `JourneyEvent` stream (created by f-journey-state, already stamping `moduleSlug`
  on `node_*` events) gained only new `type` values (X1, not migrations) + a read side; **no core
  edit, no migration** (pure framework-tier, the fifth). **t-1** (#103) — `recordModuleEngagement`,
  the emit seam: two isolated best-effort limbs (insert a `JourneyEvent` **and** fire-and-forget
  `runModuleWorkflowBindings`), wired at the module surface chat entry (`module.entered` on a _fresh_
  conversation). **This is the first production caller of the receiver f-module-bindings shipped
  deliberately unwired** — so an operator's "when a user enters this module, run workflow Y" finally
  fires (via `drainEngine`, not the outbound-webhook-only hook system). **t-2** (#105) — `record_feedback`
  (a `BaseCapability`; module attribution from the trusted `scope`, **never an arg** — a build-time
  security reconciliation of the plan's "arg fallback"; comment masked in provenance) + a plain
  `withAuth` feedback API; both write `module.feedback` to the stream (no core feedback table —
  `AiMessage.rating` is the wrong grain). **t-3** — module stats **derived from the stream** (unique
  users / entries / completions / returning / ratings summary), subject-scope-shaped (the #367 axis),
  **split at build (B25)** into **t-3a** (#106, `getModuleStats` + the `withAdminAuth` endpoint) and
  **t-3b** (#107, the read-only Stats tab on the shipped module-detail host). Every task through the
  full gate loop; **all four came back clean on `/security-review` + `/code-review`** (session/scope-
  derived identity, parameterized Prisma, React-escaped comments, failure-isolated instrumentation).
  Two honest scope calls: **dwell deferred** (a truthful metric needs sessionization, not a misleading
  `max−min` span) and **no `(moduleSlug, occurredAt)` index shipped** (a seq scan is fine at v1 scale;
  the index + raw `COUNT(DISTINCT)` are documented scale follow-ups, not speculative). A latent boundary
  gap surfaced + fixed in t-3a: the framework-tier **test** allowlist lacked
  `tests/**/app/api/v1/admin/framework/**` (the first admin-framework API-route test importing
  `@/lib/framework`) — added to `lib/framework/eslint.config.mjs`. **No `f-engagement` dependents —
  nothing new unblocks;** `f-map-editor` (14) is the single remaining unclaimed feature. Detail:
  [[f-engagement]]. Lesson: [[planning-retro#B27]].

- **2026-07-07 — `f-overlays` **shipped** (t-1 #93, t-2 #95, t-3a #96, t-3b #98 all merged).** The
  deferred **F9/F13** half of [[f-guidance]] (12) — two advisory overlays, both strictly _downstream of
  eligibility_ (F9 held throughout: a byte-identical-availability test is the load-bearing guard —
  similarity never moves what's eligible). The embedding / pgvector / scheduling / email machinery is
  **entirely Sunrise-core**; f-overlays is the thin framework adapter (reuse over reinvention, F14).
  **t-1** (#93) — `FrameworkNodeEmbedding` (`vector(1536)` + a hand-appended HNSW index; keyed
  `(graphSlug, nodeKey, version)` since map nodes are JSON in the version snapshot, not rows — so a
  republish naturally invalidates and staleness degrades to an empty advisory result, no `userId` → no
  erasure hook) + the composed node-text + on-demand `embedBatch` sync + admin re-embed trigger; the
  framework's **first Prisma-unmodelled object** needed a **third core→framework bridge** for its drift
  probe (`lib/app/db-drift.ts → lib/framework/db-drift.ts →` reserved-empty `leaf-db-drift.ts`,
  mirroring `bootstrap.ts`/`admin-nav.ts`). **t-2** (#95) — `findRelatedNodes` (cosine `<=>` within one
  `(graphSlug, version)`, self-excluded, threshold-gated, tie-broken by `nodeKey`) fills each
  `RankedMove.related` slot **after** `computeAvailability`/`rankMoves`, short-circuiting when nothing
  is embedded (graceful empty). **A plan reconciliation:** the planned dimension drift-guard (mirroring
  knowledge search's `assertActiveModelMatchesStoredVectors`) was **not needed** — similarity here is
  node-to-node over vectors from the _same_ sync run (one model, fixed dim), so there's no
  fresh-query-vs-stored comparison to guard. **t-3** (F13) **split at build per [[planning-retro#B1|B1]]**
  (the plan flagged it oversized): **t-3a** (#96) the compute half — a system-scoped
  `listStalledActiveJourneys` enumerator + `runProactiveGuidanceSweep` (per journey under the journey's
  own **self-viewer** so `canRead` holds per row; reuses the shipped deterministic `suggestFocus`, no
  new heuristic) + an admin **preview** trigger; **t-3b** (#98) delivery — the `framework_journey_nudge`
  throttle table (`journeyId` Prisma `@relation` cascade + a hand-FK `userId → user` cascade, the direct
  GDPR path; `@@unique(journeyId)` as the dedup key), **one email per user per sweep** (the nudge names
  no journey, so a multi-journey user gets one, not duplicates — a `/code-review` Medium, fixed) via a
  **dedicated `proactive-nudge.tsx`** template (plan said extend `event-notification.tsx`, but that's an
  admin/ops template — wrong tone), and `registerStepType('framework_proactive_guidance')` so an
  operator schedules the sweep on an `AiWorkflowSchedule` cron. **No workflow/schedule/embedding row
  seeded — a fresh fork boots clean.** Every task through the full gate loop; `/security-review` clean on
  all four (self-viewer scoping + both GDPR cascades + owner-scoped email verified), `/code-review`
  earned its keep on t-2 (tie-ordering) and t-3b (the per-user email-storm + best-effort
  throttle-recording). **Pure framework-tier — no upstream Sunrise issue, no core edit** (the drift
  bridge fills a reserved leaf scaffold, the step type registers through the existing open
  `registerStepType` seam). **No `f-overlays` dependents — nothing new unblocks.** Detail:
  [[f-overlays]]. Lesson: [[planning-retro#B26]].
- **2026-07-07 — `f-eval` **shipped** (t-1 #88, t-2 #90 all merged).** Governance's post-hoc face
  (spec §5.5, F14) — metric scoring + the post-hoc supervisor over the framework's own
  `facilitation`/`module` conversations, **split out of `f-emergence`** at its close-out. The
  scoring/supervision machinery is entirely Sunrise-core; f-eval is a thin **conversation-native
  adapter**. **t-1** (#88) — the `FrameworkConversationEval` store (framework-owned; `messageId`
  hand-FK → `ai_message` CASCADE, so erasable via the message — not the `userId`-owned
  `AiEvaluationSession`/`Log`) + the turn reader (pair user→assistant from `AiMessage`, citations from
  `provenance`, reusing the `capture.ts` pattern, filtered to `contextType ∈ {facilitation, module}`)
  - per-turn scoring via `scoreResponse` (the existing core `016` judges, `isSystem:true`, by slug — so
    **no new judge seed**, the planned judge-seed/`#303` task was dropped after recon) + the on-demand
    `withAdminAuth` trigger/read endpoints. **t-2** (#90) — the post-hoc supervisor: a
    conversation→`stepOutputs` adapter + a framework rubric/red-team + the engine-free `llmCall` shim
    (copied from the executions review route, boundary-mandated — its shared home would be Sunrise-owned
    `lib/orchestration/supervisor`) → `runSupervisorAssessment`, verdict stored on the eval row. Both
    through the full gate loop; `/code-review` earned its keep on t-2 (three real findings fixed — an
    escaped-quote citation mismatch that silently downgraded verdicts, a context-overflow risk from
    `includeStepOutputs:'all'`, and a mutable terminal-turn anchor that orphaned prior verdicts →
    re-anchored on the stable first turn). A fresh fork boots with an **empty** eval table (scored
    on-demand). Detail: [[f-eval]]. **No `f-eval` dependents — nothing new unblocks.** Lessons folded to
    [[planning-retro#B24]].
- **2026-07-04 — `f-slots` **shipped** (t-1 #19, t-2 #22, t-3 #24 all merged).** The Data-Slots
  layer — the third pure framework-tier feature (no Sunrise touch-point, no upstream issue). **t-1**
  (#19) — `SlotDefinition` model (`framework_slot_definition`, free-string vocabulary per X1) +
  module-declared registration (`slotDefinitions` on `ModuleDefinition`, scope-stamped `module:<slug>`)
  - the boot sync, which the build **refined from f-module-core's seed-once shape to a full reconcile**
    (a slot-definition row is a pure code projection with no operator columns, so authored edits must
    propagate); `/code-review` then caught two boot-reconcile defects — an unscoped deactivate that would
    silently retire a future global/facilitation slot, and a no-op guard keyed on the collected slot set
    rather than registered modules (both fixed in-PR, folded to [[planning-retro#B10]]). **t-2** (#22) —
    the insert-only `SlotValue` model (`supersededAt` head-read denormalisation, D4) + the pure value
    engine (`appendSlotValue` / `getSlotHeads`) + the hand-written `userId → "user"` FK cascade, whose
    first draft referenced the model name `"User"` and failed at apply — the core `User` model `@@map`s to
    lowercase `"user"` (folded to [[planning-retro#B11]]) — proven by a real-DB erasure smoke assertion.
    **t-3** (#24) — `GET /api/v1/admin/framework/slot-definitions` (`withAdminAuth`; second route under the
    framework admin-API namespace) + contract test. Every PR ran the full gate loop. A fresh fork boots to
    **empty** slot tables (Daybreak declares zero slots; leaf apps declare their own). Detail: [[f-slots]].
    `f-slots` is **shipped**, unblocking **`f-slot-capture`** (10).
- **2026-07-04 — `f-map` **shipped** (t-1 #16, t-2 #20, t-3 #21 all merged).** The authored facilitation
  map, second pure framework-tier feature (no Sunrise touch-point, no upstream issue). **t-1** (#16) — the
  pure Zod **format** (nodes / four edge types / three family-tagged condition families / region containers,
  F3–F5) + `validateMapFormat` (within-snapshot referential integrity); code-review caught `z.string().datetime()`
  being UTC-`Z`-only (fixed to `{ offset: true }`). **t-2** (#20) — `FacilitationGraph` + immutable
  `FacilitationGraphVersion` models (mirror `AiWorkflowVersion`; `createdBy` a bare `String`, no `User` FK) +
  the version service (create/draft/publish/rollback/read, slug-identified), publish gated by the composable
  `validatePublishableMap` chain **f-engine extends** with graph-invariant checks. **t-3** (#21) — the admin API
  under `/api/v1/admin/framework/maps/**` (all `withAdminAuth`, Zod bodies) + `api-schemas.ts` / `queries.ts`.
  Every PR ran the full gate loop; the `prisma migrate dev` DROP-INDEX footgun recurred and was stripped again.
  A fresh fork boots with **zero maps** (data-authored, not code-first). Detail: [[f-map]]. `f-map` is **shipped**,
  unblocking **`f-journey-state`** (09).
- **2026-07-03 — `f-module-core` **shipped** (t-1 #10, t-2 #11, t-3 #12 all merged).** The
  code-first module spine, entirely framework-tier (no Sunrise touch-point, no upstream issue). **t-1**
  (#10) — `ModuleDefinition` + `registerModule()` + registry + `framework_module` model + boot-time
  **set-based** `syncFramework()` (code-review rebuilt it from the planned per-slug upsert: that churned
  `updatedAt` every boot and `notIn:[]` mass-unregistered on an empty registry — now createMany +
  `isRegistered`-guarded updateManys + empty-registry no-op). **t-2** (#11) — pure
  `isModuleLive(module, flags, now, entitlement?)` (A5) returning a discriminated union, with the C1
  entitlement seam reserved; `MODULE_STATUS` vocabulary. **t-3** (#12) — `GET
/api/v1/admin/framework/modules` (`withAdminAuth`; first `app/api/v1/admin/framework/` route — first
  file to exercise that tier's ESLint glob) + `listModules()` + an end-to-end register→sync→read
  visibility test (stateful in-memory Prisma fake). Every PR ran the full gate loop (`/pre-pr` →
  `/security-review` → `/code-review`); code-review caught real defects on #10 and #12. Sizing: the
  spec's 4 indicative tasks folded to 3 promoted PRs ([[planning-retro#B1]]). A fresh fork still boots to
  an empty modules table. Detail: [[f-module-core]]. `f-module-core` is **shipped**, unblocking
  `f-module-config` / `f-module-bindings` / `f-engagement`.
- **2026-07-03 — `f-bootstrap` shipped (t-2 #8, t-3 #9).** The enforced boundary (X6) and the boot seam.
  **t-2** (#8) — framework↔Sunrise boundary via **fork-first** ESLint/CI seams (fork-owned
  `lib/framework/eslint.config.mjs` + reserved leaf seam + a one-line root spread + an `--if-present` CI
  hook), not direct edits to platform-owned config; `scripts/boundary/` proves it bites. **t-3** (#9) —
  `initFramework()` + the generic `initApp()` **core→fork boot seam** (`instrumentation.ts` → reserved
  `lib/app/bootstrap.ts` → `@/lib/framework`, dynamic-import so a framework-less fork still builds),
  with failure-isolation added in code-review. Both seams built fork-first as their final generic shape;
  upstream Sunrise issues **#382** (ESLint/CI seams) and **#385** (boot seam + `/framework` reservation)
  filed with the fork-build learnings for the Sunrise agent to implement. `f-bootstrap` is **shipped**;
  execution lessons captured in [[planning-retro]] §B (B5–B9).
- **2026-07-02 — `f-bootstrap` started; t-1 (framework skeleton) merged.** `lib/framework/{modules,facilitation,data-slots,shared}/` stood up, with `shared/scope.ts` (the one scoping vocabulary) as the only real logic; three empty `framework-*.prisma` files; a scope test. Merged via **PR #6** (all gates green: pre-pr + code-review clean). Sizing note: t-1 landed commit-sized rather than PR-sized — captured as process feedback in [[planning-retro]].
- **2026-07-02 — `f-seams` shipped, and Sunrise v0.5.0 merged into the fork.** The two generic core seams were filed as Sunrise **#372**, implemented upstream in **PR #373** (`CapabilityContext.scope?: Record<string,string>` + the `registerContextContributor()` registry on `buildContext()` + the empty fork-owned `lib/app/context-contributors.ts` scaffold), and released in **Sunrise v0.5.0**. Merged into this Daybreak fork via **PR #4** — `SUNRISE_VERSION → 0.5.0` while Daybreak's app version stays `0.1.0`; three trivial conflicts (`package.json`, `package-lock.json`, `CLAUDE.md`) resolved keeping the fork's identity + banner; no new migrations. Type-check / lint / format green, 64 seam tests pass. `f-seams` is **shipped**; `f-bootstrap` is unblocked and next.
  - _Note on ownership:_ scoped in the plan as "2 PRs, owner Simon (Sunrise)"; delivered as us filing the Sunrise issue and upstream implementing it as a single PR — the seams are generic, so they belong to Sunrise, exactly as intended.

---

## References

- [[planning-retro]] — feedback about the **plan-authoring process itself**, discovered while executing this plan, split by target: **§A overall-plan authoring** (the priority — it feeds the HCE Hub's plan-authoring process) and **§B feature-plan authoring**. To fold back into the agent instructions that generate plans like this one.
- [[framework-architecture]] — the authoritative design spec (rev 16). Binding decisions in Appendix A.
- [[placement-decision-memo]] — why the framework lives on a separate fork.
- [[v1-requirements|HCE Hub v1 requirements]] — the Project → Phase(epic) → Feature → Task model this plan mirrors.
- [[plan|ConQuest plan]] — the sibling plan; this one carries its task-sizing lesson.
- [[building-on-sunrise]] — how a fork relates to Sunrise upstream (applied one level up).
