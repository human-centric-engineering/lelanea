# Dependencies
node_modules

# Build outputs
.next
out
build
dist

# Caches
.cache
.turbo
.eslintcache
.prettiercache

# Package manager files
package-lock.json
yarn.lock
pnpm-lock.yaml

# Environment
.env*
!.env.example

# Generated files
*.generated.*
.vercel
.swc

# Prisma migrations (generated SQL)
prisma/migrations/**/*.sql

# Public assets
public

# Documentation
CHANGELOG.md

# Coverage
coverage
.nyc_output

# Agent tooling artefacts — specific format preserved for AI agents
# The remediation ledger uses column-aligned pipe tables that Prettier collapses,
# which breaks the deterministic read-parse-write round-trip used by /test-triage.
.claude/testing/remediation-ledger.md

# Per-developer local Claude Code settings (gitignored, written by the CLI).
# Format is owned by the tool, so Prettier should not gate `npm run validate` on it.
.claude/settings.local.json

# Authored planning material (leaf-owned, Lelañea).
# The product description, the approved prototype (`design/lelanea.html`), and
# the exported `Lelanea_Design_System` kit are source inputs the build works
# from, not source the build maintains — the bundle README states they are
# "transcribed ... corrected only for typography. Not a draft for the build to
# improve on." Reformatting them would rewrite authored copy and a signed-off
# prototype. `lelanea.html` also fails Prettier's HTML parser outright
# (implied-end-tag SyntaxError), which exits 2 and fails the whole check.
.context/app/planning/
